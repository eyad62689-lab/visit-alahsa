---
// الرئيسية الألمانية (de-DE) — نصوصها من قاموس ui.de المعتمد عبر خط الترجمة،
// وما لم يُترجم بعد (map/blog/plan) يتراجع de ← en ← ar فيظهر بالإنجليزية
// وروابطه تقصد الصفحات الإنجليزية — تسمية إنجليزية لصفحة إنجليزية، لا وعدٌ مكسور.
import HomeView from '../../components/views/HomeView.astro';
const alt = { ar: '/', en: '/en/', zh: '/zh/', de: '/de/', ru: '/ru/' };
---
<HomeView lang="de" alt={alt} />
