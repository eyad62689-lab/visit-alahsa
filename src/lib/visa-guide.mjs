// ثوابت صفحة «دليل التأشيرات» — مصدرٌ واحد للواجهة (VisaGuideView) وللحارس C28
// (tools/check-visa-guide.mjs). الملف ‎.mjs لا ‎.ts عمداً: يستورده الحارس في Node ضمن
// postbuild، على سابقة src/lib/ai-referrers.mjs.

/** مسار الصفحة — عربي فقط حتى يوفّر إياد النص الإنجليزي. بشرطة ختامية (تطابق canonical). */
export const VISA_GUIDE_PATH = '/دليل-التأشيرات/';

/** سطر التحديث كما أملاه إياد حرفياً (ليس في «الجزء الثاني»). */
export const VISA_UPDATED_LINE = 'تاريخ آخر تحديث: 26 سبتمبر 2026';

/** ملاحظة التحرير — في معاينة النشر وطلب الدمج فقط، ولا تظهر في الإنتاج (قرار إياد). */
export const VISA_PREVIEW_NOTE = 'يجب فتح كل رابط يدوياً قبل النشر';

/** «لم يُتحقَّق» بترتيب علاماته كما في المصدر (الفتحة قبل الشدّة) — بمحارف مهرَّبة كي لا
 *  يعيد محرّرٌ ترتيب التشكيل فيفوت المطابقةَ بصمت. */
export const UNVERIFIED = 'لم يُتحقَّق';
