# تشخيص التفاعل العالمي لموقع «زوروا الأحساء» وخطة التحسين

تاريخ الفحص: 2026-09-08 · الالتزام المفحوص `c71bbd8` وهو نفسه المنشور على الإنتاج (نشر Netlify رقم `6a9fc747`، 2026-09-08 08:29 UTC) · هذا الملف مرجع التنفيذ، والنسخة Word مطابقة له.

## 1. الملخص التنفيذي

1. **الحكم:** الموقع سليم تقنياً (بناء أخضر بـ24 فحص تضارب، إتاحة وسيو 100/100 على 12 صفحة مقيسة، 0 رابط مكسور من 20,296 مرجعاً داخلياً)، لكنه يستقبل الزائر العالمي بالعربية، ويعلن نسخاً بلغات لم تكتمل، ويبطئ على الجوال في أول ثانية، ولا يقيس بقاء الزائر ولا فعله.
2. **التدخل الأول:** استقبال الزائر بلغته: شريط اقتراح اللغة، وصفحة 404 بلغة المسار، وبيان تطبيق لكل لغة.
3. **التدخل الثاني:** تسريع الشاشة الأولى على الجوال إلى LCP ≤ 2.5 ثانية بتأجيل الفيديو وحصر الخطوط السيريلية وتضمين CSS.
4. **التدخل الثالث:** إجابات قابلة للاستخراج لمحركات الذكاء الاصطناعي من البيانات الموثّقة القائمة (جداول، مواعيد مبنيَنة، فقرة إجابة، صفحة اليونسكو).
5. **الخطة:** 10 بنود في 10 خطوات، كلها كود أو محتوى أو تهيئة داخل المستودع وخدماته القائمة، بلا مصدر خارجي جديد ولا حساب ولا مفتاح، وتبدأ بالقياس لأن كل مؤشر أدناه يُقرأ منه.

### أساس الأدلة وفجواته

- **الأدوات:** بناء كامل محلي (366 صفحة)؛ Lighthouse 12.8 على Chrome 141 بمحاكاة جوال لـ12 صفحة من البناء؛ سكربتات فحص على `dist/` (روابط، hreflang، JSON-LD، صور OG، أوصاف الميتا، نسبة النص غير المترجم)؛ قراءة المصدر و`docs/` وبيانات نشر Netlify وسجل GitHub Actions.
- **فجوة 1:** الموقع الحي وواجهة PageSpeed محجوبان شبكياً من بيئة الفحص؛ البديل بناء الالتزام المنشور نفسه فالصفحات مطابقة، وسلوك Netlify (13 قاعدة رؤوس، 0 تحويل) مأخوذ من ملخص النشر.
- **فجوة 2:** بيانات GA4 وSearch Console وسجلات Blobs غير متاحة بلا اعتماد، فلا خط أساس سلوكي اليوم؛ يُسجَّل بعد الخطوة 1.
- **فجوة 3:** وجود مفتاح Places على الإنتاج مثبت وثائقياً (سجل التنفيذ 2026-09-04) لا بالمشاهدة؛ الفحص المحلي بُني بلا تقييمات.

## 2. أهم خمسة عيوب (بالخطورة) ثم خمس فرص (بالأثر)

### ع1. الزائر غير العربي يهبط على العربية بلا توجيه

- **الدليل:** `netlify.toml` بلا `[[redirects]]` وملخص النشر «No redirect rules processed»؛ `navigator.language` غير مستعمل في `src/`؛ `x-default` يشير إلى الجذر العربي؛ `site.webmanifest` بلغة `ar`؛ صفحة 404 عربية.
- **التبرير:** كل وصول مباشر أو من إجابة ذكاء اصطناعي أو رابط مشارَك يضع القارئ الأجنبي أمام صفحة عربية معكوسة الاتجاه فتنتهي الجلسة قبل أن تبدأ.
- **الخطوات:** 1) مكوّن `LangSuggest.astro`: شريط واحد غير حاجب يقارن `navigator.languages` بلغة الصفحة ويربط النظير من `alt`، يُغلق ويُحفظ محلياً داخل `try/catch` ويصمد عبر ClientRouter. 2) صفحة 404 بلغة بادئة المسار. 3) بيان تطبيق لكل لغة. لا تحويل بالخادم لئلا تُحجب النسخ عن الزواحف.
- **المؤشر:** `lang_suggest_click` ÷ جلسات المتصفحات غير العربية على الصفحات العربية ≥ 30% خلال 30 يوماً.
- **الجهد:** طلب دمج واحد.

### ع2. النسخ الصينية والروسية والألمانية تُعلن كاملة وهي مختلطة

- **الدليل (مقيس على dist):** نص مطابق حرفياً للإنجليزية: zh 13%، ru 12%، de 29% (فهرس المعالم 50%)؛ مفاتيح الواجهة zh 191 وde 145 وru 146 من 200؛ `<title>` إنجليزي على فهرسَي المطاعم والإقامة الصينيين؛ `/zh/blog/` مقالات إنجليزية؛ 8 من 10 روابط تنقّل de/ru تقصد `/en/`؛ وhreflang معلَن لـ140 صفحة بهذه اللغات.
- **التبرير:** القارئ يصطدم بالإنجليزية في منتصف صفحة بلغته فيغادر، والمحركات ترى تعارض اللغة المعلَنة.
- **الخطوات:** 1) فحص `C22` يقيس النص غير المترجم لكل صفحة zh/de/ru ويحجب hreflang وsitemap والمبدّل عمّا يتجاوز 20%. 2) عنوانا الفهرسين ووصفاهما عبر خط الترجمة القائم. 3) حجب `/zh/blog/` حتى يصل مقال صيني (ف5).
- **المؤشر:** C22 أخضر؛ وقت التفاعل على `/zh/` و`/ru/` من الخطوة 1.
- **الجهد:** طلبا دمج.

### ع3. الشاشة الأولى على الجوال أبطأ من العتبة

- **الدليل (Lighthouse جوال):** الرئيسية العربية أداء 73 وLCP 4.9 ث، والرئيسيات الأخرى 2.9 إلى 3.5، وجبل القارة 2.9؛ كلها فوق 2.5. الأسباب: فيديو الهيرو (1.26 ميغابايت) يبدأ عند 450 مللي ثانية قبل رسم LCP؛ 6 خطوط سيريلية (73 كيلوبايت) في كل صفحة بكل لغة لأن وجوه fontsource بلا `unicode-range`؛ 3 ملفات CSS حاجبة (400 مللي ثانية).
- **التبرير:** أول ثانيتين تقرران بقاء زائر الجوال العالمي.
- **الخطوات:** 1) `HeroVideo.astro`: التحميل بعد `load` ثم `requestIdleCallback`، ويُتخطّى عند `saveData` أو شبكة دون 4g. 2) الوجوه السيريلية في `fonts.css` بـ`unicode-range`. 3) تجربة `inlineStylesheets: 'always'`. 4) بوابة Lighthouse في `build.yml` بعتبة LCP ≤ 2.5 ث.
- **المؤشر:** LCP ≤ 2.5 ث على الرئيسيات الخمس وقالب المعلم، وميدانياً عبر `web_vitals`.
- **الجهد:** طلبا دمج.

### ع4. لا قياس للبقاء والفعل

- **الدليل:** أحداث GA4 في الكود: `page_view` و`ai_referral` وأربعة أحداث لـ«رحلتي» و`map_3d_opened`؛ لا حدث للبحث أو تبديل اللغة أو حفظ معلم أو فتح خرائط قوقل أو الفيديو أو المشاركة؛ لا قياس ميداني لمؤشرات الويب الحيوية.
- **التبرير:** «يبقى ثم يفعل» لا يُقرأ اليوم من أي تقرير، فلا يمكن الحكم على أي تدخل.
- **الخطوات:** 1) `src/lib/analytics.ts` بدالة `track` تحرس `gtag` وتصمد عبر ClientRouter. 2) عشرة أحداث للبحث والتبديل والحفظ والخرائط والفيديو والفلاتر والمشاركة والروابط و«موقعي» واقتراح اللغة. 3) حزمة `web-vitals` ترسل LCP وINP وCLS حدثاً واحداً مع `lang` و`ai_source`. 4) اختبار Playwright يثبت وصولها إلى `dataLayer`.
- **المؤشر:** الأحداث العشرة في تقرير أحداث GA4 خلال 48 ساعة، وخط أساس مسجَّل في `docs/` بعد 14 يوماً.
- **الجهد:** طلب دمج واحد.

### ع5. صفحات رقيقة مفهرسة بأربع لغات وبطاقات مشاركة عربية للصفحات الأجنبية

- **الدليل:** 5 صفحات معالم بمتن من 6 إلى 13 كلمة (الاستاد، الملك عبدالله، السوسن، الفراولة، الليمون) مبنية بثلاث لغات أو أربع في sitemap؛ 25 متناً عربياً دون 80 كلمة و24 إنجليزياً دون 100؛ 96 صفحة بصورة `og-default.png` العربية النص، منها فهارس EN وzh و40 صفحة منشأة لها صورتها.
- **التبرير:** نسخ رقيقة بأربع لغات تُضعف ثقة المحركات بالنطاق، ومشاركة الصفحات الإنجليزية تظهر ببطاقة عربية.
- **الخطوات:** 1) عتبة نشر كعتبة المنشآت: دون 40 كلمة تبقى بطاقة، وتخرج صفحتها من sitemap وllms.txt بـ`noindex` حتى يصل متن مصدري. 2) تمرير `image` في قالبي المنشأة والإقامة. 3) `og-default-{lang}.png` بخمس لغات وقت البناء بـ`sharp` المثبّتة. 4) فحص `C23`.
- **المؤشر:** 0 صفحة مفهرسة دون العتبة، وC23 أخضر.
- **الجهد:** طلبا دمج.

### ف1. مشاركة بضغطة وروابط عميقة للخريطة

- **الدليل:** `navigator.share` غير مستعمل في أي ملف؛ المشاركة موجودة في «رحلتي» فقط (3 صفحات: واتساب ونسخ الرابط)؛ `MapView.astro` بلا حالة في الرابط فلا تُشارَك خريطة مفلترة ولا علامة بعينها، بينما فهرس المعالم يدعم `?cat=`.
- **التبرير:** «يشارك» أحد أفعال التفاعل المطلوبة، ومشاركة الجوال بلا زر تكاد تكون صفراً.
- **الخطوات:** 1) مكوّن `ShareButton.astro` بواجهة Web Share وتراجع «نسخ الرابط» في قوالب المعلم والمنشأة والفعالية والمقال بنصوص اللغات الخمس عبر `localize`. 2) حالة الخريطة في الرابط (`?cat=`، `?q=`، `#slug`) تُقرأ عند التحميل وتُكتب بـ`history.replaceState`. 3) حدثا `share` و`map_deeplink`.
- **المؤشر:** حدث `share` لكل 100 جلسة ≥ 2 خلال 30 يوماً، ودخول عبر روابط خريطة عميقة أكبر من 0.
- **الجهد:** طلب دمج واحد.

### ف2. «بالقرب مني» على الخريطة وصفحة المعلم

- **الدليل:** `Permissions-Policy: geolocation=()` في `netlify.toml` و`public/_headers` يعطّل تحديد الموقع كلياً؛ لا `geolocation` في `src/`؛ صفحة المعلم تحسب «القريب» من إحداثيات المعالم فقط (58 من 58 لها إحداثيات).
- **التبرير:** الزائر على الأرض يسأل أولاً «ما القريب مني الآن؟» وهو أعلى فعل في خريطة سياحية، والموقع لا يغادر الجهاز.
- **الخطوات:** 1) `geolocation=(self)` في الملفين. 2) زر «موقعي» في `MapView` عبر `map.locate` يرتّب القائمة بالمسافة ويضع علامة الزائر بلا تخزين. 3) في `DetailView` زر اختياري «كم يبعد عني» يحسب المسافة محلياً. 4) اختبار Playwright بصلاحية موقع محاكاة.
- **المؤشر:** حدث `map_locate` ÷ جلسات الخريطة على الجوال ≥ 15%، وعمق التصفح بعد الخريطة.
- **الجهد:** طلب دمج واحد.

### ف3. إجابات قابلة للاستخراج لمحركات الذكاء الاصطناعي

- **الدليل:** 0 جدول في 367 صفحة؛ بنود الدراسة 4.1 و4.2 و3.3 و5.2 «ينتظر دوره» في سجل التنفيذ؛ البيانات جاهزة: 33 بنداً موثّقاً في 25 معلماً، مواسم 10 فعاليات والثمار، 6 مكوّنات يونسكو؛ `llms.txt` عربي الشرح.
- **التبرير:** محركات الإجابة تقتبس الجداول والجمل الأولى المباشرة.
- **الخطوات:** 1) ثلاثة جداول مولَّدة من البيانات: مواعيد ورسوم الموثّق، روزنامة الفعاليات، مواسم الثمار. 2) `openingHoursSpecification` و`isAccessibleForFree` للبنود الموثّقة. 3) حقل `answer` (35 إلى 45 كلمة من المتن والبطاقة) لأهم 15 معلماً يتصدّر الصفحة. 4) صفحة مكوّنات اليونسكو من المحتوى القائم بلا حقيقة جديدة. 5) `llms.txt` بسطر إنجليزي مواز. 6) فحص `C24`: كل رقم وارد في مصدره.
- **المؤشر:** جلسات `ai_referral` الأسبوعية ترتفع 50% خلال 90 يوماً، وC24 أخضر.
- **الجهد:** 3 طلبات دمج.

### ف4. ربط داخلي بين المعالم والمقالات والمنشآت

- **الدليل:** كل مقال إنجليزي يربط من 4 إلى 12 معلماً ولا معلم يربط المقالات التي تذكره؛ صفحة المنشأة بلا رابط سوى العودة وخرائط قوقل؛ صفحة المعلم تعرض «القريب» من المعالم فقط.
- **التبرير:** عمق التصفح يزيد بالروابط السياقية، ومحركات الإجابة تتتبعها لتكوين الإجابة.
- **الخطوات:** 1) وقت البناء: فهرس عكسي «مقالات تذكر هذا المعلم» يُعرض في `DetailView`. 2) في صفحة المنشأة قسم «معالم في الحي نفسه» بربط `district` القائم بحقل `district` يُضاف للمعالم التي يذكر متنها حيّها (الكوت، وسط الهفوف) ولا يُضاف لغيرها. 3) حدث `related_click`. 4) فاحص روابط في `postbuild`.
- **المؤشر:** صفحات لكل جلسة ≥ 2.5 للجلسات غير العربية، و`related_click` ÷ مشاهدات المعلم ≥ 8%.
- **الجهد:** طلب دمج واحد.

### ف5. فتح المدونة للغات الثلاث بنيوياً

- **الدليل:** مخطط المدونة يحصر `lang` في `ar|en` فلا مقال بلغة ثالثة ممكن؛ `/zh/blog/` يعرض المقالات الإنجليزية (45% من نصه إنجليزي)؛ المقالات الإنجليزية أطول محتوى في الموقع (422 إلى 1997 كلمة) مقابل متون المعالم (160 فأقل)، ومسارا 24 و48 ساعة هما ما يبحث عنه الزائر الأجنبي.
- **التبرير:** أعلى أثر لكل ساعة ترجمة، لأن مقالاً واحداً يجيب عن رحلة كاملة.
- **الخطوات:** 1) توسيع `lang` إلى اللغات الخمس، وصفحات `[slug]` تحت `/zh/blog/` و`/de/blog/` و`/ru/blog/`، واقتران `key` في sitemap وhreflang. 2) ترجمة مقالي 24 و48 ساعة بالخطوط الثلاثة القائمة بعتبة 90/100. 3) إدراج `/zh/blog/` في hreflang عند أول مقال صيني.
- **المؤشر:** 6 مقالات مترجمة خلال الدورة ووقت تفاعل ≥ 90 ثانية عليها.
- **الجهد:** طلب دمج بنيوي و3 جلسات ترجمة.

## 3. خارطة التنفيذ

| # | الخطوة | يعتمد على | فحص القبول الآلي |
|---|---|---|---|
| 1 | ع4 القياس: `analytics.ts` والأحداث العشرة و`web-vitals` | لا شيء | اختبار Playwright يثبت وصول كل حدث إلى `dataLayer`، و`npm run build` أخضر |
| 2 | ع1 استقبال الزائر بلغته: الشريط و404 والبيان | 1 | Playwright بلغة متصفح de وzh وen على `/` يُظهر الشريط بالرابط الصحيح، ولا يُظهره عند التطابق، ويُبقيه مغلقاً بعد الإغلاق؛ axe 0 مخالفات |
| 3 | ع3 الأداء: تأجيل الفيديو والخطوط السيريلية وCSS | لا شيء | بوابة Lighthouse في CI: LCP ≤ 2.5 ث على 6 صفحات؛ 0 ملف سيريلي في طلبات `/en/map/` |
| 4 | ع5 عتبة النشر وصور OG بلغة الصفحة | لا شيء | فحص `C23`؛ sitemap وllms.txt بلا صفحة دون العتبة؛ C1 وC2 وC3 كما هي |
| 5 | ع2 بوابة اللغة `C22` وعناوين الفهرسين الصينية | 4 | `C22` ≤ 20% لكل صفحة معلَنة؛ فاحص hreflang متبادل بلا خلل |
| 6 | ف1 المشاركة وروابط الخريطة العميقة | 1 | Playwright: الزر في القوالب الأربعة، وحالة الخريطة تُكتب وتُقرأ ذهاباً وإياباً |
| 7 | ف2 «بالقرب مني» | 1 و6 | Playwright بموقع محاكاة يرتّب القائمة بالمسافة؛ `dist/_headers` يحمل `geolocation=(self)` |
| 8 | ف3 الجداول والمواعيد المبنيَنة وفقرة الإجابة وصفحة اليونسكو وllms.txt | 4 | فحص `C24`؛ JSON-LD صالح في كل الصفحات؛ C4 وC21 كما هي |
| 9 | ف4 الربط الداخلي | 8 | فاحص الروابط 0 مكسور؛ كل معلم مذكور في مقال يحمل القسم |
| 10 | ف5 المدونة متعددة اللغات | 5 | حاكم خطوط الترجمة ≥ 90؛ `C22`؛ hreflang متبادل؛ sitemap |

كل خطوة طلب دمج مستقل يمر ببناء CI الحالي (البناء والفحوص الأمنية) قبل الدمج، وبعد الخطوة 1 بأربعة عشر يوماً يُسجَّل خط الأساس في `docs/` ثم تُقاس كل خطوة عليه.

## 4. قائمة «لا يُمس»

- **فاحص التضارب على `dist/`** بفحوصه الـ24 (الأعداد المشتقة، الأرقام اللاتينية، الإسناد، JSON-LD بلا تقييمات، sameAs، الأسئلة الشائعة): أي فحص جديد يُضاف إليه ولا يُستبدل.
- **بنية الروابط وhreflang:** الشرطة الختامية من `routes.ts`، وتبادل hreflang على 366 صفحة، و0 رابط مكسور، وsitemap بـ356 رابطاً كلها بـ`lastmod` حقيقي، وIndexNow بعد كل نشر.
- **قاعدة «لا معلومة بلا مصدر»** وحجب `verified: false` في الكود، ومخطط `practical` الذي يرفض البناء بلا إسناد.
- **الأداء المُحكم القائم:** الخطوط المستضافة ذاتياً بـ`font-display: swap` وبدائل مضبوطة المقاس (CLS ≤ 0.011 في كل الصفحات المقيسة)، وصور `srcset` بأربعة عروض، وفيديو الهيرو بـ`preload=none` وزر إيقاف، والخرائط بلا طرف ثالث إلا البلاطات، وMapLibre المحمَّل كسولاً.
- **الإتاحة والسيو 100/100** في 12 صفحة، والرؤوس الأمنية، وسياسة `robots.txt` (حجب زواحف التدريب وإبقاء زواحف البحث والإجابة).
- **«رحلتي»** بمشاركة القائمة عبر الرابط والاستيراد والطباعة، وقياس إحالات محركات الذكاء الاصطناعي (`ai_referral` مع 16 مصدراً).
- **خطوط الترجمة الثلاثة** بعتبة 90/100 ومعاجمها الملزمة، وبحث Pagefind بخمس لغات، وCI (البناء والفحوص الأمنية أخضران على آخر 8 تشغيلات).

## 5. سجل التنفيذ

| الخطوة | التاريخ | ما نُفِّذ | فحص القبول |
|---|---|---|---|
| 1 | 2026-09-08 | `src/scripts/analytics.ts` (وحدة مُجمَّعة تُحمَّل من `Base.astro`): `window.VATrack` وسمة `data-track` العامة، وتفويض للأحداث القائمة (`search_open`، `search_result`، `lang_switch`، `plan_add`/`plan_remove`، `maps_open`، `filter_use`، `video_play`) و`web_vitals` (LCP وINP وCLS وTTFB عبر حزمة `web-vitals`) مع `lang` و`ai_source` في كل حدث. **اكتشاف جانبي أُصلح:** `gtag` لم تكن عامةً قط لأن `define:vars` يغلّف السكربت، فكانت أحداث «رحلتي» و`map_3d_opened` تُسقَط بصمت؛ أُضيف `window.gtag = gtag`. | `tools/test-analytics-events.mjs` على `dist` بمتصفح حقيقي: 18/18 شرطاً (بناء محلي بلا أوقات حيّة، فزر «مفتوح الآن» يُتخطّى)؛ `npm run build` أخضر (0 أخطاء، 0 تحذيرات، 24 فحصاً)؛ الفحص أُضيف خطوةً في `build.yml`. |
| 2 | 2026-09-08 | `LangSuggest.astro` من `Base.astro` لكل صفحة لها `alt`: يقيّم `navigator.languages` مرة في الجلسة، ويقترح أول لغة مفضّلة تسبق لغة الصفحة ولها نظير (شريط ثابت أسفل الشاشة بلا CLS، يُرفض ويُحفظ الرفض في `localStorage`)؛ نصوص ar/en في `ui.ts` (`lang.suggest.*`) وzh/de/ru باسم اللغة الأصلي حتى تمرّ بخطوط الترجمة. صفحة 404 تكشف كتلة لغة بادئة المسار (نصوص zh/de/ru من مفاتيح معتمدة). بيان تطبيق لكل لغة (`/{lang}/site.webmanifest`) و`Base` يربطه. `LANG_META` صار مصدراً واحداً في `src/i18n/langs.ts`. | `tools/test-lang-suggest.mjs`: 35/35 (سيناريوهات اللغات، الرفض والقياس، 404 بخمس لغات، البيانات الخمسة، axe-core 0 مخالفات على 4 صفحات)؛ `test-analytics-events.mjs` 18/18؛ `npm run build` أخضر (24 فحصاً). |
| 3 | 2026-09-08 | (1) فيديو الهيرو لا يبدأ إلا بعد `load` ثم خمول، وعلى الجوال بعد 3 ثوانٍ أخرى، ويُتخطّى على `saveData` و2g/3g (كان يبدأ عند 450 مللي ثانية). (2) الوجوه السيريلية تُعلَن في `fonts.css` بـ`unicode-range` (ملفات fontsource الفرعية بلا نطاق) — 0 ملف سيريلي خارج `/ru/` بدل 5–6 في كل صفحة. (3) خطوط Plex العربية مُشذَّبة إلى الكتلة الأساسية (الموقع يستعمل 50 محرفاً منها): 133 → 90 كيلوبايت للأوزان الثلاثة. (4) `inlineStylesheets: 'always'`: 0 ملف CSS حاجب بدل 3. (5) إزالة `text-rendering: optimizeLegibility` (قياس A/B على الرئيسية العربية: الأداء 81 → 88). (6) الصور الكسولة `fetchpriority="low"`. **المقيس على المحاكاة:** الرئيسية العربية 17 ملف خط/365 كيلوبايت → 11/250، والإنجليزية 14/239 → 8/166؛ حجم الصفحة وقت الفحص 1985 → 615 كيلوبايت (عربي) و1788 → 461 (إنجليزي). | `tools/lighthouse-gate.mjs` (وسيط 3 تشغيلات) بميزانيات حتمية: لا CSS حاجب، لا سيريلي خارج /ru/، خطوط ≤ 12 ملفاً و≤ 270 كيلوبايت، لا فيديو قبل LCP، صورة LCP محمَّلة مسبقاً؛ وحارسا محاكاة خشنان (LCP ≤ 4 ث، أداء ≥ 80). **انحراف مُعلَن عن الخطة:** عتبة LCP ≤ 2.5 ث لا تصلح بوابةً في المحاكاة لأن البناء نفسه قاس 1.6 و3.6 ثانية على الآلة نفسها خلال 10 دقائق (Lantern يستند إلى أثر CPU فعلي متقلّب)؛ فالهدف 2.5 ث يُقرأ ميدانياً من `web_vitals` (الخطوة 1) والبوابة تحرس الأسباب. البوابة والاختباران المتصفحيان في `build.yml`. |
| 4 | 2026-09-08 | عتبة نشر المعالم في `src/lib/publish.ts` (المتن + الأسئلة الشائعة دون 40 كلمة بالعربية أو الإنجليزية): الصفحة تبقى للزائر لكنها `noindex` بلا hreflang (`Seo.astro`) وخارج sitemap في كل لغاتها — 5 معالم (حديقة الاستاد، منتزه الملك عبدالله، حدائق السوسن، مزرعتا الفراولة والليمون) و17 رابطاً خرجت من الخريطة (356 → 339). بطاقات المشاركة: `og-default-{en,zh,de,ru}.png` مولَّدة بـ`tools/gen-og-images.mjs` (sharp) من مفتاح `home.hero.eyebrow` المعتمد، و`Seo.astro` يختار الافتراضية بلغة الصفحة، وصفحات المنشآت والإقامة والفعاليات تمرّر صورتها أو تتركها للافتراضية بلغتها (كشف الفحص فعالية «شتاء الوفرة» ببطاقة عربية على صفحتيها الإنجليزية والصينية). | فحص `C23` على dist: المحجوب noindex وبلا hreflang وخارج sitemap، وغير المحجوب مفهرس وداخلها (231 صفحة معلم)؛ و341 صفحة مفهرسة كلها بصورة OG بلغتها (47 منها بالافتراضية)، والملفات الخمسة موجودة. `npm run build` أخضر (25 فحصاً)؛ الاختباران المتصفحيان أخضران. |
| 5 | 2026-09-08 | فحص `C22` على dist: نسبة الكتل الإنجليزية المطابقة للنظيرة الإنجليزية في كل صفحة zh/de/ru معلَنة (أسماء الأعلام والأرقام مستثناة) بعتبة 20%. `src/i18n/unlisted.ts` يُخرج ما يتجاوزها من hreflang وsitemap ومبدّل اللغة وشريط الاقتراح: `/zh/blog/` (91%، وnoindex حتى أول مقال صيني) و`/de/attractions/` (23%: 52 من 58 بطاقة إنجليزية). كتلة الموسم (`SeasonNow`) تُخفى على الرئيسيتين الألمانية والروسية بدل عرضها إنجليزية (كانت 90% من الإنجليزي المتبقي على `/ru/`). عنوان ووصف فهرسَي المطاعم والإقامة الصينيين من الكتلة الصينية المعتمدة (`餐厅与咖啡馆 — 哈萨`، `住宿 — 哈萨`) بدل النسخة الحرفية من الإنجليزية. **إصلاح جانبي:** محمّل Pagefind في `Search.astro` صار وعداً واحداً مشتركاً (كان كل حرف يُكتب قبل اكتمال التحميل يستدعي `options()` و`init()` من جديد). | `C22` أخضر: 131 صفحة معلَنة كلها ≤ 7% (الأعلى zh 7%، de 2%، ru 2%) وصفحتان غير معلَنتين بلا hreflang وخارج sitemap (337 رابطاً)؛ `npm run build` أخضر (26 فحصاً)؛ الاختباران المتصفحيان أخضران. |
| 6 | 2026-09-08 | `ShareButton.astro` في قوالب المعلم (بجانب زر الحفظ) والمقال والمنشأة والإقامة والفعالية والخريطة: Web Share API حيث تتوفر وإلا نسخ الرابط مع تأكيد مرئي، وحدث `share` بطريقته (web_share/copy) ونوع المحتوى ومعرّفه. النصوص: ar/en (`share.btn`)، zh من `plan.copy` المعتمد، de/ru أيقونة وحدها حتى تمرّ `share.*` بالخطوط. الخريطة: حالتها في الرابط تُقرأ عند الدخول (`?cat=` و`?q=` و`#slug` يفتح الدبوس) وتُكتب بـ`replaceState` عند كل تغيير، وحدث `map_deeplink`. | `tools/test-share-map.mjs`: 25/25 (مسارا المشاركة والحدث في القوالب الستة وبثلاث لغات؛ الخريطة تقرأ الرابط وتكتبه وتشارك حالتها)؛ الاختباران السابقان أخضران؛ `npm run build` أخضر (26 فحصاً). |
| 7 | 2026-09-08 | `geolocation=(self)` في `netlify.toml` و`public/_headers` (كان `()` فيعطّل الموقع كلياً). زر «موقعي» في الخريطة: علامة الزائر، المسافة على كل عنصر، وترتيب القائمة من الأقرب، مع رسالة عند الرفض وحدث `map_locate`. زر «كم يبعد عني» في بطاقة الزيارة بصفحة المعلم، بحدث `distance_check`. الحساب محلي (`src/lib/distance.ts`، مشترك مع بطاقات «القريب») ولا يُخزَّن الموقع. النصوص ar/en، وzh/de/ru أيقونة وحدها مع صياغة المسافة المعتمدة سلفاً لكل لغة. | `tools/test-near-me.mjs` بموقع محاكاة (وسط الهفوف): إذن ممنوح ومرفوض على الخريطة، والمسافة بصياغة الإنجليزية والعربية والروسية في صفحة المعلم، والرؤوس في `dist/_headers` و`netlify.toml`؛ في `build.yml`. |
| 8 | 2026-09-08 | **(أ) الجداول والمواعيد المبنيَنة وllms.txt.** `src/lib/tables.ts` + `DataTable.astro`: ثلاثة جداول تُبنى من مصادر الحقيقة نفسها — المواعيد والرسوم الموثّقة (21 معلماً) في «خطط لرحلتك» بالعربية والإنجليزية والصينية (رؤوسه من تسميات البنود المعتمدة)، وروزنامة الفعاليات العشر في صفحة الفعاليات بثلاث لغات، ومواسم الثمار (14 ثمرة) بالعربية والإنجليزية (الصينية بانتظار رؤوس معتمدة). حقلا `hoursSpec` و`fee` في مخطط المعالم لا يُقبلان إلا مع بند `practical` موثّق للمواعيد/الرسوم (يرفضه البناء وإلا)، ومُلئا لـ21 معلماً من بنودها الموثّقة حرفياً (8 بمواعيد مبنيَنة و20 برسوم)، وتُصدَّر `openingHoursSpecification` و`isAccessibleForFree`/`offers` في `TouristAttraction`. `llms.txt`: مرآة إنجليزية للأقسام، وقوائم الصفحات المعلَنة بالصينية والألمانية والروسية بعناوينها المعتمدة، ورابط التصحيح الإنجليزي. **(ب) فقرة الإجابة وصفحة اليونسكو.** حقلا `answer`/`answer_en` في مخطط المعالم (35–45 كلمة، معاً أو لا شيء — يرفضه البناء وإلا) كُتبا لـ15 معلماً من متن كل صفحة وبطاقتها حصراً، وتتصدّر الفقرة المتن وتصير وصف الصفحة (`DetailView`). صفحة «واحة الأحساء في اليونسكو» (`/اليونسكو/` و`/en/unesco/`، `UnescoView.astro`) مركّبة من المكوّنات الستة الحاملة لمعرّف `unesco` بعناوينها ونبذها وصورها القائمة، ورقم الموقع 1563 وسنة 2018 من `schema.ts`، وإحالة القائمة الكاملة إلى صفحة اليونسكو الرسمية بلا حقيقة جديدة؛ ItemList وBreadcrumb، وhreflang متبادل، وsitemap، ورابط في التذييل (عربي/إنجليزي — تسميته الإنجليزية رفعت `/zh/map/` إلى 22% في C22 فحُصر)، وشارة على صفحات المكوّنات الستة تقصدها، وسطر في `llms.txt`. zh/de/ru بلا فقرة إجابة ولا صفحة يونسكو حتى تمرّ بخطوط الترجمة. | فحص `C24` على dist: كل رقم في خلايا الجداول الثمانية (348 خلية) وارد في مصدره؛ 1131 كتلة JSON-LD كلها تُحلَّل؛ المواعيد/الرسوم المبنيَنة في 34 و80 صفحة معلم حصراً حيث يحملها المصدر وبقيمه؛ 30 فقرة إجابة مطابقة حرفياً لمصدرها وفي وصف الصفحة، بين 35 و45 كلمة، وكل رقم فيها وارد في بقية المتن أو البطاقة (لا في الفقرة نفسها — وC23 يستثنيها من عتبة الصفحة الرقيقة)؛ صفحة اليونسكو بلغتيها تحمل المكوّنات الستة بمعرّفاتها وروابطها وItemList بعددها وhreflang وsitemap. `npm run build` أخضر (27 فحصاً)؛ الاختبارات المتصفحية الأربعة خضراء (98 شرطاً)؛ axe-core 0 مخالفات على صفحات الجداول الأربع وصفحتي اليونسكو. |
| 9 | 2026-09-08 | **الربط الداخلي.** (1) `src/lib/related.ts`: فهرس عكسي يُبنى مرة في البناء من روابط المقالات إلى المعالم (`/معالم/<slug>/` و`/<lang>/attractions/<slug>/`)، وصفحة المعلم تعرض «مقالات تذكر هذا المعلم» بلغتها (24 صفحة، 68 رابطاً؛ zh/de/ru تلقائياً حين تُكتب مقالاتها). (2) حقل `district` في مخطط المعالم بمفاتيح أحياء المنشآت نفسها (`src/data/districts.ts` صار المصدر الواحد للمفاتيح والتسميات الثلاث بدل نسختين)، يُسند فقط حين يسمّي نصّ `area` القائم أو العنوان الحيَّ (يرفضه المخطط وإلا) — 20 معلماً في 7 أحياء — وصفحة المنشأة تعرض «معالم قريبة في الحيّ» (28 من 38 صفحة). (3) حدث `related_click` (kind = article/attraction، item_id) على الأقسام الثلاثة عبر `data-track`. (4) `tools/check-links.mjs` في `postbuild`: كل هدف داخلي في dist (href/src/srcset/poster/og، وsitemap وllms.txt) موجود، ورابط الصفحة بلا شرطة ختامية مكسور بقاعدة الموقع؛ `blogHref` في `routes.ts`. | فحص `C25` على dist: قسم المقالات في كل صفحة معلم يطابق حرفياً المقالات التي تربطها (وجوداً وعدماً)، وقسم معالم الحيّ يطابق معالم `district` كاملةً ولا قسم بلا حيّ؛ فاحص الروابط 23747 هدفاً في 369 صفحة و0 مكسور؛ `test-analytics-events.mjs` 20/20 (`related_click` بنوعيه)؛ `npm run build` أخضر (28 فحصاً)؛ الاختبارات الأربعة خضراء. |
| 10 | 2026-09-10 | **المدونة متعددة اللغات.** (1) البنية: `lang` بخمس قيم (ar/en/zh/de/ru) في مخطط المدونة، وصفحات `[slug]` تحت `/zh/blog/` و`/de/blog/` و`/ru/blog/`، و`postAlt` (الاقتران بـ`key`) مصدراً لـhreflang ومبدّل اللغة في اللغات الخمس، وsitemap مقترن بـ`key`، والفهرس الصيني يقدّم المقالات الصينية ثم ما لا نسخة له بالإنجليزية، و`blogHref` في `routes.ts`. **اكتشاف جانبي أُصلح:** محمّل glob يجعل حقل `slug` معرّفاً ضمنياً، فالمقال الواحد بلغاته تصادمت معرّفاته وطُمس مقالٌ بصمت — صار المعرّف من اسم الملف. (2) الترجمة: مقالا 24 و48 ساعة عبر الخطوط الثلاثة بمراحلها كاملة (مترجم ← محرر ← موائم ← مقارن وسياج وقائع ← مدقق ← قارئ أعمى لا يرى المصدر ← حاكم)، وكل مرحلة وكيل مستقل: الصينية 91/100 (92 و91؛ 81 ملاحظة للقارئ الأعمى: 19 مطبَّقة · 22 جزئياً · 26 مرفوضة · 14 علّة مصدر)، الألمانية 90/100 (92 و90، و93 لخمس سلاسل واجهة؛ 67 ملاحظة؛ 9 مدخلات معجم بأجناسها واستبدال «Uqair» بـ«Al-Uqair» المطابق لعنوان الصفحة)، الروسية أُرجعت في الدورة الأولى 86/100 (تسريبان من أزواج الذاكرة كشفهما القارئ الأعمى) ثم اعتُمدت في الثانية 93/100 (94 و93، و97 للواجهة؛ 70 ملاحظة جديدة). ذاكرات الترجمة +23/+34/+26 زوجاً، وسجلات الدرجات، ومفاتيح `blog.*` و`plan.printNote` بالألمانية والروسية. علل المصدر التي رفعها الحكّام الثلاثة (تناقض توقيت جبل القارة بين المقالين، غياب المسافات، سنة 1203هـ بلا ميلادي…) مجموعة في `*-translation/output/blog/judge.json` لقرار المالك. (3) `/zh/blog/` يبقى غير معلَن بقياس C22 (83% بمقالين صينيين من ثمانية). | `C26` على dist (تبادل hreflang تامّ: 347 صفحة و900 رابط نظير)؛ `C22` 137 صفحة معلَنة (zh ≤ 13%، de ≤ 4%، ru ≤ 3%)؛ `C25` 46 صفحة معلم بمقالات تذكرها (22 مقالاً)؛ `C4` و`C20` كما هي؛ فاحص الروابط 24046 هدفاً و0 مكسور؛ `npm run build` أخضر (29 فحصاً، 375 صفحة)؛ الاختبارات المتصفحية الأربعة خضراء (100 شرط)؛ axe-core 0 مخالفات على صفحات المقالات الست. |

اكتملت الخطوات العشر في 2026-09-10 على الفرع `claude/alahsa-site-audit-plan-u7kp5v`؛ فاحص التضارب صار 29 فحصاً (أُضيفت C22–C26) ومعه فاحص الروابط في `postbuild`، وأربعة اختبارات متصفحية وبوابة Lighthouse في `build.yml`. نسخة Word من هذا الملف مولَّدة من النص نفسه: الأقسام 1–4 (التقرير التنفيذي) أربع صفحات، وهذا السجل ملحق بعدها.

**ملحق ما بعد التشغيل في CI (2026-09-10):** البناء يعمل على عدّاء GitHub عند طلبات الدمج فقط، فشُغّل يدوياً على الفرع (التشغيلات 78–83). التشغيلات 78–81 أخفقت في حارس LCP المحاكى وحده: الرئيسيات 4.1–4.6 ثانية مقابل السقف 4.0 (الأداء 81–86)، وكل الميزانيات الحتمية والفحوص الـ29 والاختبارات الأربعة خضراء. فرضيتان أُوليان دُحضتا بالقياس: (1) إعادة تخطيط الصفحة مع وصول الخطوط — `content-visibility: auto` على الأقسام الست تحت الطيّة خفّض زمن التخطيط المقيس من 490 إلى 289 مللي ثانية (وأكبر تخطيط من 650 إلى 233 عقدة) لكنه لم يحرّك LCP على العدّاء (الإنجليزية 4289 → 4287)؛ (2) بطء معالج العدّاء — قياس `benchmarkIndex` بدالة Lighthouse نفسها أظهر العدّاء أسرع من المحلي (2412–2639 مقابل 1988)، ومعايرة مضاعف إبطاء المعالج بصيغة حاسبة Lighthouse الرسمية (`docs/throttling.md`) لم تحرّك LCP هناك أيضاً (4589 عند 7.8x مقابل 4588 عند 4x) — فالسبب شبكي لا معالجي. سطر تشخيص أُضيف إلى البوابة (عنصر LCP ومراحله، والأثر الفعلي غير المحاكى، وما بدأ قبل LCP المرصود فيدخل مخطط Lantern) كشف في التشغيل 81 السبب: بطاقة المعلم الأولى في الرئيسية كانت `eager` وهي على عمق 3392px على الجوال (412×823)، فتُطلب أثقل صورة في الصفحة (126 كيلوبايت) عند 16 مللي ثانية بأولوية Medium مع صورة الهيرو وتزاحمها، وتدخل حتى مخطط Lantern المتفائل. بجعلها كسولة كسائر البطاقات (التشغيل 82، أخضر): الرئيسية العربية LCP 4588 → 2719 مللي ثانية (الأداء 81 → 93)، الإنجليزية 4287 → 2714 (84 → 93)، الروسية 4061 → 2260 (86 → 97)، وصفحة جبل القارة 3160 (كانت تجتاز). أُضيفت ميزانية حتمية سابعة «لا صورة eager تحت الطيّة» (تفتح كل صفحة على 412×823 وتعدّ الصور غير الكسولة تحتها) كي لا يعود الخلل بصمت، وبقيت المعايرة والتشخيص في البوابة (`CPU_MULT` يثبّت المضاعف عند الحاجة). ملاحظة مقيسة للسجل: كروم 152 على العدّاء يجلب الصور الكسولة عند التحميل حتى عمق ≈2850px (6 صور غير LCP، 529 كيلوبايت) بينما كروم 141 محلياً حتى ≈2070px (3 صور، 277 كيلوبايت)، ونوع الشبكة في NetInfo ‏4g في الحالتين حتى تحت محاكاة Lighthouse — فرق إصدار لا فرق شبكة، وأثره على LCP المحاكى صغير لأن الكسول يدخل المخطط بعد مهمة التخطيط لا مع الهيرو؛ فرضية تثبيت نوع الشبكة جُرِّبت ودُحضت وأُزيلت. **الحالة النهائية:** التشغيل 83 على الإصدار النهائي للبوابة (كروم 152، مضاعف 7.8x) أخضر بكل الفحوص والاختبارات: الرئيسية العربية LCP 2714 مللي ثانية (الأداء 94)، الإنجليزية 2711 (94)، الروسية 2257 (97)، جبل القارة 3158 (92)، والميزانيات الحتمية السبع مجتازة على الصفحات الأربع.
