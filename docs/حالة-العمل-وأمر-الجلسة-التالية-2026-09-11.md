# حالة العمل وأمر الجلسة التالية — 2026-09-11

يخلُف `docs/حالة-العمل-وأمر-الجلسة-التالية-2026-09-10.md`. الفرع العامل: `claude/alahsa-site-audit-plan-u7kp5v`.

## 1. ما أُنجز في هذه الجلسة

| البند | الحالة |
|---|---|
| **طلبا الدمج #40 و#41** | **دُمجا كلاهما** (‏20:56 و21:15 بتوقيت 2026-09-10) |
| **البند 10أ — المدونة الصينية (6 مقالات)** | **مكتمل ومنشور**، درجة الدفعة **91/100** |
| **`/zh/blog/` في `unlisted.ts`** | **خرج منه بقياس** — المدونة الصينية 8/8 |
| البند 10ب — المعالم الألمانية (52 صفحة) | **لم يبدأ** — هو العمل التالي |
| البند 5 (المسافات) · البند 9 (خط الأساس) | **ينتظران المالك** كما كانا |

### 1-1 طلبا الدمج

انقلبت حالتُهما عمّا في ملف 2026-09-10 لأن المالك دمج طلب #39 (‏Snyk) بينهما: صار #40 `dirty` و#41 `behind`.
- **#40**: التعارض في `package-lock.json` وحده. حُلّ بأخذ قفل main كما هو (لا دمج يدويّ لملفٍ مولَّد) ثم إعادة `npm audit fix` فوقه. فتبيّن أن ترقية Snyk أغلقت سلفاً ثلاثاً من الأربع، وبقيت **svgo وحدها** (ثغرة عالية في `removeScripts`). فنزل الفرق من 303/390 سطراً إلى 58/103، وحُدِّث عنوان الطلب ومتنه لأن نصّهما صار يصف ما لم يعد فيه.
- **#41**: لا تعارض (الفرع لا يمسّ ملفات الحزم)، فدُمج main فيه.
- ثم **دمجهما المالك**، فأُعيد تأسيس الفرع على main (طلبٌ مدموجٌ لا يصلح لتتبّع عملٍ جديد).

### 1-2 دفعة المدونة الصينية 19

| المقال | الدرجة | | المقال | الدرجة |
|---|---|---|---|---|
| `crafts-tour` | 93 | | `hasawi-lomi` | 92 |
| `worth-visiting` | 93 | | `hasawi-lomi-history` | 92 |
| `jabal-qarah-guide` | 93 | | `souvenirs-guide` | 91 |

**درجة الدفعة 91** (أدنى الستّ). ذاكرة الترجمة **+51 زوجاً** (‏647)، والمعجم **+58 مدخلاً**، وسطر الدرجات مسجَّل.

**التحقق:** `npm run build` أخضر — **390 صفحة** (كانت 384)، و**30 فحصاً** بلا تضارب، وفاحص الروابط **24981 هدفاً و0 مكسور**. والاختبارات المتصفحية الأربعة: **96 شرطاً تحقق · 0 أخفق**.

**قياس C22 على `/zh/blog/`:** بعد النشر صارت المدونة الصينية 8/8، فنزل الفهرس إلى **14% بالقياس المباشر (‏2 كتلة من 14)** — والكتلتان هما الوردمارك `VISIT AL-AHSA`، **وهو مستثنًى صراحةً في قائمة أسماء C22** فقياسُ الحارس نفسِه **0%**. فحُذف من `unlisted.ts` ومرّ البناء بـ**150 صفحة معلَنة** وواحدةٍ غير معلَنة (فهرس المعالم الألماني وحده).

### 1-3 إصلاحٌ في محتوًى منشور

**نجمات التغميق كانت تظهر حرفياً للقارئ الصيني** في `/zh/blog/one-day-in-al-ahsa/`. العلّة قاعدةٌ في CommonMark: `**` الخاتمة لا تُغلق إذا سُبقت بعلامة ترقيم وتُبعت بحرف — والصينية بلا مسافةٍ بعد الترقيم (`？**那`)، والإنجليزية تنجو لأن مسافةً تليها. القياس على البناء الكامل: صفحةٌ واحدةٌ من 384 وموضعٌ واحدٌ فيها. أُصلح بنقل علامة الاستفهام خارج التغميق.

**وهذه العلّة صامتة**: لا يُفشلها بناءٌ ولا فحصٌ قائم، وقد كسرت **خمسة** مقاطع في الدفعة الفرعية A و**عشرين** في B ونجت من أربع مراحل (المرحلة 4 عدّت `**` فمرّ الملفّان). **مقترحٌ للمالك:** فحصٌ آليّ يعدّ `<strong>` في المُصيَّر لا `**` في المصدر.

## 2. قرارات خطٍّ تنتظر المالك — لا تُصلَح في دفعة

| # | البند | الحال المقيس |
|---|---|---|
| **POL-1** | `站点` مقابل `地点` لمحطات الرحلة | مدخل القاعدة 390 يقول `地点` ونصُّ حكم حاكم الدفعة 13 «Not 站点 — reads as bus stop / website». والمنشور **4:1 ضدّه**: `24-hours-zh:44` و`48-hours-zh:15,56` و`ui.ts:486` تقول `站点`، و`ui.ts:552` وحدها تقول `地点`. **رصده أربعة قرّاء عميان مستقلّين في أربعة مقالات.** الدفعة أبقت الحال ولم تزده، **ولم تُضِف إلى الذاكرة زوجاً يحمل `站点`** لئلا تُعيد إنتاجه. الخياران: توحيدٌ على `地点` مع `tm.json`، أو تضييق المدخل نصّاً. **وإبقاء الحال ليس خياراً.** |
| **POL-2** | `集市` مقابل `市集` | مرفوعٌ منذ الدفعة 13، ورصده ثلاثة قرّاء. الموقع 95 مقابل 45 في `src` |
| **POL-3** | قاعدة `page_reference` | المنشور فيه **عشر إحالاتٍ بصفر تشخيصٍ للصفحات**؛ الأنماط `详情见……页面` · `收录在……里` · `页面汇集了` · `涵盖`. **إعلامٌ لا استئذان** — تُكتب في `style_rules_additions` |
| **POL-4** | إملاء گلوس «Land of Civilis/zations» | المدخل 17 (‏`verify: true`) يكتب الإنجليزيَّ بالسين والگلوسَ الصينيَّ **بالزاي** عن VisitSaudi zh. وقياسي: **الصينية المنشورة منقسمة 1–1** (‏`jabal-al-qarah` ‏`value_zh` بالسين مقابل `PlanTripView:156` بالزاي). المطلوب: **يُكتب هذا الاختلاف نصّاً في `note` المدخل** وإلا أعادت كل دفعةٍ اقتراحَ القلب |
| **POL-5** | مكانة درجة القارئ الأعمى | **حكمُ هذه الدفعة**: الدرجة كاشفةُ عيوبٍ لا مقياسُ جودة، والفيصل **قابليةُ الدعوى للتحقّق لا عددُ القرّاء** — دعوى النحو أو الإسناد أو المصنِّف يكفي فيها قارئٌ واحد، ودعوى «لا يقولها الصينيّ» تُسجَّل ولا تُنفَّذ بشهادةِ واحد |
| **POL-6** | المدخل 100 (‏`jameed`) | الگلوس **مقيَّدٌ بالمدلول لا بالمصطلح**، وإلا أُعيد إقحامُه كلَّ دفعةٍ بحجّة «نصٌّ محميّ» |

## 3. علل المصدر — ستّ عشرة، مجمَّعةٌ في `zh-translation/output/batch-19/judge.json` تحت `source_defects_for_owner`

أبرزها (**الترجمة أمينةٌ لها كلِّها فلم تُخصَم**):
1. **`worth-visiting-en:42`** — «open-air stops for morning and **sunset**, and the souq for **evening**»: الغروب والمساء متداخلان فيُطلَب من الزائر أن يكون في مكانين.
2. **خرائط قوقل محجوبةٌ عن قارئ البرّ الصيني**، وخمسةُ نصوصٍ تَعِد بها. **تناقض قرارك 6ب** الذي أضاف 高德 لصفحات المعالم. مسألةُ منتَجٍ لا ترجمة.
3. **`crafts-en` و`souvenirs-en` لا يذكران السعودية ولا مرة** (و`worth-visiting-en` يذكرها أربعاً) — وأربعةُ قرّاءٍ عميان قالوا إن «哈萨» بلا بلدٍ تُقرأ «哈萨克斯坦». **والخطّ لا يملك إضافتها.**
4. **`souvenirs-en` يناقض نفسه**: نبذتُه تضع الفخار «in Hofuf's heritage souqs» ومتنُه يقول ثلاثَ مراتٍ إنه يُشترى «from the craft house itself at Jabal Al-Qarah».
5. **تعريف «الجميد» بوجهين** بين المقالين الإنجليزيين: «sun-dried lime» (ثمرةٌ مجفَّفة) مقابل «lomi juice sun-dried and aged in glass jars» (عصيرٌ محفوظ).
6. **المرجع 13** في مقال التاريخ يُسنِد عدد الأشجار إلى غرفة الأحساء و«الوطن» — وهما لا تَرِدان في المتن الذي ينسبه إلى واس.
7. **«ثلاثة أسماء» في المتن مقابل أربعةٍ في الخلاصة** (مقال التاريخ).
8. **الصلة المفقودة `Bin Zuhairi` ← `banzahir`**: العنوان يسأل عن الأول والمتن لا يتحدّث إلا عن الثاني بلا جملةٍ تربطهما. سمّاها القارئ «أكثر ما يحتاجه المقال».
9. **صفحة جبل القارة**: متنُها العربيّ يحمل ثلاث وقائع لا يحملها `body_en` (المغارات الاثنتا عشرة · اعتدال 20°م · 14 كم² وألف متر)، **ولأن zh/de/ru تُترجم عن الإنجليزية سقطت من اللغات الأربع** — والمقال الصيني الجديد يذكرها فلا يجدها من ينقر صفحةَ المعلم. العلاج: إضافتها إلى `body_en` فتتبعها اللغات.
10. **ثلاثة روابط `en.wikipedia.org`** في مقالٍ موجَّهٍ لقارئ البرّ — محجوبة عنه.

## 4. أمر بدء الجلسة التالية

> اقرأ `docs/حالة-العمل-وأمر-الجلسة-التالية-2026-09-11.md` كاملاً ثم `CLAUDE.md`. الفرع `claude/alahsa-site-audit-plan-u7kp5v`.
>
> **البند 10ب: المعالم بالألمانية (52 صفحة) على دفعات ~10 عبر `de-translation-pipeline`.** المراحل 1→5 بالتسلسل الصارم، كل مرحلةٍ وكيلاً مستقلاً، ثم القارئ الأعمى (6.5) — وكيلٌ جديدٌ لا يرى المصدر ولا يُخبَر أنها ترجمة، ونسخته من `tools/translation/blind-copy.py` — ثم الحاكم (6). العتبة 90.
>
> **وخذ من دفعة المدونة 19 هذه الدروس الأربعة**، فهي مقيسةٌ لا مقترحة:
> 1. **المرحلة 2 هي التي تُدخل الارتدادات** (أدخلتها في الدفعتين الفرعيتين معاً، وأشدُّها مفتتحٌ صار بلا مسندٍ إليه ومرّ على ثلاث مراحل). فألزِمها بأن تقرأ ناتج كل جملةٍ تشطرها **وحده بلا نظرٍ إلى المصدر**.
> 2. **صِّر المتن وعُدّ `<strong>` لا `**`** — علّةُ التغميق صامتةٌ وقد كسرت 25 مقطعاً في هذه الدفعة.
> 3. **امسح السلاسل المحظورة بحدود الكلمات لا بالبحث عن الكلمة مفردةً** — أمسك المسحُ في هذه الدفعة `哈萨人` داخل «哈萨|人家»، و`哈萨卡` داخل «哈萨|卡拉山»، وكلمتين وُلدتا من تحرير المرحلة 5 نفسِها.
> 4. **الحاكم يفرز ملاحظات القارئ الأعمى ولا يطيعها جملةً** — ردّ في هذه الدفعة أكثر من عشرين بنداً بعلل مكتوبة، وقارئان تناقضا نصّاً في اللفظة ذاتها التي أُصلحت لأحدهما.
>
> **وينتظر المالك وحده**: قرارات الخط الستّة في القسم 2، وعلل المصدر الستّ عشرة في القسم 3، والبند 5 (مسافات التنقّلات التسعة من خرائط قوقل — لا وصول للخرائط من بيئة التنفيذ)، والبند 9 (خط الأساس، تذكيرٌ مجدولٌ لـ2026-09-24).
>
> لا تختلق أي معلومة: لا رقم ولا موعد ولا مسافة بلا مصدر.

**المؤقّتات:** فحصُ طلبَي الدمج الدوريُّ **أُزيل** لتحقّق شرط توقّفه (دُمج الاثنان). وتذكيرُ خط الأساس (‏`trig_01Hoy1vkssyh5bsoDF4ngbzC`، ‏2026-09-24) **قائمٌ ومفعَّل** — لا تُنشئ له نظيراً.
