// يولّد out/{lg}/04-input.json: مخرج المرحلة 2+3 + مصدرُ كل موضع بالعربية والإنجليزية
// + البند العابر للغات (ظرف الأوعية) بجدوله المقيس + مطلب التحقق من عطب التخيير.
import { readFileSync, writeFileSync } from 'node:fs';
import { dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const R = process.argv[2];
const HERE = dirname(fileURLToPath(import.meta.url));
if (!R) { console.error('مرّر جذر الشجرة'); process.exit(1); }

const lines = (f) => readFileSync(`${R}/${f}`, 'utf8').split(/\r?\n/);
const AR = lines('src/content/blog/hasawi-lomi-ar.md');
const EN = lines('src/content/blog/hasawi-lomi-en.md');
const EV = lines('src/data/events.ts');
const FR = lines('src/data/fruits.ts');

// مصدر كل دور: [نصّ عربي, موضعه, نصّ إنجليزي, موضعه]
const SRC = {
  'hasawi-lomi-3': [AR[2], 'hasawi-lomi-ar.md:3', EN[2], 'hasawi-lomi-en.md:3'],
  'hasawi-lomi-18': [AR[17], 'hasawi-lomi-ar.md:18', EN[17], 'hasawi-lomi-en.md:18'],
  'hasawi-lomi-37': [AR[36], 'hasawi-lomi-ar.md:37', EN[36], 'hasawi-lomi-en.md:37'],
  'hasawi-lomi-39': [AR[38], 'hasawi-lomi-ar.md:39', EN[38], 'hasawi-lomi-en.md:39'],
  events: [EV[213], 'events.ts:214', EV[330], 'events.ts:331 (لا تسمّي الجميد إطلاقاً — فلا نظير)'],
  fruits: [FR[65], 'fruits.ts:66', FR[181], 'fruits.ts:182'],
};

const NEIGHBOUR_86 = {};
for (const lg of ['de', 'zh', 'ru']) {
  NEIGHBOUR_86[lg] = lines(`src/content/blog/hasawi-lomi-history-${lg}.md`)[85].trim();
}

const CROSS = {
  عنوانه: 'البند العابر للغات — ظرفُ «في أوعية زجاجية»: لم يبتّه أحد، ويُبتّ في هذه المرحلة صريحاً',
  لماذا_يصل_إليك:
    'اللغاتُ الثلاث طبّقت القاعدةَ نفسها («الجار يُلزم بالصياغة لا بالمضمون») فانتهت إلى مخرجين مختلفين. ' +
    'ولأنّ كلَّ لغةٍ مسنَدةٌ في ملفّها، يمرّ الخلافُ بلا بتٍّ إن لم يُعرض صريحاً. فاحكم فيه بالوقائع أدناه، ' +
    'لا بما فعلته لغتُك. وأنت ترى الآن ما لم يره وكيلُ المرحلة 3 في لغتك: مخرجَ اللغتين الأخريين.',
  الجدول_المقيس: [
    { الموضع: 'مقال المدونة، جواب FAQ (السطر 18)', مصدره_العربي: 'يقول «ويُعتَّق في أوعية زجاجية»', مصدره_الإنجليزي: 'يقول «aged the juice in glass jars»', de: 'أبقته', ru: 'أبقته', zh: 'أسقطته' },
    { الموضع: 'مقال المدونة، عنوان H2 (السطر 37)', مصدره_العربي: 'بلا ظرف (عنوان)', مصدره_الإنجليزي: 'بلا ظرف (عنوان)', de: 'لا ظرف', ru: 'لا ظرف', zh: 'لا ظرف' },
    { الموضع: 'مقال المدونة، المتن (السطر 39)', مصدره_العربي: 'يقول «المعتّق في أوعية زجاجية»', مصدره_الإنجليزي: 'يقول «aged in glass jars»', de: 'أبقته', ru: 'أبقته', zh: 'أسقطته' },
    { الموضع: 'بطاقة معرض اللومي (events.ts)', مصدره_العربي: 'يقول «المعتّق» مجرّداً بلا ظرف', مصدره_الإنجليزي: 'لا يسمّي الجميد إطلاقاً', de: 'أسقطته', ru: 'أسقطته', zh: 'أسقطته' },
    { الموضع: 'نبذة ثمار الواحة (fruits.ts)', مصدره_العربي: 'يقول «المعتّق» مجرّداً بلا ظرف', مصدره_الإنجليزي: 'يقول «sun-dried and aged» بلا ظرف', de: 'أسقطته', ru: 'أسقطته', zh: 'أسقطته' },
  ],
  حجة_de_وru:
    'خريطةٌ للمصادر: الظرفُ يُكتب حيث يحمله مصدرُ الموضع نفسِه (موضعا المدونة) ويُترك حيث يقول المصدرُ ' +
    '«المعتّق»/«aged» مجرّداً (البطاقتان). فإسقاطُه من البطاقتين امتناعٌ عن استيراده من الجار لا حذفُ واقعة.',
  حجة_zh:
    'حكمٌ واحدٌ في المواضع الستة: الصينيةُ المنشورةُ لم تحمل الظرفَ في أيٍّ منها، ومأموريّةُ الدفعةِ فارقٌ ' +
    'دلاليٌّ واحد (العصيرُ لا الثمرة)، فإدخالُه في 18 و39 توسيعٌ للشرح لا تصحيحٌ للمرجع — وانقسامٌ مقصودٌ ' +
    'يحتاج حارساً في المعجم ليصمد. والأوعيةُ باقيةٌ في الجار المنشور بمصدرها.',
  ما_هو_مطلوب_منك: [
    'احكم لموضعَي المدونة في لغتك (18 و39): هل يُكتب الظرفُ أم يُترك؟ بحجّةٍ من الوقائع لا من الاتّساق مع ما سبق.',
    'صِغ حكمك قاعدةً قابلةً للتطبيق على أي موضعٍ قادم — لا حكماً على هذه الخمسة وحدها. (مثالُ صياغةٍ ملزمة: «واقعةٌ يحملها مصدرُ الموضع تُكتب/لا تُكتب متى كان المنشورُ السابق لا يحملها».)',
    'سمِّ الثمنَ الذي يدفعه حكمُك: ما الذي يخسره القارئُ أو الاتّساقُ إن حكمتَ كما حكمتَ.',
    'إن رأيتَ أن الحكمَ يجب أن يكون واحداً في اللغات الثلاث، قُلْه صريحاً — فأنا (المنسّق) أملك فرضَه على الثلاث، أما أنت فلا تملك إلا لغتك.',
    'وانبِّه: البطاقتان (events.ts و fruits.ts) محكومتان سلفاً في الثلاث بالإسقاط ولا خلافَ فيهما — فلا تُعِد فتحَهما.',
  ],
  ملاحظة_على_موضع_18_بالذات:
    'جوابُ FAQ يُصدَّر في FAQPage JSON-LD فيظهر في نتائج قوقل المنسّقة مقتطفاً قائماً بذاته بلا صفحته. ' +
    'فتفاوتُ مضمونِه بين اللغات يظهر للقارئ الواحدِ إن قلب اللغة، ولا يظهر داخل صفحةٍ واحدة.',
};

const HUO = {
  عنوانه: 'مطلبُ تحقّقٍ بالعدّ: عطبُ التخيير (الجميدُ بديلٌ عن العصر والتخزين لا هو)',
  نصّه:
    'رصدت الصينيةُ أن 「或」 في `events.ts` كانت تجعل الجميدَ **بديلاً** عن العصرِ والتخزين، بينما العربيةُ ' +
    'تقول إن العصرَ والتخزينَ **هو** الجميد. ورجّحت zh وru أن لها نظيراً بنيوياً في `oder`/`или`؛ وde أصلحت ' +
    'بنيةَ `events.ts` فعلاً بلا أن تربطها بذلك. **تحقّق في لغتك بالعدّ لا بالظنّ** أن العطبَ زال من كل ' +
    'المواضع الخمسة/الستة المقترحة: لا أداةَ تخييرٍ تعطف الجميدَ على العصرِ والتخزين، ولا بنيةَ تعدادٍ تُدرجه ' +
    'صنفاً ثالثاً بجوار «طازجاً/عصيراً/مجففاً». واذكر الأمرَ الذي فحصتَ به والعددَ الذي خرج.',
};

for (const lg of ['de', 'zh', 'ru']) {
  const s3 = JSON.parse(readFileSync(`${HERE}/out/${lg}/03.json`, 'utf8'));
  for (const p of s3.positions) {
    const key = p.id.replace(`${lg}-`, '').replace(/^(events|fruits)-\d+$/, '$1');
    const [ar, arAt, en, enAt] = SRC[key];
    p.source_ar = ar;
    p.source_ar_at = arAt;
    p.source_en = en;
    p.source_en_at = enAt;
  }
  const out = {
    ...s3,
    stage: '04+05 input',
    neighbour_approved_line86: {
      file: `src/content/blog/hasawi-lomi-history-${lg}.md`,
      line: 86,
      text: NEIGHBOUR_86[lg],
      note: 'منشورٌ ومُسنَد (أخبار 24). لا يُمسّ — يُستورد منه الصوغ لا المضمون.',
    },
    cross_language_open_item: CROSS,
    disjunction_defect_check: HUO,
  };
  writeFileSync(`${HERE}/out/${lg}/04-input.json`, JSON.stringify(out, null, 2), 'utf8');
  console.log(`${lg}/04-input.json — ${s3.positions.length} موضعاً، ومصدرُ كلٍّ بلغتيه`);
}
