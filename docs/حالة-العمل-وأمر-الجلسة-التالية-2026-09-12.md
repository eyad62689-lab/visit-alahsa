# حالة العمل وأمر الجلسة التالية — 2026-09-12

## أين وقفنا

الفرع `claude/alahsa-site-audit-plan-u7kp5v` عند `a316c4c`، **مدفوعٌ وشجرةُ العمل نظيفة**، وطلبُ الدمج **#43 مفتوحٌ وأخضر** (‏`mergeable_state: clean`، خيطا المراجعة محلولان) **بانتظار دمج المالك**.

**الألمانية 23 معلماً من 58.** ولا مكوّنَ يونسكو ولا متحفَ بلا ألمانية.

| الدفعة | المحتوى | الدرجة | الحال |
|---|---|---|---|
| 1 | `qaisariyah` · `craftsmen-souq` · `baiah` · `duqat-algharash` · `lemon-farm` | 93 | **منشورة** |
| 2 | `ardh-alhadarat` · `hamidiyah-souq` · `asfar` · `khuzam` · `women-souq` | 91 | **منشورة** |
| 3 | عنقودُ المتاحف السبعة + مراجعةُ 5 حقولٍ في `hamidiyah-souq` | 91 | **منشورة** (الدورة 1، صفرُ تعديل حاكم) |
| **4** | **عنقودُ المواقع الدينية العشرة** | — | **المراحل 1–6 تمّت · تبقى 7** |

---

## الدفعة 4: أين تقف بالضبط

**العشرة**: `abubakr-mosque` · `bubakr-ribat` · `dibs` · `hubaish` · `jabri-mosque` · `jalaniyah` · `qubba-mosque` · `tahimiyah-e` · `umzurainiq` · `uqair-mosque` — **65 حقلاً** (خمسةٌ لكلٍّ، وبندُ بطاقةٍ في خمسٍ منها).

**المراحل 1–5 تمّت وكلُّها ملتزَمةٌ ومدفوعة.** المتغيّر عبرها: **14 ← 5 ← 2** حقلاً، والباقي بايتاً ببايت.

**المرحلة 6 (القارئ الأعمى) تمّت بعد التوقّف** — ‏`blind-reader.md` مكتوبٌ وملتزَم (‏99 ملاحظة). **فلا تُعاد**، والجلسةُ الجديدة تبدأ من **المرحلة 7 (الحاكم)** مباشرة.

### حصيلةُ القارئ الأعمى — وهي أثقلُ مما في الدفعات الثلاث قبلها
الدرجات **58–76 والمتوسط 66.6** (الدفعة 3 كانت 74.9). الأدنى `uqair-mosque` **58**.
التوزيع: **7 أخطاءٍ لغوية فقط من 99** · 48 مُلبِسة · 20 نقصَ محتوى · 23 ذوقاً · 1 إيجابية. **فالنصُّ لا يسقط في اللغة بل في المرجع والإسناد.**

**وثلاثٌ منها مقيسةٌ تحقّقتُ منها بنفسي:**
1. **‏10 من 10 متونٍ تفتتح بالبنية نفسها** — ظرفُ مكانٍ ثم `steht`، بلا استثناءٍ واحد. فالقارئُ في القراءة السريعة يرى «النصَّ نفسه عشر مرات»، ولا يميّز إلا أربعاً. **والمادةُ الخاصة بكلِّ موضعٍ موجودة** (محرابان · الدبس في الزقاق · المبيتُ المجاني · قريةُ الأربعين مسجداً) لكنها في الموضع نفسه من القالب نفسه فيلغي بعضُها بعضاً. **على الحاكم أن يقيس أولاً أهي موروثةٌ من المصدر أم صنعتها الألمانية.**
2. **سطرُ الإسناد `(Bestätigung durch das Redaktionsteam von Visit Al-Ahsa)` معروضٌ للقارئ** في الصفحات الخمس ذوات البطاقة (يحرسه C12) — **والموقعُ فيه يستشهد بنفسه**. وهي عينُ ما رفعه قارئُ الدفعة 3 على `hamidiyah-souq`: **علّةُ مصدرٍ متكرّرة** لا عيبُ ترجمة.
3. **`Ribat` أسوأ من غير المشروح** بحكم القارئ: شرحُه بـ«مؤسسةٍ وقفية» يرسل القارئةَ إلى كيانٍ قانونيّ ثم يصف لها مبنىً بطابقين وفناءً وبئراً ومطابخ. **وهذا نقضٌ لتصديق المراحل 2 و3 و4 الثلاثة** — وهو بعينه ما وُضع له القارئ الأعمى.

**وأربعُ صفحاتٍ تناقض نفسها أو بعضها في مواضع قابلة للتحقّق** (بطاقةُ الزيارة تقول استمرارَ الصلاة والمتنُ يقول غيره · القبّة تتناقض بين نبذتها ومتنها · مسجدُ القرن التاسع عشر «من أقدم» وفي الدفعة نفسها مسجدٌ من الحادي عشر)، و**عشرُ صفحاتٍ عن دور عبادةٍ بلا كلمةٍ عن دخول غير المسلم ولا اللباس ولا التصوير** — وأكثرُ ذلك علَلُ مصدرٍ يبتّها الحاكم ولا يُخصَم عليها.

**ثم التطبيق** بـ:
```
node tools/translation/apply-attraction-batch.mjs de batch-de-4 judge.json --dry
```

### ما يجب أن يعرفه حاكمُ الدفعة 4
1. **`Arabischen Golf` عبرت المراحل 1 و2 و3** — خرقٌ صريحٌ لمدخل المعجم ولقاعدة الدور («الخليج = `die Golfregion`/`der Golf` فقط»، لأن التسمية متنازَعة). أمسكتها المرحلة 4 فصارت `Golfküste`. **يُسجَّل درساً إجرائياً.**
2. **سياجُ الوقائع حذف دعوى واحدة**: تأريخُ إنجاز ترميم `abubakr-mosque` بـ1441هـ — والفيصلُ العربيّ يُسنده إلى **المرحلة الأولى**. **والمرحلة 4 أفصحت عن معارض**: الصينيُّ المعتمد يقرؤها بالوجه الآخر — **القرارُ للحاكم**.
3. **`Chandal-Holz`**: الهويةُ متنازَعة (مانغروف من زنجبار/تنزانيا · مستوردٌ من الهند)، و`Dalbergia sissoo` **سقطت بالقياس**. لا گلوس، ولا يُخترَع `Mangrovenholz`.
4. **`Ribat`**: المعنى الألمانيّ المتداول **حصنُ ثغر** لا وقفاً علمياً. السياجُ ثلاثيّ (الدلالةُ قبل أيّ وصفٍ مادّيّ في الملخّص والمتن · صفرُ لفظِ تحصين على الصفحة · لا گلوس في العنوان)، وصدّقته المراحل 2 و3 و4 كلٌّ بقياسه. **والحكمُ النهائيّ للقارئ الأعمى.**
5. **بندُ بطاقةٍ واحدٌ يتكرّر خمساً حرفياً** — تحقّق أنه بقي صيغةً واحدة. و`kein Eintrittsgeld` لا `kein Eintritt` (الثانية تُقرأ «ممنوع الدخول»).

---

## قرارات المالك المعلّقة — ولا تتقدّم الدفعة 5 بدونها

### 1. قرارك 8: «بانتظار التأكيد» — منشورةٌ في خمس لغات
أمرتَ بحذفها نهائياً. **وقياسي: 11 موضعاً في 6 ملفات ما زالت تحملها.**

و**أربعةُ قرّاء عميان مستقلّين رفعوها** بلا علمٍ بالقرار ولا بعضهم ببعض؛ رابعُهم أضاف أن **حاشية المصدر تُحيل إلى فريق تحرير الموقع نفسه** — أي أن الدليل على الدعوى هو ناشرُها.

**وأثرُها صار إجرائياً**: `tahimiyah-s` و`taraf-zoo` تحملانها في `summary_en` و`body_en` — **وهما مصدرُ الترجمة الألمانية** — فأُخرجتا من الدفعة 4 لئلا تُنشأ **نسخةٌ ألمانيةٌ سادسة** من نصٍّ مأمورٍ بحذفه. (‏`delfoon-lake` و`hubail-lake` و`kanzan-park` تحملها في المتن العربيّ وحده، والألمانيةُ لا تقرؤه، فتُترجَم بلا إشكال.)

**قل الكلمة وتُحذف في المواضع كلها.**

### 2. وبقيتها
- **POL-DE-18** (`Frauen-Souk` تُقرأ «سوقاً للنساء») و**POL-DE-15** (`Land of Civilisations`) — **الحسمُ في المصدر يصلح أربع لغاتٍ معاً**.
- **إحدى وخمسون علّةَ مصدر** تحت `source_defects_for_owner` في `judge*.json` للدفعات 1–3.
- **البند 5**: أزمنة التنقّلات التسعة — `maps.googleapis.com` يردّ `REQUEST_DENIED` بلا مفتاح.
- **البند 9**: خط الأساس (تذكيرٌ مجدولٌ 2026-09-24).

---

## الباقي من البند 10ب: 25 معلماً

| العنقود | العدد | الحقول | ملاحظة |
|---|---|---|---|
| منتزهات | 8 | 54 | `jawatha-park` وحدها 19 حقلاً |
| طبيعية | 10 | 98 | `uqair-beach` 17 · البحيرتان 14 لكلٍّ |
| مزارع | 4 | 20 | **`taraf-zoo` مؤجَّلة** (قرار 8) |
| تاريخية | 2 | 10 | `muhairis` · `uqair-castle` |
| دينية | 1 | 5 | **`tahimiyah-s` مؤجَّلة** (قرار 8) |

**والعناقيدُ أثبتت نفسها**: الدفعة 3 أغلقت المتاحف كلَّها بلا لفّةِ تصحيح.

---

## مهامٌّ مفتوحة غير الدفعات
- **`det.mentions.h` للروسية والصينية** (المهمة 15): عيبٌ حيٌّ في الإنتاج — ترويسةٌ إنجليزية تعلو قائمةَ عناوينَ مترجَمة في **9 صفحات روسية و12 صينية**. الألمانيةُ أُصلحت في الدفعة 3. ولكلِّ لغةٍ خطُّها، والكتابةُ في `src/i18n/ui.ts` تُسلسَل.

---

## دروسٌ إجرائية تُنقل إلى الجلسة الجديدة
1. **لا `git add -A` ووكيلٌ يكتب في الخلفية** — مسارات صريحة. (‏التزامٌ في الدفعة 3 التقط `fields.stage3.json` وهو ما زال نسخةً من المرحلة 2.)
2. **لا يُلتزَم ملفُ مرحلةٍ ما زال نسخةً حرفيةً من سابقه** تحت اسم المرحلة — يُسمّى بما هو.
3. **التعديلُ والدفعُ القسريّ يُلغيان فحصَ CI الجاري** — المقايضةُ خاسرة، فالتزاماتٌ عادية وإن تعدّدت.
4. **ابحث في الألمانية بالصيغ المصرَّفة لا بصيغة المعجم** — بحثتُ عن `bauliches Element` فأخفقتُ على `baulichen Elemente`، وكِدتُ أحسب تقريراً صحيحاً خطأً.
5. **وكيلٌ واحدٌ لمرحلتين يكتب ملفَّي JSON كاملين ينهار** بسقف الإخراج 64000 رمز. الحلّ: `cp` ثم تحريرُ المتغيّر وحده — **وفائدتُه أن «بلا تغيير» تصير مُثبَتةً بالفرق لا مُدّعاة في تقرير**.
6. **فاحصُ المرحلة 5 كذب أربع مرات** عبر الدفعات — تحقّق منه قبل أن تصدّقه.

---

## أمرُ الجلسة الجديدة — انسخه كما هو

> أكمل من حيث توقّفنا في مستودع `visit-alahsa`، الفرع `claude/alahsa-site-audit-plan-u7kp5v`.
>
> اقرأ أولاً `docs/حالة-العمل-وأمر-الجلسة-التالية-2026-09-12.md`، ثم `de-translation/output/batch-de-4/brief.md`.
>
> **الدفعة 4 (عنقود المواقع الدينية العشرة) أتمّت المراحل 1–6.** ابدأ من **المرحلة 7 (الحاكم)**: العتبة 90، ودرجةُ الدفعة أدنى صفحاتها، ويبتّ الملاحظات التسع والتسعين كلَّها. ثم التطبيق والبناء والاختبارات المتصفحية الأربعة، ثم حدّث وصفَ الطلب #43.
>
> **ونبّه الحاكمَ**: القارئ أعطى 58–76 (متوسط 66.6) وهو أثقلُ من الدفعات الثلاث، لكن **7 ملاحظاتٍ فقط من 99 تمسّ صحة الألمانية** — فالقاعدة المقرَّرة أن درجةَ القارئ كاشفةُ عيوبٍ لا مقياسُ جودةِ ترجمة، وما كان علّةَ مصدرٍ يُدرَج ولا يُخصَم عليه.
>
> وراقب الطلب #43: أخضرُ الآن وينتظر دمج المالك.
