# حالة العمل وأمر الجلسة التالية — 2026-09-12 (ب)

> يَخلُف `docs/حالة-العمل-وأمر-الجلسة-التالية-2026-09-12.md`، وقد صارت مقدّمتُه ساقطةً: الطلبُ #43 **دُمج** والدفعة 4 **نُشرت**.

## أين وقفنا

الفرع `claude/alahsa-site-audit-plan-u7kp5v` **مدفوعٌ وشجرتُه نظيفة**، و**طلبُ الدمج #44 مفتوح** (الدفعة 4) بانتظار الفحوص ودمج المالك. والطلبُ **#43 دُمج** في 2026-09-12 الساعة 05:31 UTC بالدفعات 1 و2 و3.

**الألمانية 33 معلماً من 58** (مقيسةً بـ`grep -l 'title_de:' src/content/attractions/*.md`). ولا مكوّنَ يونسكو ولا متحفَ ولا موقعَ دينيٍّ بلا ألمانية، إلا المؤجَّلتين بقرار المالك 8.

| الدفعة | المحتوى | الدرجة | الحال |
|---|---|---|---|
| 1 | `qaisariyah` · `craftsmen-souq` · `baiah` · `duqat-algharash` · `lemon-farm` | 93 | **مدموجة** (#43) |
| 2 | `ardh-alhadarat` · `hamidiyah-souq` · `asfar` · `khuzam` · `women-souq` | 91 | **مدموجة** (#43) |
| 3 | عنقودُ المتاحف السبعة + مراجعةُ `hamidiyah-souq` | 91 | **مدموجة** (#43) |
| **4** | **عنقودُ المواقع الدينية العشرة** | **92** | **منشورةٌ في #44** |

---

## الدفعة 4: ما جرى بالضبط

العشرة: `abubakr-mosque` · `bubakr-ribat` · `dibs` · `hubaish` · `jabri-mosque` · `jalaniyah` · `qubba-mosque` · `tahimiyah-e` · `umzurainiq` · `uqair-mosque` — **65 حقلاً**.

**دورتان**: ردٌّ عند 87 إلى المرحلة 2، ثم اعتمادٌ عند **92** (‏`qubba-mosque` و`uqair-mosque` متعادلتان على الأدنى؛ والعشرةُ 92–94).

والمتغيّر عبر لفّة التصحيح: **المرحلة 2 ثمانيةَ عشرَ حقلاً · المرحلة 3 حقلان (داخلَها) · المرحلة 4 صفر · المرحلة 5 خمسةٌ (داخلَها) · الحاكمُ حقلان**. فالإجماليُّ 18 من 65، والسبعةُ والأربعون بايتاً ببايت — **مُثبَتاً بالفرق لا مُدّعى في تقرير**.

### الأربعُ التي أوجبت الردّ — وكلُّها سُدَّت

1. **`Weiterhin` في بند البطاقة ×5**: دعوى استمرارٍ لا يقولها `A working mosque` ولا «مسجد عامل»، وصفرُ ورودٍ لها في المنشور، **وتناقضٌ نصّيٌّ مع متنَي `hubaish`** («wieder» بعد الترميم) **و`umzurainiq`** («Eine Zeit lang wurde dort nicht mehr gebetet»). صارت `Für das Gebet genutzt – zu den Gebetszeiten geöffnet, kein Eintrittsgeld`، متطابقةً في الخمس بحقولها الثلاثة وبزوجِ ذاكرةٍ واحد.
2. **ثلاثُ نبذاتٍ دون النظيرين المعتمدين** (`dibs` · `hubaish` · `bubakr-ribat`) — القاعدةُ المستخلَصة: **متى أسقطت الألمانيةُ وحدها معلومةً تحملها الصفحةُ نفسُها ويحملها zh وru، فالإسقاطُ عيبٌ لا أمانة.**
3. **أربعةُ گلوساتٍ ملزمةٍ مفقودة** (`Qasr Ibrahim` ×3 · `Bayt Al-Bay'ah`) — 4/4 الآن.
4. **الأصالة** في `qubba` و`bubakr-ribat` و`uqair`.

### وثلاثةُ أحكامٍ قِيس فيها المصدرُ قبل الخصم على الترجمة

- **الرتابةُ البنيوية موروثة**: 10/10 إنجليزية **و10/10 عربية** تفتتح بظرفٍ ثم `stands`/«يقوم» ⇒ علّةُ مصدر، و«إعادةُ بناء العشر» تُعيد كتابةَ المصدر لا ترجمتَه. والتصديرُ الألمانيُّ الصنع **ثلاثةٌ على صفحتين** (لا أربعةٌ على ثلاث كما قِيس أولاً).
- **`Ribat` مقبولٌ اقتراناً مرفوضٌ استبدالاً**: `Studienherberge` تمحو «وقف علمي» وتخرق مدخل `waqf ⇒ die Stiftung`؛ ولفظُ الإيواء داخلُ المصدر، وzh وru **تقرنان** ولا تستبدلان.
- **دعاوى القارئ الثاني الأربعُ الكبرى أمانةٌ للمصدر**: «خمسُ تسمياتٍ للبرنامج» هي ثلاثُ تسمياتٍ لثلاثة برامجَ يفرّقها المصدر · «1430هـ بدقّتين» دقّتا المصدر · «`um 850` بـ`1446` مضبوطاً» صيغةُ المصدر (وإصلاحُها مستحيلٌ بلا خرقٍ: حذفُ «um» زيادةُ يقين، وتقريبُ 1446 تغييرُ رقم) · وتناقضُ نبذةِ `qubba` ومتنِها يحمله المصدرُ عينَه.

### القارئان الأعميان: 234 ملاحظةً مبتوتةً كلُّها

| الدورة | ملاحظات | مطبَّقة | جزئياً | مرفوضة | علّةُ مصدر | خارج النطاق | المتوسط |
|---|---|---|---|---|---|---|---|
| 1 | 99 | 13 | 14 | 26 | 45 | 1 | 66.6 |
| 2 (قارئٌ جديد) | 135 | 3 | 13 | 54 | 64 | 1 | 66.8 |

**المتوسطُ ثابتٌ بين قارئين مستقلّين، والتوزيعُ ينتقل من اللغة إلى المادّة**: «نقصٌ أو سؤالٌ مفتوح» 20 ⇒ 49. أي أن الصياغةَ تحسّنت والنقصَ المملوكَ للمالك ظهر عارياً. **فدرجةُ القارئ كاشفةُ عيوبٍ لا مقياسُ جودةِ ترجمة** (POL-DE-21) ولم تدخل الحساب في الدورتين.

---

## ستُّ كذباتِ فاحصٍ وخطأُ حاكمٍ — تُنقل درساً

1. **الخامسةُ بالإسقاط وهي أهمُّها**: فاحصُ المرحلة 5 أعلن «Glossierung: PASS» و«Termbase 100 %» وأربعةُ گلوساتٍ ملزمةٍ مفقودة. **والعلّةُ بنيويةٌ لا تساهل**: الفحصُ من النصّ إلى المعجم، والأربعةُ **مداخلُ مقترنةٌ بلا قوسٍ** في حقل `de` فلا يراها مسحٌ قوسيّ ⇒ نُفِّذت **POL-DE-28** بالاتجاه المعاكس على 199 مدخلاً: 13 وجوباً و**13/13** التزاماً (كانت 9/13).
2. **والسادسةُ بالنقص**: «الفاصلة قبل `und`» عُدّت 4 وهي **5** (لـ`abubakr` موضعان لا واحد). والخمسةُ صحيحةٌ بـ§72 E1.
3. **وحاكمُ الدورة 1 أخطأ مرّة**: `freistehend` «مصطلحٌ قائم» وهو **صفرٌ** في المعجم (145) وفي إضافات الدفعة الثلاث وفي `src/` كلِّه ⇒ سقط الخصمُ على الامتثال المعجميّ، والكلمةُ صحيحةٌ فأُضيفت مدخلاً بسند دودن والاستعمال.
4. **وفاحصُ المنسّق كذب أيضاً** في أول تشغيل: أسقط الدفعةَ على ترتيبِ مفاتيحَ لا على عيب. **فالقاعدةُ: تحقّق من فاحصك قبل أن تصدّقه، واختبر كلَّ تعبيرٍ نمطيٍّ على ضابطٍ موجبٍ وسالب.**
5. **وخرقٌ لم يره أحدٌ في ست مراحلَ ولا في حاكم الدورة 1**: `Östliche Moschee` اسمُ علمٍ مستعملٌ في `abubakr.body_de` وصفرٌ في المعجم ⇒ أُضيف (POL-DE-37).

---

## التحقّق — وخطُّ الأساس أُثبت قبل التطبيق

| | قبل | بعد |
|---|---|---|
| الصفحات | 407 | **417** |
| `astro check` | 0 خطأ · 0 تحذير | **0 · 0** |
| فاحصُ التضارب | 30 بلا تضارب | **30 بلا تضارب** |
| الأهدافُ الداخلية | 26107 · 0 مكسور | **26781 · 0 مكسور** |
| `C17`/`C18` | 27 صفحة ألمانية | **37** |
| `C22` أعلى ألمانية | 4% | **4%** |
| `C26` روابطُ النظائر | 1092 | **1172** متبادلة |
| الاختباراتُ المتصفحية الأربعة | 96 شرطاً · 0 إخفاق | **100 شرطاً · 0 إخفاق** |

المعجم **145 ⇒ 200 مدخلاً** كلٌّ بجنسه ومصدره، والذاكرة **174 ⇒ 200 زوجاً** وصفرُ زوجٍ محذوف.

> **تصحيحٌ بقياسٍ لاحق:** خانةُ «بعد» في السطر الأخير كانت **96 منقولةً عن خانة «قبل»** لا مقيسةً — وسجلُّ الالتزامات يكشفها: النشرُ ‏09:44:42 والوثيقةُ ‏09:49:39 وبينهما بناءٌ كامل، فلا متّسعَ لأربعة اختباراتٍ متصفحية. **والمقيسُ فعلاً على `dc82bf9`: 100 شرطاً · 0 إخفاق** — ‏`test-analytics-events` **20** · `test-lang-suggest` **35** · `test-share-map` **25** · `test-near-me` **20**.
>
> **والعددُ متغيّرٌ بالمحتوى لا ثابت**: الأدواتُ الأربعُ تطبع ما **نجح فعلاً** (‏`seen.length`/`passed`) وفيها شروطٌ تدور على الصفحات واللغات، فبعضُها لا يُعدّ حين لا يُبنى موضوعُه (‏«مفتوح الآن» تُتخطّى في بناءٍ بلا أوقاتٍ حيّة). **فالمقارنةُ من الآن بالتفصيل لكلِّ أداةٍ لا بالمجموع.**

**وبيئةُ الاختبار مهيَّأة**: ‏`puppeteer-core` و`axe-core` في `/tmp/pptr`، والمتصفّح `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`، والتشغيل:
```bash
PUPPETEER_DIR=/tmp/pptr CHROME=/opt/pw-browsers/chromium-1194/chrome-linux/chrome \
  BASE=http://127.0.0.1:4399 node tools/<الاختبار>.mjs     # ومعاينةٌ على 4399
```
(‏`/tmp` غيرُ باقٍ بين الجلسات، فيُعاد التثبيت: `npm i puppeteer-core axe-core`.)

---

## قرارُ المالك 2026-09-12: **«مسموح دخول غير المسلم»** — مبتوتٌ، والتنفيذُ عملُ الجلسة القادمة

هذا نصُّ كلمة المالك، وهو **تأكيدُ المصدر** الذي كان الناقصَ الوحيد. ولا يحتاج صياغةً جديدة ولا خطَّ ترجمة: **الصيغةُ معتمدةٌ سلفاً بخمس لغات** في `src/content/attractions/jawatha-mosque.md` (قرارُ المالك 4ج، ‏2026-09-10)، ومدخلُ المعجم الألماني **62** يأمر بإعادتها **حرفياً** متى حملت مساجدُ أخرى البندَ نفسه ويسمّي `Hubaish` و`Al-Uqair-Hafenmoschee` بعينهما.

### ما أذن به القرار — وما لم يأذن

| | |
|---|---|
| **أذن** | واقعةُ **إباحة الدخول** لغير المسلم على الصفحات الدينية العشر التي عُرضت عليه. |
| **لم يأذن — ولا مصدرَ لها فلا تُكتب** | التصويرُ · دخولُ بيت الصلاة · غطاءُ الرأس · «الكتفان والركبتان». وحاشيةُ مدخل المعجم 62 **تسجّلها هي نفسُها ثغرةَ مصدر** (`als Quellenlücke gemeldet`)، فكتابةُ أيٍّ منها اختلاقٌ لا استنباط. |

**وشرطُ اللباس المحتشم جزءٌ من الصيغة المعتمدة نفسها** (‏«متاحة، مع المحافظة على اللباس المحتشم») لا زيادةٌ عليها — فإعادتُها حرفياً تنقل الإذنَ وشرطَه معاً كما أقرّهما المالك في 4ج. **وإن أراد المالكُ الإذنَ بلا الشرط على صفحةٍ ما فليقلها**، وإلا فالحرفيةُ هي الأمان.

### الصيغةُ التي تُنسخ حرفياً (من `jawatha-mosque.md`)

```
label: "زيارة غير المسلمين"            value: "متاحة، مع المحافظة على اللباس المحتشم"
label_en: "Visitors of other faiths"   value_en: "Welcome, with modest dress"
label_de: "Nichtmuslimische Besucher"  value_de: "Willkommen – bedeckende Kleidung vorausgesetzt"
label_ru: "Вход для немусульман"       value_ru: "Открыт при условии скромной одежды"
label_zh: "非穆斯林游客"                value_zh: "欢迎参观，请注意衣着端庄"
source: "تأكيد فريق «زوروا الأحساء»"   source_en: "Confirmed by the Visit Al-Ahsa team"
source_de: "Bestätigung durch das Redaktionsteam von Visit Al-Ahsa"
source_ru: "Подтверждено редакцией этого сайта"   source_zh: "本站编辑团队确认"
verified: true                          verifiedAt: "2026-09-12"
```

`verifiedAt` **تاريخُ هذا القرار** لا تاريخُ جواثا. والمخطط (`src/content.config.ts:123`) يرفض `verified: true` بلا `source` و`source_en` و`verifiedAt` — فلا بندَ بلا إسناد.

### والنطاقُ مقيسٌ، وهو صنفان لا صنفٌ واحد

| الحال | الصفحات | ما يلزم |
|---|---|---|
| بطاقةٌ قائمةٌ ببندٍ واحد | `abubakr-mosque` · `dibs` · `hubaish` · `jabri-mosque` · `umzurainiq` | **بندٌ ثانٍ** يُضاف |
| **بلا حقل `practical` أصلاً** | `bubakr-ribat` · `jalaniyah` · `qubba-mosque` · `tahimiyah-e` · `uqair-mosque` | **حقلٌ يُنشأ** ببنده الأول |

فالخمسُ الأخيرةُ لا بطاقةَ زيارةٍ لها البتّة — **والبندُ فيها يفتح البطاقة لا يزيدها سطراً**، وهذا أعظمُ أثرِ القرار.

**وثلاثُ حواشٍ تُرفع للمالك عند التنفيذ:**

1. **`bubakr-ribat` ليس مسجداً** بل رباطٌ (وقفٌ علميّ ومبيت). القرارُ عُرض ضمن العشر فيشملُه، واللصيقةُ «زيارة غير المسلمين» تصلح للزائر لا للمصلّي — **لكن يُنبَّه المالكُ إليه صراحةً** ليعترض إن شاء.
2. **`tahimiyah-s` (مسجد التهيمية الغربي) هو الحادي عشر** ولم يكن في العشر المعروضة (مؤجَّلٌ بقرار 8، وتأجيلُه يخصّ **الترجمة الألمانية** لا هذا البند). الواقعةُ نفسُها تنطبق عليه، **فيُسأل المالكُ قبل إضافته** لا يُقاس عليه صمتاً.
3. **`jawatha-mosque` يبقى كما هو** — بنده قائمٌ بـ`verifiedAt: 2026-09-10`، ولا يُعاد تأريخُه.

### والتنفيذُ يمسّ خمسَ لغاتٍ في خطوةٍ واحدة بلا خطِّ ترجمة

لأن القيم الخمس **معتمدةٌ سلفاً**، فلا حاجة إلى `zh`/`de`/`ru`-pipeline: هذه **إعادةُ استعمالٍ حرفية** لزوجٍ مُقرٍّ لا ترجمةٌ جديدة (وهو نصُّ مدخل المعجم 62). ويلزم بعدها `npm run build` كاملاً — **C24** يحرس بنود البطاقة، و**C12** يحرس ظهور سطر الإسناد، و**C22** تتأثر لأن البند يضيف نصّاً مترجَماً لا إنجليزياً.

---

## قرارات المالك المعلّقة

### 1. قرارك 8: «بانتظار التأكيد» — منشورةٌ في خمس لغات

‏11 موضعاً في 6 ملفات ما زالت تحملها، وأربعةُ قرّاء عميان مستقلّين رفعوها بلا علمٍ بالقرار ولا بعضهم ببعض؛ ورابعُهم أضاف أن **حاشيةَ المصدر تُحيل إلى فريق تحرير الموقع نفسه**.

**وأثرُها إجرائيّ**: `tahimiyah-s` و`taraf-zoo` **مؤجَّلتان** — مصدرُهما الإنجليزيّ يحملها في `summary_en` و`body_en` معاً، فترجمتُهما تُنشئ **نسخةً ألمانيةً سادسة** من نصٍّ أُمر بحذفه، وفي `summary` الذي هو وصفُ الصفحة وبطاقةُ مشاركتها. (‏`delfoon-lake` و`hubail-lake` و`kanzan-park` تحملها في المتن العربيّ وحده، والألمانيةُ لا تقرؤه، فتُترجَم بلا إشكال.)

### 2. وبقيتها

- **تأريخُ 1441هـ في `abubakr`**: سياجُ الوقائع قرأها «المرحلة الأولى» بثلاثة أسانيد، **و`body_zh` المعتمد يقرؤها بالوجه الآخر** ⇒ علّةُ اتّساقٍ **تُحسم في الخط الصينيّ** لا في الألمانيّ (POL-DE-33).
- **POL-DE-18** (`Frauen-Souk`) و**POL-DE-15** (`Land of Civilisations`) — الحسمُ في المصدر يصلح أربع لغاتٍ معاً.
- **ستٌّ وخمسون علّةَ مصدر** تحت `source_defects_for_owner`، و**إحدى وأربعون سياسة** تحت `policy_for_owner`.
- **البند 5**: أزمنة التنقّلات التسعة — `maps.googleapis.com` يردّ `REQUEST_DENIED` بلا مفتاح.
- **البند 9**: خط الأساس (تذكيرٌ مجدولٌ 2026-09-24).

---

## مهامٌّ مفتوحة غير الدفعات

**المهمة 15 — `det.mentions.h` للروسية والصينية**: عيبٌ حيٌّ في الإنتاج — ترويسةٌ إنجليزية تعلو قائمةَ عناوينَ مترجَمة في **9 صفحات روسية و12 صينية**. الألمانيةُ أُصلحت في الدفعة 3. ولكلِّ لغةٍ خطُّها، والكتابةُ في `src/i18n/ui.ts` تُسلسَل.

---

## الباقي من البند 10ب: 25 معلماً

| العنقود | العدد | الحقول | ملاحظة |
|---|---|---|---|
| منتزهات | 8 | 54 | `jawatha-park` وحدها 19 حقلاً |
| طبيعية | 10 | 98 | `uqair-beach` 17 · البحيرتان 14 لكلٍّ |
| مزارع | 4 | 20 | **`taraf-zoo` مؤجَّلة** (قرار 8) |
| تاريخية | 2 | 10 | `muhairis` · `uqair-castle` |
| دينية | 1 | 5 | **`tahimiyah-s` مؤجَّلة** (قرار 8) |

**والعناقيدُ أثبتت نفسها**: الدفعة 3 أغلقت المتاحفَ كلَّها بلا لفّة، والدفعة 4 أغلقت الدينيةَ بلفّةٍ واحدة.

---

## دروسٌ إجرائية تُنقل

1. **لا `git add -A` ووكيلٌ يكتب في الخلفية** — مسارات صريحة دائماً.
2. **كلُّ مرحلةٍ تنسخ ملفَ سابقتها ثم تحرّر المتغيّر وحده** — فيبقى غيرُ الممسوس بايتاً ببايت وتصير «بلا تغيير» مُثبَتةً بالفرق. (وسقفُ الإخراج 64000 رمز يُسقط من يكتب ملفاً كاملاً.)
3. **أثبت خطَّ الأساس قبل التطبيق** — بناءٌ واختباراتٌ على الشجرة غير الممسوسة، فيصير كلُّ فرقٍ منسوباً إلى الدفعة لا إلى الأدوات.
4. **تحقّق من فاحصك قبل أن تصدّقه** — كذب ستَّ مرات، وكذب فاحصي أنا مرّة. والإسقاطُ أخطرُ من الخطأ في العدّ: فاحصٌ يفحص في اتجاهٍ واحدٍ لبندٍ له اتجاهان يُعلن PASS كذباً.
5. **ابحث في الألمانية بالصيغ المصرَّفة لا بصيغة المعجم** (`\bBaudenkmal\w*` لا يلتقط `Baudenkmäler`).
6. **قِس المصدرَ قبل أن تخصم على الترجمة** — أربعُ دعاوى كبرى من خمسٍ في الدورة 2 كانت أمانةً للمصدر، وإصلاحُها كان سيكون خرقَ سياج.
7. **والحاكمُ يُراجَع كما تُراجَع المراحل**: نُقض له قياسٌ في الدورتين كلتيهما، ونُقض لي قياسٌ منه.

---

## أمرُ الجلسة الجديدة — انسخه كما هو

> أكمل من حيث توقّفنا في مستودع `visit-alahsa`، الفرع `claude/alahsa-site-audit-plan-u7kp5v`.
>
> اقرأ أولاً `docs/حالة-العمل-وأمر-الجلسة-التالية-2026-09-12-ب.md`.
>
> **عملُك الأول: قراري «مسموح دخول غير المسلم» (‏2026-09-12) — نفِّذه.** أضف بندَ «زيارة غير المسلمين» إلى الصفحات الدينية العشر، **بنسخِ الصيغة الخماسية اللغات حرفياً** من `src/content/attractions/jawatha-mosque.md` كما يأمر مدخلُ المعجم الألماني 62 — **لا خطَّ ترجمة ولا صياغةَ جديدة**، بـ`verified: true` و`verifiedAt: "2026-09-12"`. وخمسٌ منها (`bubakr-ribat` · `jalaniyah` · `qubba-mosque` · `tahimiyah-e` · `uqair-mosque`) **بلا حقل `practical` أصلاً** فالبندُ فيها يفتح البطاقة.
>
> **ولا تكتب ما لم أُجزه**: لا تصوير ولا دخولَ بيت صلاةٍ ولا غطاءَ رأسٍ ولا «كتفين وركبتين» — لا مصدرَ لها، ومدخلُ المعجم 62 يسجّلها ثغرةَ مصدرٍ بنفسه. وشرطُ اللباس المحتشم جزءٌ من الصيغة المُقرّة فيُنقل معها.
>
> **واسألني في اثنتين قبل أن تمسّهما**: `bubakr-ribat` رباطٌ لا مسجد، و`tahimiyah-s` لم تكن في العشر المعروضة عليّ.
>
> ثم `npm run build` كاملاً والاختباراتُ المتصفحية الأربعة (تفاصيلُها في القسم «التحقّق»، وتُنقل **لكلِّ أداةٍ على حدة** لا مجموعاً).
>
> **وراقب الطلب #44** حتى يُدمج — أخضرُ كلُّه على `6f0ac56` وينتظر دمجي. وإن دُمج قبل أن تبدأ فابدأ فرعَك من `main` الجديد بالاسم نفسه.
>
> **ولا تبدأ الدفعة 5 قبل إذني** — عنقودُ المنتزهات الثمانية (54 حقلاً، و`jawatha-park` وحدها 19). و`tahimiyah-s` و`taraf-zoo` مؤجَّلتان حتى أبتّ **قراري 8** («بانتظار التأكيد»: 11 موضعاً في 6 ملفات، وأربعةُ قرّاء عميان رفعوها) — **ذكّرني به.**
>
> وباقي ما ينتظرني: **POL-DE-18** و**POL-DE-15** (الحسمُ في المصدر يصلح أربع لغات) · **البند 5** (أزمنةُ التنقّلات — لا مفتاحَ خرائط) · **البند 9** (خطُّ الأساس، تذكيرٌ 2026-09-24) · **والمهمة 15**: `det.mentions.h` للروسية والصينية، عيبٌ حيٌّ في الإنتاج على 9 صفحاتٍ روسية و12 صينية، ولكلِّ لغةٍ خطُّها.
