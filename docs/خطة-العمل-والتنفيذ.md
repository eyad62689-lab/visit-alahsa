# خطة العمل — التنفيذ وحلقة التحقق

مرجع الفجوات: [خطة-تنفيذ-دراسة-التطوير.md](./خطة-تنفيذ-دراسة-التطوير.md).
هذا الملف هو **سجل التنفيذ**: ما نُفِّذ، وبأي دليل تحقق، وما التالي.

## قاعدة العمل

1. **ما تنفيذه بيدي أبدأ به فوراً** — لا انتظار.
2. **طلب واحد من إياد في كل مرة**، وحين يُجاب يأتي الذي يليه.
3. **حلقة تحقق مستقلة لكل مخرج**: لا يُعدّ البند منجزاً حتى يمرّ بفحص لا يعتمد على الكود الذي كتبه.

### شكل حلقة التحقق

| الطبقة | ماذا تُثبت | الأداة |
|---|---|---|
| 1. أنواع | لا خطأ نوع | `astro check` (صفر أخطاء شرط) |
| 2. بناء | الصفحات تُبنى فعلاً | `astro build` |
| 3. **المخرج المبنيّ** | الزائر يرى الصحيح | `tools/check-consistency.mjs` على `dist/` |
| 4. متصفح | العنصر ظاهر لا مخفيّ | تأكيد DOM حيّ على خادم المعاينة |

الطبقة الثالثة هي الأهم: تفحص `dist/` لا المصدر — فحص المصدر يثبت أن الكود صحيح، وفحص `dist` يثبت أن **المنشور** صحيح.

---

## المرحلة صفر: التوحيد — ✅ منجزة (2026-08-30)

### 0.1 اشتقاق عدد المعالم بدل تثبيته

**كان:** `HomeView.astro` يكتب `'60+'` نصاً حرفياً، والخريطة تحسب 58، وllms.txt يقول 58.
**صار:** `String(allEntries.length)` مشتقاً من المجموعة.

**التحقق المستقل:**
- `C3` في فاحص التضارب ينزع الوسوم من `dist/index.html` ويطابق الرقم المرئي بعدد ملفات المجموعة → `✓ الرئيسية تعرض 58 معلماً`.
- تأكيد DOM حيّ: `.fact-n` الثالث نصّه `58`، `opacity: 1`، `visibility: visible`.

### 0.2 توليد llms.txt وقت البناء

**كان:** `public/llms.txt` ملفاً ثابتاً فيه عددان بخط اليد (58 معلماً، 20 منشأة) يتقادمان بصمت.
**صار:** `src/pages/llms.txt.ts` يشتقّ الأعداد من مجموعة المعالم و`dining.ts` و`events.ts` والمدونة.
**زيادة:** أُضيف قسم المدونة (7 مقالات) وذكر النسخة الصينية (16 معلماً) — كلاهما كان غائباً عن الملف رغم وجوده على الموقع.

**التحقق المستقل:**
- `C2` يستخرج العدد من `dist/llms.txt` ويطابقه بعدد الملفات → `✓ llms.txt يعلن 58 معلماً`.
- فحص ترميز فعلي على الإنتاج: `curl -I` أثبت أن Netlify يخدم `charset=UTF-8` (والمحارف العربية سليمة)، والتشويه ظهر في `astro preview` وحده. ثُبِّت الرأس صراحةً في `public/_headers` رفعاً للاعتماد على السلوك الافتراضي.

### 0.3 فاحص التضارب في خط البناء

**المخرج:** `tools/check-consistency.mjs`، مربوط في `postbuild` فلا يمكن تخطّيه، ومتاح مستقلاً بـ`npm run check:consistency`.
ثماني مجموعات فحص على `dist/`:

| # | الفحص | النتيجة |
|---|---|---|
| C1 | صفحات المعالم المبنية = ملفات المجموعة | ✓ 58 |
| C2 | العدد المعلن في llms.txt = الحقيقة | ✓ 58 |
| C3 | العدد المعروض في الرئيسية = الحقيقة | ✓ 58 |
| C4 | لا أرقام عربية-هندية في أي صفحة | ✓ 200 صفحة |
| C5 | المصطلحات المرفوضة توثيقياً | ✓ |
| C5b | كل اسم شائع مقرون باسمه الموثّق | ✓ |
| C6 | البنود غير الموثّقة محجوبة عن بطاقة الزيارة | ✓ 58 صفحة |
| C7 | sitemap غير فارغ | ✓ 190 رابطاً |
| C8 | lastmod تواريخ حقيقية لا ملفَّقة | ✓ 190 |

**درجتا خطورة:** `fail` يُفشل البناء، و`warn` يُبلِّغ ولا يعطّل النشر — للبنود الموقوفة على قرار.

**تصحيحان ذاتيان أثناء البناء** (الفاحص أخفق أولاً وكان محقّاً في مرة وخاطئاً في مرة):
- C3 كان يفشل بسبب `<bdi>` متداخلة داخل `<span>` — أُعيدت صياغته على النصّ المجرَّد من الوسوم.
- C6 كان يبحث عن نصّ القيمة غير الموثّقة في الصفحات، وكل القيم عبارات نائبة («بانتظار التأكيد») ترد مشروعةً في نبذات أخرى — فأعطى إنذاراً كاذباً في 54 صفحة. أُعيدت صياغته ليقارن **عدد صفوف `<dt>`** في بطاقة الزيارة بعدد البنود الموثّقة في المصدر.

### حالة البناء بعد المرحلة

```
astro check        →  0 errors (102 ملفاً)
astro build        →  200 صفحة
check-consistency  →  10 فحوص ناجحة، صفر تحذيرات
```

### 0.4 حسم «الخبز الأحمر» — ✅ منجزة (2026-08-30)

**قرار إياد:** «الخبز الأحمر» و«خبز التمر» اسمان لطبقٍ واحد — فلا طبق تاسع يُضاف.

**ما نُفِّذ:**
- `dishes.ts`: أُثبت الاسم مرادفاً على بطاقة خبز التمر باللغتين («ويُسمّى أيضاً الخبز الأحمر» / «Also known as red bread (khubz ahmar)») مع تعليق إسناد في ترويسة الملف.
- `PlanTripView.astro`: قُرن الاسمان في المواضع الأربعة — «خبز التمر (الخبز الأحمر)» و«date bread (also called red bread)» — فصار كل ذكر للاسم الشائع موصولاً بالطبق الموثّق في صفحة الأكلات.

**التحقق المستقل:**
- الفحص انقلب من قائمة حظر إلى **قاعدة اقتران** (`C5b`): ورود الاسم الشائع في أي صفحة يلزمه ورود الاسم الموثّق في الصفحة نفسها، وإلا فشل البناء. هذا يمنع عودة الانفصال مستقبلاً بدل مجرد رصده مرة.
- تحقق على `dist`: بطاقة خبز التمر تحمل المرادف باللغتين، وصفحتا «خطط لرحلتك» العربية والإنجليزية تقرنان الاسمين في كل ورود.
- **الفاحص صار 10 فحوص كلها خضراء بصفر تحذيرات.**

---

## المرحلة الأولى: الفهرسة والقياس

### 1.2 `lastmod` في sitemap — ✅ منجزة (2026-08-30)

**كان:** صفر ورود لـ`lastmod` في `sitemap.xml` — لا إشارة حداثة لأي محرك.
**صار:** 190 رابطاً من 190 يحمل تاريخاً حقيقياً.

**قاعدة المصدر — تاريخ حقيقي أو لا تاريخ:**

| نوع الصفحة | مصدر التاريخ |
|---|---|
| مقال مدونة | `updatedDate ?? pubDate` من ترويسة المقال — إعلان الكاتب أوثق من تاريخ الالتزام |
| صفحة معلم | آخر التزام git مسّ ملف المعلم |
| صفحة قائمة | أحدث تاريخ بين قالبها وملفات محتواها (الرئيسية = القالب + القاموس + كل المعالم) |
| صفحة فعالية | آخر التزام مسّ `events.ts` أو قالب التفاصيل |

المخرج الجديد `src/lib/git-dates.ts`: استدعاء واحد لـ`git log --name-only` لكل بناء يبني خريطة (ملف → تاريخ)، وتُخزَّن في الذاكرة.

**الحارس ضد التلفيق:** الاستنساخ الضحل (shallow clone) يجعل `git log` يعيد تاريخاً موحَّداً لكل الملفات — رقماً كاذباً. الحقل يُرفع كلياً في تلك الحالة، لأن غياب `lastmod` أصدق من تاريخ موحَّد لا يعني شيئاً.

**التحقق المستقل:**
- `C8` في الفاحص: صيغة `YYYY-MM-DD`، لا تاريخ مستقبلي، ولا تاريخ واحد يعمّ كل الروابط → `✓ 190 lastmod، 8 تواريخ مميزة، أحدثها 2026-08-29`.
- تحليل XML مستقل بمُحلِّل خارجي: **XML صالح**، 190 عنصر `<url>`، ترتيب العناصر مطابق لمواصفة sitemaps.org (`loc` ثم `lastmod`) في 190 من 190، صفر تاريخ مستقبلي، والتوزيع طبيعي عبر ثمانية تواريخ لا كتلة واحدة.

**فخّ مكتشف أثناء التنفيذ:** Vite يخزّن وحدة `sitemap.xml.ts` مؤقتاً، فبناءٌ بعد تعديلها أعطى 14 تاريخاً فقط من 190 — والنتيجة الصحيحة ظهرت بعد إبطال الكاش. **عند تعديل أي endpoint وقياس مخرجه: احذف `dist` وأعد البناء، ولا تثق ببناء تفاضلي.**

### 1.1 ربط Google Search Console — ✅ منجزة (2026-08-31)

تبيّن أن الموقع **لم يكن مسجَّلاً في GSC أصلاً** — فجوة لم تلحظها الدراسة رغم عدّها البند منجزاً في المرحلة صفر.

- ملكية **Domain** بتحقق DNS: سجل TXT في لوحة Netlify (خوادم الأسماء `nsone.net` = Netlify DNS).
- التحقق **نجح**. سجلا البريد (`onmicrosoft.com` وSPF) سليمان لم يُمسّا.
- خريطة الموقع أُرسلت بنجاح — **228 رابطاً**.

**التحقق المستقل:** انتشار سجل TXT عبر ثلاثة محلّلات مستقلة (8.8.8.8 و1.1.1.1 و9.9.9.9) بنفس القيمة، ومنها محلّل قوقل نفسه.

**عطبان أمسكهما التشخيص:**
1. أخطأتُ في التعليمة الأولى فقلت اكتب `sitemap.xml` مجرداً — وهي صيغة ملكية URL prefix لا Domain.
2. قوقل ردّ «Sitemap is HTML». فحصتُ الخريطة الحيّة فوجدتها سليمة تماماً (HTTP 200، بلا BOM، XML صالح، 228 رابطاً، صفر خارج النطاق، صفر غير مُرمَّز، robots يسمح) — ثم جرّبت المسارات الخاطئة فوجدت **كل مسار غير موجود يعيد صفحة 404 تبدأ بـ`<!DOCTYPE html><html`**، وهو حرفياً ما وصفه قوقل (`Line 1, Tag: html`). فالخلل في الرابط المُرسَل لا في الملف — والعلاج إعادة الإرسال بالرابط الكامل.

**بالمناسبة:** صفحة 404 تُعاد بحالة 404 صحيحة لا 200 — فلا «soft 404».

### 1.4 قياس إحالات محركات الإجابة — ✅ منجزة (2026-08-31)

المؤشر الرئيسي في الدراسة. نُفِّذ على طرفين:

**(أ) جانب الزائر — GA4.** `src/lib/ai-referrers.mjs` قائمة واحدة بـ16 نطاقاً (ChatGPT، Perplexity، Copilot، Gemini، Claude، Grok، DeepSeek…)، تُحقن في سكربت GA4 عبر `define:vars` فلا تتفرّق نسختان. يُفحص `document.referrer` ثم `utm_source` (ChatGPT يُلحقه بروابطه، وقد يصل الزائر بلا مُحيل).

- حدث مستقل `ai_referral` بمعامل `ai_source` — الحدث المستقل يظهر في تقرير الأحداث بلا تسجيل بُعد مخصص، بخلاف معاملات `page_view`.
- ويُضاف `ai_source` إلى كل `page_view` أيضاً، لنسبة الصفحات لاحقاً.
- **يُحسب مرة واحدة عند دخول الجلسة**: مع ClientRouter يبقى `document.referrer` هو المُحيل الخارجي عبر كل تنقّل داخلي، فحسابه في كل `page_load` يضخّم العدّ.
- **المطابقة على حدود النطاق لا `includes`** — وإلا طابق `chatgpt.com.attacker.net`.

**(ب) جانب الخادم — سجل الزحف.** `netlify/edge-functions/lib/bot-kinds.mjs` (استُخرجت من `bot-log.mjs` لأنها تستورد `@netlify/blobs` غير المتاحة في Node) تصنّف 26 زاحفاً إلى ثلاثة أنواع:

| النوع | أمثلة | لماذا يُسجَّل |
|---|---|---|
| `answer` | OAI-SearchBot · ChatGPT-User · PerplexityBot · Claude-User · DuckAssistBot | جوهر المؤشر — من يقرأ الموقع ليجيب عنه |
| `search` | Googlebot · bingbot · Applebot · YandexBot | Bingbot بوابة Copilot |
| `training` | GPTBot · ClaudeBot · CCBot · Bytespider · Google-Extended | **كلها محجوبة في robots.txt — فتسجيلها يكشف من يتجاهل الحجب**، ولا تُرى بغير هذا السجل |

الحقلان `bot` و`botKind` أُضيفا للسجل بلا مساس بما يقرؤه `theft-scan`.

**التحقق المستقل — ثلاث طبقات:**

1. **اختبار وحدة** `tools/test-ai-detection.mjs` — **47 حالة**، مربوط في `prebuild` فيفشل البناء عند أي انحراف. السلاسل صيغ حقيقية تنشرها الجهات نفسها. يغطي: النطاقات الفرعية المشروعة، ومسار `duckduckgo.com/chat` دون بحثها العادي، وأولوية المُحيل على `utm`، و**أفخاخ انتحال النطاق** (`chatgpt.com.attacker.net`، `notchatgpt.com`)، و**فخّ ترتيب الزواحف** (`Applebot-Extended` يجب ألّا يُبتلع في `Applebot`).
2. **بناء نظيف** — `astro check` بصفر أخطاء، و13 فحصاً في فاحص التضارب خضراء.
3. **متصفح حيّ** على المخرج المبنيّ:

| الحالة | `ai_referral` | `ai_source` |
|---|---|---|
| بلا مُحيل | لم يُطلق | `(none)` |
| `?utm_source=chatgpt.com` | أُطلق | `chatgpt` |
| `?utm_source=perplexity` | أُطلق | `perplexity` |
| `?utm_source=chatgpt.com.attacker.net` | **لم يُطلق** | `(none)` |
| دخول من chatgpt ثم تنقّلان داخليان | **1** (لا 3) | `chatgpt` في `page_view` الثلاثة |

الصف الأخير هو الضمانة الحاسمة: المؤشر لا يتضخّم بالتنقّل الداخلي، والإسناد يتبع الزائر عبر الموقع.

**نقص لاحق (خطوة واجهة عند إياد، تأتي في دورها):** تسجيل `ai_source` بُعداً مخصصاً في GA4 (`المشرف ← تعريفات مخصصة ← إنشاء بُعد مخصص`، النطاق: حدث) — بدونه يظهر عدد `ai_referral` لكن لا تفصيله بالمصدر.

### 1.3 IndexNow عبر Netlify — ✅ منجزة (2026-08-30)

**التصحيح المعماري:** الدراسة اقترحت GitHub Action، وهو **ممتنع** — رمز OAuth بلا نطاق `workflow` يُعطِّل أي دفعة تلمس `.github/workflows/` (قرار 2026-08-16). البديل المنفَّذ: إضافة Netlify محلية تعمل عند `onSuccess`.

**المخرجات:**
- `netlify/plugins/indexnow/submit.mjs` — منطق الاختيار والإرسال، قابل للتشغيل مستقلاً بـ`--dry-run`.
- `netlify/plugins/indexnow/index.mjs` + `manifest.yml` — خطّاف `onSuccess`.
- `public/bfa79848415477bf3b25134e4b45f0a3.txt` — ملف المفتاح.
- `[[plugins]]` في `netlify.toml`.

**اختيار الروابط — لا دفع أعمى:** يُقرأ `lastmod` من sitemap المبنيّ (وهو مشتقّ من تواريخ git الحقيقية)، وتُدفع الروابط التي تغيّرت منذ آخر نشر ناجح عبر `CACHED_COMMIT_REF`. وبلا مرجع سابق يُقتصر على روابط اليوم — أضيق احتمال وأسلمه. سقف 10000 رابط لكل طلب.

**عدم إسقاط النشر:** كل المسار داخل `try/catch` يعيد سطر تقرير لا استثناءً — فشل الفهرسة لا يُفشل نشراً ناجحاً.

**التحقق المستقل — ثلاثة مسارات دخول:**

| المسار | النتيجة |
|---|---|
| CLI بلا مرجع نشر | `لا رابط تغيّر تاريخ اليوم (2026-08-30) من أصل 190 — لا إشعار` ✓ |
| CLI بمرجع نشر حقيقي (`HEAD~5`) | `98 رابطاً من 190 منذ 2026-08-29` ✓ |
| محاكاة `onSuccess` بواجهة Netlify | نفس النتيجة + عرض `status` في ملخّص النشر ✓ |
| `onSuccess` بلا `utils` وبلا `PUBLISH_DIR` | نجا بلا انهيار ✓ |

**عطبٌ حقيقي أمسكته الحلقة:** حارس «التشغيل المباشر» كان يقرأ `process.argv[1].replace(...)` — و`argv[1]` غير معرَّف حين تستورد الوحدةَ إضافةُ Netlify، فينهار **الاستيراد نفسه** ويسقط الخطّاف كله. لولا محاكاة `onSuccess` لظهر العطب أول نشرٍ على الإنتاج. أُبدل بحارسٍ يحتمل الغياب.

**فحص دائم:** `C9` يقارن اسم ملف المفتاح المنشور بمحتواه وبثابت `KEY` في الإضافة — انفصال أيٍّ منها يجعل كل إشعار يُرفض بصمت، وصار الآن يُفشل البناء.

**ما سيحدث عند أول نشر:** الإضافة تُشعر Bing وYandex وSeznam وNaver بالروابط المتغيّرة. للتعطيل: احذف قسم `[[plugins]]` من `netlify.toml`، أو اضبط `INDEXNOW_DRY_RUN=1` في متغيرات بيئة Netlify.

---

## المرحلة الثانية: التوثيق

### 2.1 إسناد كل معلومة تشغيلية — ✅ منجزة (2026-08-31)

بند الدراسة: «لا معلومة تشغيلية (موعد، سعر، فعالية) دون مصدر وتاريخ تحقق».

**كان:** ملاحظة واحدة «رُوجعت في يونيو 2026م» مكتوبة يدوياً في القالب، تسري على كل البنود بتاريخٍ واحد، ولا تقول من أين جاءت المعلومة — وتتقادم بصمت.

**صار:** خمسة حقول في نموذج `practical` — `source` و`source_en` و`source_zh` و`sourceUrl` و`verifiedAt` و`nextReview` — **وفحص Zod يرفض البناء** إن حمل بندٌ `verified: true` بلا `source` و`source_en` و`verifiedAt`. فاستحال نشر موعدٍ أو رسمٍ بلا إسناد: يوقفه البناء لا المراجعة البشرية.

**الملء:** 14 بنداً موثّقاً في ستة معالم، مصدرها «مسودة دليل الأحساء السياحي المعتمدة» بتاريخ تحقق 2026-06-21 — **وكلاهما موثّق في CLAUDE.md، لا مُستنتَج**.

**العرض:** سطر إسناد أسفل بطاقة الزيارة يجمع البنود المتشاركة في المصدر والتاريخ فلا يتكرّر تحت كل صف، وبوسم `<time datetime>` آلي القراءة.

**التحقق المستقل — خمس طبقات، أمسكت ثلاثة عيوب حقيقية:**

| # | ما فُحص | النتيجة |
|---|---|---|
| 1 | **إثبات أن الحارس يحرس**: أُزيل الإسناد من بند عمداً | البناء فشل برسالة مقروءة ✓ ثم استُعيد الملف |
| 2 | `astro check` | صفر أخطاء (110 ملفات) |
| 3 | `C12` على `dist` | الإسناد ظاهر في 6 صفحات موثّقة، عربية وإنجليزية |
| 4 | `C13` على `dist` | لا رمز لون دون AA في سطر الإسناد |
| 5 | متصفح حيّ | النصّ والتباين والتدفّق |

**العيوب الثلاثة:**

1. **تسريب لغة**: اسم المصدر ظهر **بالعربية داخل الصفحتين الإنجليزية والصينية** — وهي المخالفة نفسها التي يحرسها فلتر `label_en/value_en` في البطاقة. العلاج: حقلا `source_en` و`source_zh`، والمخطط يفرض `source_en`. وأُضيف إلى `C12` فحصٌ يرفض أي حرف عربي في سطر الإسناد بالصفحة الإنجليزية.
2. **تباين دون AA**: قِسته في المتصفح فكان `--c-ink-60` يعطي **4.36:1** على خلفية البطاقة `#EFE8D6` — دون العتبة 4.5، **بعكس ما كتبتُه في التعليق**. بُدِّل بـ`--c-ink-soft` (‏**8:1** مقيسة). و`C13` يمنع عودة أي رمز خافت بلا قياس.
3. **تاريخ بلغة خاطئة**: `formatDate` كان يعطي الصينية صيغة `en-GB`. أُضيف `zh-CN` فصار «2026年6月21日» — صيغة Intl لا نصّاً مؤلَّفاً، فلا تمرّ بخط الترجمة الصينية.

النتيجة في اللغات الثلاث:

| اللغة | سطر الإسناد |
|---|---|
| عربي | المصدر: مسودة دليل الأحساء السياحي المعتمدة · تاريخ التحقق: 21 يونيو 2026 |
| إنجليزي | Source: Approved draft of the Al-Ahsa tourist guide · Verified: 21 June 2026 |
| صيني | 来源: Approved draft… · 核实日期: 2026年6月21日 |

الصينية تتراجع للإنجليزية في اسم المصدر عمداً — أي نص صيني جديد يلزمه خط `zh-translation-pipeline`، ولا يُختلق هنا.

**الفاحص صار 15 فحصاً كلها خضراء.**

---

## الطابور — بالترتيب

| # | البند | المنفِّذ | الحالة |
|---|---|---|---|
| 0.4 | حسم «الخبز الأحمر» | إياد ثم أنا | ✅ منجزة |
| 1.1 | ربط Google Search Console | إياد ثم أنا | ✅ منجزة — 228 رابطاً مُرسلة |
| 1.2 | `lastmod` في sitemap | أنا | ✅ منجزة |
| 1.3 | IndexNow عبر Netlify (لا GitHub Action) | أنا | ✅ منجزة |
| 1.4 | تصنيف إحالات الذكاء الاصطناعي في GA4 + توسيع `bot-log` | أنا | ✅ منجزة |
| 1.5 | ربط Bing Webmaster Tools (استيراد من GSC) | إياد | ✅ منجزة |
| 1.6 | تسجيل `ai_source` بُعداً مخصصاً في GA4 | **إياد** | ✅ منجزة — أنجزها إياد 2026-09-03؛ تحقُّق الطرف البرمجي على الإنتاج: `/?utm_source=chatgpt.com` يُلقي `ai_referral` بـ`ai_source: chatgpt` |
| 2.1 | توسيع نموذج `practical` بحقول الإسناد | أنا | ✅ منجزة |
| 2.2 | بيانات مواعيد ورسوم المعالم (43 معلماً بلا أي بيان — عدّ فعلي 2026-09-03) | **إياد** | ⏳ **الطلب المعروض الآن** — ملف الاستقبال `مصادر-المشروع/مستندات-العمل/مواعيد-ورسوم-المعالم-للتعبئة.csv` (51 صفاً) مرسَل 2026-09-03 |
| 3.1 | `sameAs` + Wikidata + اليونسكو | أنا (يُراجعها إياد) | ✅ **منجز بطلب دمج** (2026-09-04، فرع `feat/sameas-wikidata`): حقلا `sameAs` (أولها كيان Wikidata حصراً — يرفض المخطط غيره) و`unesco` (معرّف المكوّن `1563-NNN`) في ترويسة المعلم؛ `DetailView` يصدرهما في `TouristAttraction` (‏`sameAs` + `identifier` + عقدة الواحة 1563 ثانيةً في `isPartOf`)، والرئيسيات الأربع تحمل `OASIS_SAMEAS` (‏Q311341 + قائمة اليونسكو + ويكيبيديا الأربع) من `lib/schema.ts`. **16 معلماً** بكيان محقَّق (لا بالاسم: مطابقة إحداثيات ≤0.5كم أو نصّ المقالة — Wikidata فيه «جبل القارة» يمنيّان ومصري و«قصر خزام» جدّاوي) و**6 مكوّنات يونسكو** من جدول `whc.unesco.org/en/list/1563/maps/` الرسمي: إبراهيم 004 · القيصرية 005 · خزام 006 · صاهود 007 · جواثى 009 · الأصفر 012. جبل القارة بلا معرّف (داخل الواحة الشرقية 001)، ومنتزه جواثى لم يُربط بالموقع الأثري 008 (كيان آخر على بُعد 1.2كم). **التحقق المستقل**: 911 عقدة JSON-LD في 314 صفحة كلها صالحة، 47 رابطاً فريداً ASCII خالصاً و46 تعيد 200 (اليونسكو تحجب UA الفاحص وتعيد 200 لمتصفح)، وكل كيان في صفحات معلمه وحده باللغات الأربع. يحرسه **`C19`** (المصدر = المنشور لكل لغة، لا كيان مشترك، حارس إيجابي ≥10/≥6) — اختُبر بكسرين عمداً (كيان مكرَّر · حذف معرّف يونسكو) فأخفق بـ1 في كليهما. |
| 3.2 | `faq` لكل معلم + تصدير `FAQPage` | أنا (يُراجعها إياد) | ✅ **منجز بطلب دمج** (2026-09-05، فرع `feat/attraction-faq` **فوق `feat/ru-integration`** لأن الملفات الأربعة نفسها — المخطط و`DetailView` والقاموس والفاحص — مفتوحة في #17، فالبناء على main كان سيتعارض معه حتماً): حقل `faq` في مخطط المعالم على نمط المدونة بحقول `q/a` و`q_en/a_en` (و`_zh/_de/_ru` اختيارية بوابةً للدفعات القادمة عبر خطوط الترجمة — البند لا يظهر في لغة بلا ترجمته، كبطاقة الزيارة)، `DetailView` يعرضها قسماً بعد المتن (`<details>` بأنماط `post-faq` حرفياً وبعرض المتن 68ch) ويصدر `FAQPage` في JSON-LD، ومفتاح `det.faq` (ar/en، وzh من ذاكرة الخط الصينية `faqH`؛ de وru تتراجعان للإنجليزية ولن يُصيَّر العنوان قبل بنود بلغتهما). **14 معلماً** (كل ما فيه بند موثّق): **55 سؤالاً عربياً و54 إنجليزياً** — كلها من متن الصفحة وبنودها الموثّقة حصراً، ولا موعد ولا رسم من خارجها (شاطئ العقير: الكرفانات وأفضل وقت والملاءمة للعائلات، لا مواعيد؛ عدد مغارات جبل القارة بالعربية وحدها لأن المتن الإنجليزي لا يذكره). يحرسه **`C21`** على dist: FAQPage = المصدر حرفياً لكل لغة (وجوداً وترتيباً ونصاً، ولا FAQPage لصفحة بلا بند بلغتها)، القسم المرئي = السكيما عدداً (قوقل تشترط ظهور المحتوى المنظَّم)، **وكل رقم في سؤال أو جواب يرد في متن الصفحة أو بطاقة الزيارة بحدود رقمية** — والبند غير الموثّق لا يُصيَّر أصلاً فلا يُجيز رقماً — وحارس إيجابي ≥10 معالم، وصيغة المصدر محدَّدة (سلاسل مزدوجة التنصيص بهروب JSON؛ سطر خارجها يُفشل الفحص لا يتخطّاه). **أمسك عيباً حقيقياً في نفسه أول تشغيل**: «عام 2018.» — عُدّت النقطة الختامية فاصلاً عشرياً فأخفق البناء بـ`exit 1` على قصر إبراهيم بالعربية والإنجليزية؛ أُصلحت الحدود فصارت رقمية لا ترقيمية («18» لا تُقبل لـ«8» و«8:00» لا تُقبل لـ«8»، والنقطة الختامية تُقبل). ثم **ثلاثة كسور عمداً على نسخ dist لا على المصدر** (تعديل جواب في JSON-LD · حذف بند مرئي · إزالة عقدة FAQPage) فأخفق بـ`exit 1` في الثلاثة بثلاث رسائل مختلفة وعاد أخضر بعد الاستعادة من الذاكرة — بلا `git checkout` ولا إعادة بناء. **التحقق المستقل**: DOM حيّ على المعاينة (`.att-faq` بـopacity 1 وأربعة `<details>` والثاني يُفتح بالنقر). البناء أخضر: `astro check` صفر أخطاء، الفاحص **22 معرّفاً · 24 سطر نتيجة**، 47 صفحة مفحوصة و109 أسئلة منشورة. **الصينية أُنجزت** (2026-09-05، الفرع نفسه): **54 سؤالاً** لكل بند له `q_en` في المعالم الـ14 عبر `zh-translation-pipeline` بثلاث دفعات **15/16/17 — 92 · 91 · 92 كلها من الدورة الأولى** (السجل في `zh-translation/memory/scores.csv`)؛ بند جبل القارة العربي وحده تُخطّي. قواعد الدفعة المثبَّتة: قالب صيني واحد لكل نوع سؤال عبر الصفحات (`templates` من الدفعة 15 حرفياً، وفرعا `feeFreeVenue`/`hoursByDay` من 17)؛ لا گلوس لاتيني داخل الأسئلة (المتن فوقها يحمله)؛ الأرقام بصيغة `value_zh`/`body_zh` حرفياً وإلا أسقطها `C21` (163 سؤالاً منشوراً الآن)؛ «每日» تُكتب فقط حين تحملها بطاقة الصفحة؛ لاحقة «此信息已由本站编辑团队确认» حين يحملها المصدر أو بطاقة الصفحة نفسها. القارئ الأعمى أعطى 39 ملاحظة على الثلاث، طُبّق أو طُبّق جزئياً 15 ورُفض 24 بالسوابق (سياج الوقائع، شكل حقل المنطقة، الگلوس، الأسماء المقيَّدة). **علل مصدر رصدها الخط لإياد** (لم تُمسّ الحقول المنشورة): الكلابية «قرية» في أسئلة مسجد جواثى و«بلدة» في أسئلة منتزهه؛ «10–15 ريالاً» بلا تفصيل فئات في بطاقتَي جواثى؛ المسجد «مفتوح طوال اليوم» وبطاقته تحمل مواعيد المنتزه 8:00–24:00؛ الجمعة في القيصرية «傍晚» مقابل فترة 15:30؛ `value_zh` مواعيد أرض الحضارات فيه «与旺季» بلا نظير؛ مدخل termbase «Civilizations» بـz والصفحة بـs؛ وبطاقة سوق الحرفيين تنسب الساعات لـ«商家» بينما الأسئلة لـ«市集方». **المتبقي**: الألمانية والروسية عبر خطّيهما، والمعالم الـ44 الباقية بعد وصول بيانات 2.2 — لا أسئلة لمعلم بلا بند موثّق. |
| 3.3 | `openingHoursSpecification` + `offers` | أنا (بعد 2.2) | ينتظر دوره |
| 4.1 | حقل `answer` (فقرة Answer-First) لأهم 15 صفحة | أنا | ينتظر دوره |
| 4.2 | ثلاثة جداول HTML | أنا | ينتظر دوره |
| 5.1 | صفحة «من نحن والسياسة التحريرية» | أنا (يعتمدها إياد) | ينتظر دوره |
| 5.2 | صفحة اليونسكو الشاملة | أنا | ينتظر دوره |
| 5.3 | صفحة الفرق بين الأحساء والهفوف والمبرز | أنا | ينتظر دوره |
| 5.4 | صفحة العائلات والأطفال | أنا | ينتظر دوره |
| ب.٢ | `fetch-places.mjs` يتخطّى نداء Places API حين `CONTEXT !== 'production'` (المعاينات تُبنى بلا تقييمات — `places-live.json` لا يُخزَّن بشرط قوقل)، **ثم** يحصر إياد المفتاح في Production. طلب دمج مستقل. | أنا ثم إياد | ✅ **الجزء البرمجي منجز** — PR #10 (2026-09-04): تحقق `exit 0` في كل السياقات، بناء كامل بـ`CONTEXT=deploy-preview` أخضر، ومعاينة Netlify للطلب بلا نجمة واحدة في صفحة المطاعم مقابل 29 في الإنتاج. **المتبقي بيد إياد بعد الدمج:** حصر `GOOGLE_PLACES_API_KEY` في نطاق Production على Netlify. |
| ب.٣ | استعادة قيمة PR #3 المغلق على فرع جديد من `main`: مستخرج الجمل الصينية في `scripts/fingerprint.mjs` + `hl=zh-cn` في `theft-scan.mjs`، وإعادة توليد البصمات بالسكربت | أنا | ✅ **منجز بطلب دمج** (2026-09-04، فرع `feat/zh-fingerprints` من `main`): **58 بصمة صينية** (كل معلم يحمل `title_zh`) فصار المجموع **224** (82 عربية · 84 إنجليزية · 58 صينية). ما لم يُنقل من #3: سطر `Base.astro` (وصل main أصلاً) ومنطق «لا كتابة إن لم تتغير» (على main بصيغة مكافئة) وملف JSON (مولَّد لا منقول). زيادة على #3: القصّ عند آخر فاصلة CJK لا وسط اسم لاتيني بين قوسين. **التحقق المستقل:** سكربت خارج الخط يفكّ كيانات HTML ويطابق كل عبارة حرفياً في صفحة `dist` المقابلة — 224/224، وصفر تكرار في العبارات والمعرّفات. |
| ب.٤ | بصمات الألمانية بعد دمج #12: فرع `de` في `scripts/fingerprint.mjs` (بوابة `title_de`، مسار `/de/attractions/`، `pickPhrase` اللاتيني نفسه) + `hl=de` في `theft-scan.mjs` | أنا | ✅ **منجز بطلب دمج** (2026-09-04، فرع `feat/de-fingerprints` من `main` بعد #13): **6 بصمات ألمانية** (المعالم الستة التي تحمل `title_de`) فصار المجموع **230** (82 عربية · 84 إنجليزية · 58 صينية · 6 ألمانية)، والـ224 القديمة لم يتبدّل منها حرف. زيادة ألمانية وحيدة: **حارس الأعداد الترتيبية** — «im 12. Jahrhundert» كانت النقطة بعد الرقم ستُقرأ فاصلَ جملة فتبدأ بصمة قصر صهود بـ«Jahrhundert nach der Hidschra»؛ الحارس ألماني فقط لأن قوائم المدونة المرقّمة عناصر `<li>` مستقلة وضمّها ينتج عبارة لا وجود لها. **التحقق المستقل:** السكربت نفسه (الوسوم الداخلية بلا مسافة) — 230/230 حرفياً في `dist`، والمعرّف المخفي في كل صفحة ألمانية يطابق معرّف بصمتها، وصفر U+00A0، وصفر تكرار؛ واختُبر بكسر عبارة عمداً فأخفق بـ1. البناء 21 فحصاً. كل معلم ألماني قادم يحصل بصمته تلقائياً في prebuild بلا تعديل. |
| ب.٥ | **التكامل الهيكلي للروسية ru-RU**: تنفيذ قائمة `ru-translation/PLAN.md §٦` مرة واحدة — `localize` إلى صيغة السجلّ، خطوط السيريلية، نواة `/ru/`، حارس `C20`، وru في أداة قياس الترويسة | أنا | ✅ **منجز** (2026-09-05، فرع `feat/ru-integration` فوق `feat/ru-pipeline`): `Lang` صار خماسياً و`ui.ru` **جزئي جداً** (`lang.menu` وحده) فيتراجع كل شيء ru ← en ← ar، وبوابة `title_ru` تولّد `/ru/attractions/[slug]`، ونواة `/ru/` (رئيسية + فهرس) على غرار `/de/`. **صفحة واحدة منشورة**: جبل القارة (91/100) — حقولها منسوخة **حرفياً** من `ru-translation/output/` وتحقّقت بمقارنة سلسلةٍ بسلسلة. ثلاثة أعمال جديدة على هذا الخط: **① `localize` إلى صيغة السجلّ** `{ ar, en, zh?, de?, ru? }` بترحيل **13 موضعاً** وإزالة الصيغة الموضعية (لا واجهتان لدالة واحدة)؛ و`NoInfer` جُرِّب فرُدَّ لأنه يعدّ كتل الجمع الإنجليزية (صيغتان) ناقصةً أمام العربية (خمس صيغ). **② ستة استيرادات سيريلية** من الحزم المثبّتة أصلاً — بلا حزمة جديدة وبلا preload (‏`plex-lat-400` المحمَّل مسبقاً مداه لاتينيّ خالص فلا يفيد الروسية، وLora cyrillic أصلٌ مبصوم لا يُحمَّل بلا قياس). **③ القياس قبل التسمية**: الأداة صارت خمس لغات، واللازم مقيساً ar ‏1123px · en ‏1188 · zh ‏1023 · de ‏1298 · **ru ‏1188** — الإنجليزية بالضبط لأن تسميات التنقّل تتراجع إليها؛ 75 خانة خضراء (‏15 عرضاً × 5 لغات، `exit 0`)، فلا عتبة جديدة، **وميزانية دفعة `ui.ru` معروفة سلفاً: 630px**. **④ `C20`** (حكم المعايرة P1): يُفشل البناء إن ظهرت `Эль-Ахс…` أو `Аль-` أمام غير `Ахс` في نصّ أي صفحة `/ru/`، بحارسٍ إيجابيّ على الجذع لا على المرفوع (الروسية تُصرّف بست حالات) — **اختُبر بكسر الاتجاهين معاً** فأخفق بـ`exit 1` وأبلغ عن الموضعين، ثم عُكس الكسر تحريراً مضاداً لا بـ`git checkout`، والملف مطابقٌ بايتاً ببايت لنسخته قبله. و`C19` وُسِّع بالروسية (توسعة بنيوية: 54 صفحة والرئيسيات الخمس) بينما **`C17` و`C18` بقيا خاصَّين بـ`/de/`** كما نصّ المعجم. البصمات 230 ← **231**. الفاحص **21 معرّفاً · 23 سطر نتيجة**، والبناء أخضر (‏`astro check` صفر أخطاء وصفر تحذيرات). **صحّح التنفيذُ خطأً في الخطة**: بند «كتل `*_RU` في `src/data` على نمط `*_DE`» لا محلّ له — لا وجود لأي كتلة `*_DE` هناك أصلاً، والألمانية تتراجع للإنجليزية بترنري، والروسية تفعل مثلها بلا سطر. **المتبقي: دفعة `ui.ru`** (القاموس والسلاسل المضمّنة التي تعطي الإنجليزية عمداً الآن) وبقية المعالم الـ57. |
| ب.٦ | **دفعة `ui.ru`** — نصوص الواجهة الروسية عبر الخط بسبع مراحل (Opus لكل مرحلة) | أنا | ✅ **منجز** (2026-09-05، فرع `feat/ru-ui` فوق `feat/ru-integration`، الحاكم **91/100 · APPROVE من الدورة الأولى**، المحاور 28/26/19/8/10): **164 سلسلة** = 144 مفتاحاً في `ui.ru` + 10 في `CATEGORY_LABELS.ru` + 10 سلاسل مضمّنة. **قرار مقيس في الشريط**: التسميات الطبيعية تلزمها 1391px مقابل ميزانية 1188 (35 مرشّحاً و6 مجموعات مقيسة بأداة على الصفحة الحقيقية)، فانضمّت الروسية إلى كتلة الألمانية المضغوطة (`[lang='de'], [lang='ru']`) واللازم تحتها **1291px** بهامش 8px — `Что увидеть` في الشريط و`Достопримечательности` فيما عداه (سابقة Sehenswertes). **P6 بُتّت** (`Практическая информация` · `Расположение` · `Лучшее время дня`) و**C14 وC15 وُسِّعا بالروسية** واختُبرا بكسر dist. القارئ الأعمى: 38 ملاحظة — 7 مطبَّقة · 18 مرفوضة · **13 علّة مصدر** (SD1–SD14، خمسٌ تطابق طابور إياد وواحدة كودية: 7 مفاتيح ميتة في القاموس). حكمان جديدان: گلوس الاسم الثاني بـ«или»، والشرطة بين مرفوعين رابطة. 12 مدخل معجم (مزرعة الليمون `pending` عند إياد). البناء أخضر 23 فحصاً، والقياس 75 خانة خضراء. |
| ب.٧ | **الدفعة الروسية الأولى للمعالم `ru-attr-1`**: خمسة معالم كاملة بترتيب `order` (قصر إبراهيم · العُقير · مسجد جواثا · صاهود · الأميرية) ببطاقاتها الموثّقة وأسئلتها `q_ru/a_ru`، + أسئلة جبل القارة الروسية (بقية البند 3.2 للروسية على الصفحات المنشورة) | أنا | ✅ **منجز بطلب دمج** (2026-09-05، فرع `feat/ru-attractions-1` **فوق `feat/attraction-faq`** لأن حقول الأسئلة فيه): سبع مراحل بوكيل مستقل لكلٍّ، **91/100 · APPROVE من الدورة الأولى** (الصفحات 92/91/91/91/91/92؛ المحاور 28/26/19/8/10؛ السجل في `ru-translation/memory/scores.csv`، والنصوص في `ru-translation/output/attractions/`). **28 حقلاً و6 بنود بطاقة و17 سؤالاً** — أسئلة الـ13 معلماً الباقية لم تُترجم عمداً: لا صفحة روسية لها بعد فلن تُصيَّر ولن يعدّها `C21`؛ تُترجم مع دفعات صفحاتها. **قرار مقيس لا ذوقي**: أسئلة FAQ الروسية تُنتَج مع صفحاتها لا قبلها. سياج الوقائع حذف 3 (تفضيل «самыми»، وگلوسان للهجري أعادتهما المرحلة 4 إلى الصيغة الثابتة `hidzhra`). القارئ الأعمى **36 ملاحظة: 15 مطبَّقة · 11 مرفوضة بسابقة · 10 علل مصدر** — أهم المطبَّق: جملة صاهود «стены охраняют башни» كانت تُقرأ معكوسةً (الرفع والنصب متطابقان) فقُلب الترتيب؛ سؤال «لماذا مهم» بجواب بلا سبب صار «Чем славятся…»؛ سلسلة إسناد فريق الموقع «редакцией сайта «Аль-Ахса»» قُرئت اسمَ موقع آخر فصارت **«Подтверждено редакцией этого сайта»** (نظير 本站). **18 إضافة معجم** (8 محقَّقة على ru.wikipedia/saudipedia · 7 بالنمط pending · 3 سلاسل قرار)، وتصحيحات المرحلة 4: `Эль-Килабия` (كانت Килябия — مقالة «Мечеть Джаваса»)، `мечеть аль-Джумах` (عنوان المقالة)، «ибн Абу Бакра» (الكنية تثبت في المرفوع). **فخّ مرصود**: ru.wikipedia تعنون المسجد «Мечеть Джаваса» والمعجم يثبّت «Джавата» (تؤيّده saudipedia) — لم يُمسّ. **علل مصدر جديدة لإياد**: `area_en` «east/northeast of Al-Ahsa» في العقير وجواثا تجعل الأحساء نقطةً لا إقليماً (يصيب اللغات الخمس)؛ صاهود بلا مقابل ميلادي لـ«أواخر القرن الثاني عشر الهجري»؛ منتزه جواثا يظهر في البطاقة وحده بلا ذكر في المتن؛ وبقية العلل في طابوره (2.2 لصاهود/الأميرية/العقير، فئات 10–15). **`det.faq` الروسية «Часто задаваемые вопросы» معتمدة من الخط لكن لم تُكتب في `ui.ts`** على هذا الفرع لأن #18 يعيد كتابة كتلة `ru` كاملة (تعارض مؤكَّد) — تُضاف بسطر واحد بعد دمج #18؛ حتى ذلك يظهر العنوان بالإنجليزية كما تظهر بقية واجهة `/ru/` على هذا الفرع. البناء أخضر: `astro check` صفر أخطاء، **24 فحصاً**، `C20` على 8 صفحات روسية، `C21` **180 سؤالاً منشوراً** (كان 163). الأدوات في سكراتش الجلسة (`ru-attr-1/`: BRIEF · pack · verify مختبَر بـ17 خطأ مزروع · mkblind · apply مجرَّب على fake-root) قابلة للتكرار للدفعات القادمة. **المتبقي: 52 معلماً** دفعات من 5 بترتيب `order`. |
| ب.٨ | **الدفعة الروسية الثانية للمعالم `ru-attr-2`**: خمسة معالم بترتيب `order` (قصر محيرس · قصر خزام · بيت البيعة · قصر العقير · مسجد الجبري) — خمسة حقول لكل صفحة، بلا بنود بطاقة ولا أسئلة لأن مصدرها خالٍ منهما | أنا | ✅ **منجز بطلب دمج** (2026-09-06، فرع `feat/ru-attractions-2` من main بعد دمج #16–#20): سبع مراحل بوكيل Opus مستقل لكلٍّ، الحاكم **90/100 · APPROVE من الدورة الأولى** (الصفحات 91/91/91/90/90؛ المحاور 28/25/19/8/10) — على حافة العتبة لا فوقها. سياج المرحلة 4 حذف 3 وقائع مضافة (مادة الجدران، «разные по форме»، نقطتان سببيّتان)؛ القارئ الأعمى 46 ملاحظة: 15 مطبَّقة · 16 مرفوضة بحكم مسنَد · **15 علّة مصدر** (أثقلها: لا بند بطاقة في الصفحات الخمس والأربع منها تدعو للزيارة؛ مسجد الجبري بلا آداب زيارة؛ «قصر» عنواناً و«حصن» متناً في محيرس وخزام؛ «الإفتاء» ساقط من الإنجليزية). المعجم +20 (13 محقَّق من ru.wikipedia · 7 بالنمط) = 95 مدخلاً؛ قراران مسنَدان: «Умар» لا «Омар» و«Укайрский договор» لا «протокол» (4 مقالات مقابل صفر). `det.faq` الروسية كُتبت في `ui.ts` (كانت معتمدة من ب.٧ ومؤجَّلة لتعارض #18). البناء أخضر: 24 فحصاً، `C20` 13 صفحة. |
| ب.٩ | **الدفعة الروسية الثالثة للمعالم `ru-attr-3`**: خمسة مساجد بترتيب `order` (الجعلانية · الدبس · رباط آل أبي بكر الملا · القبة · التهيمية الغربي) — خمسة حقول لكل صفحة، بلا بنود بطاقة ولا أسئلة | أنا | ✅ **منجز بطلب دمج** (2026-09-06، فرع `feat/ru-attractions-3` فوق `feat/ru-attractions-2`): سبع مراحل بوكيل Opus مستقل لكلٍّ، الحاكم **90/100 · APPROVE من الدورة الأولى** (الصفحات 91/91/90/91/90؛ المحاور 27/26/19/8/10). **تصحيح مسنَد بتنبيه إياد**: الجعلانية بالعين ⇐ «Эль-Джаалания» لا «Эль-Джалания» (جدول النقحرة العربية-الروسية: العين الساكنة تُطيل الفتحة «بعث ⇐ Баас»؛ و«Джаалан» لجعلان العُمانية في مقالتين) — سرى على المعجم وذاكرة الترجمة ومتن مسجد جواثا المنشور؛ والإنجليزية «Al-Jalaniyah» بلا عين عِلّة مصدر. حكمان آخران: الإدغام الشمسي «Эд-Дибс» (سابقة «Эд-Диръия»)، وعائلة «Аль Абу Бакр аль-Мулла» بلا واصلة (نمط «Аль Сауд»). سياج المرحلة 4 حذف واقعتين («обращённой к Мекке» صحيحة وليست في المصدر؛ «держится на»)؛ القارئ الأعمى 43 ملاحظة: 22 مطبَّقة · 14 مرفوضة · 7 علل مصدر (أثقلها: لا صفحة تقول هل يُسمح بالدخول؛ مسجد القبة «أُسّس عام 979هـ بعد خمس سنوات بناء» متناقض في العربية والإنجليزية معاً؛ صفحة التهيمية عن القرية لا عن المسجد). المعجم +27 = 122 مدخلاً (12 محقَّق · 14 بالنمط). البناء أخضر. |
| ب.١٠ | **الدفعة الروسية الرابعة للمعالم `ru-attr-4`**: خمسة مساجد بترتيب `order` (التهيمية الشرقي · الحبيش · أم زرينيق · مسجد ميناء العقير · مسجد الشيخ محمد بن أبي بكر الملا) — خمسة حقول لكل صفحة، بلا بنود بطاقة ولا أسئلة | أنا | ✅ **منجز بطلب دمج** (2026-09-06، فرع `feat/ru-attractions-4` فوق `feat/ru-attractions-3`): سبع مراحل بوكيل Opus مستقل لكلٍّ، الحاكم **90/100 · APPROVE من الدورة الأولى** (الصفحات 91/91/90/90/90؛ المحاور 27/26/19/8/10). **النقحرة من الرسم العربي حصراً** (درس الجعلانية): أربعة مواضع اختلفت فيها الإنجليزية عن العربية حُسمت كلها للعربية — «الرفعة الجنوبية» نُقحرت `Эр-Рифаа аль-Джанубия` لا تُرجمت (سابقة ru.wikipedia «Эр-Рифа-эш-Шарки» البحرينية)، «نهر الزهيري» نهرٌ لا جدول، «الشندل» بالشين، وبيت الشيخ أبي بكر لا «العائلة». أحكام مسنَدة: الأداة بعد «Абу» بحرف واحد مدغَماً (`Абу-н-Нууш` على «Абу-ль-Фида»/«Абу-н-Насир»)، `Умм-Зурайник` بواصلة («Умм-Каср»)، `Эз-Зухайри` («Эз-Зубайр»)؛ ومقاييس المبنى (المساحة والسعة) أرقاماً والتقريب السردي بالكلمة. سياج المرحلة 4 حذف 3 (تعميم «أقدم المساجد» بإسقاط «التراثية»، و«самом/самых»)؛ القارئ الأعمى 37 ملاحظة: 12 مطبَّقة · 21 مرفوضة · 4 علل مصدر — أمسك «всего» يقلب المعنى (نموّ سبعة أضعاف يُقرأ ضئيلاً) واسم «المسجد الشرقي» على مسجدين في الموقع. المعجم +12 = 134 (كلها بالنمط: لا شاهد روسي لأي منها). 12 علّة مصدر لإياد أثقلها: لا معلومة زيارة في الصفحات الخمس وثلاث تدعو للزيارة؛ مسجد ميناء العقير «رُمّم» و«يُعاد تأهيله الآن» معاً؛ ثلاثة أسماء في الموقع لبرنامج المساجد التاريخية. البناء أخضر. |
| ب.١١ | **الدفعة الروسية الخامسة للمعالم `ru-attr-5`**: خمسة جبال بترتيب `order` (الشبعان · كنزان · الأربع · الثليم · أبو حصيص) — خمسة حقول لكل صفحة، وشارة جديدة «معلم طبيعي» ⇐ «Природная достопримечательность» | أنا | ✅ **منجز بطلب دمج** (2026-09-06، فرع `feat/ru-attractions-5` فوق `feat/ru-attractions-4`): سبع مراحل بوكيل Opus مستقل لكلٍّ، الحاكم **90/100 · APPROVE من الدورة الأولى** (الصفحات 90/91/91/90/91؛ المحاور 28/26/19/7/10 — ملاءمة المسافر 7 لأن صفحتي الشبعان والثليم بلا نشاط ملموس في مصدرهما). **عنوان الجبال «Гора X»** لا «Джабаль-X» بحكم `titel_regel`: الإنجليزية ترجمت اسم الجنس («X Mountain») بخلاف «Jabal Al-Qarah» — قاعدة واحدة ومدخلان؛ والمنقحَر في گلوس المتن «(по-арабски — Джабаль-эш-Шабаан)». **حكم العين ثلاثي الفروع** كُتب في `termbase._meta.naming_policy.ayn` ويسري على كل ما بقي: عين + حركة تُطال الحركة (Эш-Шабаан)؛ عين ساكنة + صامت تُضاعَف السابقة (Эш-Шууба، كالجعلانية)؛ عين آخر الكلمة تُسقَط (Эль-Арба، Эль-Ку بسند «Янбу») — وأسقط صيغتي المرحلة 1 «Эль-Арбаа/Эль-Куу». ث ⇐ «с» (Эс-Сулайм، Эт-Тувайсир). «Сальва» لا «Салва» (مقالة «Сальва (Эль-Хаса)»)؛ «Эль-Уюн» لا «Эль-Аюн» (الأخيرة عبر الإسبانية/الفرنسية للعيون الصحراوية). سياج المرحلة 4 صفر حذف (سبعة مرشّحين بُرّئوا)؛ العربية غلبت الإنجليزية في الشبعان («نسبةً إلى البلدة المجاورة» ساقطة من الإنجليزية). القارئ الأعمى 32 ملاحظة: 12 مطبَّقة · 12 مرفوضة · 8 علل مصدر — أمسك تناقضاً ملاحياً في أبو حصيص (`area` «قرية التويثير» والمتن «شمال القرية») ودوران فقرة تسمية الشبعان (البلدة المجاورة لا تُسمّى في أي لغة). المعجم +21 = 155. 12 علّة مصدر لإياد أثقلها: أربع صفحات من خمس تختم بجملة أمانة الأحساء بلا تاريخ فتُقرأ بياناً صحفياً واحداً؛ لا صفحة تقول كيف يُوصَل أو هل يُصعَد. البناء أخضر. |
| ب.١٢ | **الدفعة الروسية السادسة للمعالم `ru-attr-6`**: بحيرتان وشاطئان ومنتزه بترتيب `order` (الأصفر · شاطئ العقير · سلوى · الحبيل · منتزه الملك عبدالله البيئي) — 26 حقلاً و3 بنود بطاقة موثّقة و**11 سؤالاً `q_ru/a_ru`** (نمط `ru-attr-1` الكامل)، وشارتان جديدتان «بحيرة» ⇐ «Озеро» و«منتزه» ⇐ «Парк» | أنا | ✅ **منجز بطلب دمج** (2026-09-07، فرع `feat/ru-attractions-6` فوق `feat/ru-attractions-5`): سبع مراحل بوكيل Opus مستقل لكلٍّ، الحاكم **90/100 · APPROVE من الدورة الأولى** (الصفحات 91/91/90/90/90؛ المحاور 28/26/19/7/10 — ملاءمة المسافر 7 لأن ثلاث صفحات فقيرة في مصدرها). **ستة أحكام عامة** ثُبّتت في `termbase._meta.batch_rulings["ru-attr-6"]` وتسري على ما بقي: (١) العدد في التقريب السردي بالكلمة إلى رقمين وبالأرقام من ثلاثة («примерно в 150 км» و«в двадцати километрах» معاً صحيحتان)؛ (٢) قالب سؤال الرسوم منقسم بحسب المكان: «Нужно ли платить за посещение X?» للموقع الطبيعي المفتوح و«Сколько стоит вход в X?» لما له مدخل؛ (٣) **مكوّنات اليونسكو مكوّناتُ الموقع لا الواحة**: «один из двенадцати компонентов объекта «Оазис Аль-Ахса»» — تمسّ المعالم الستة ذات `unesco`؛ (٤) قيمة البند الموثّق بلا نقطتين داخلها (التسمية تُصيَّر بنقطتين)؛ (٥) اسم الجنس يُعاد لرفع اللبس حين يحمل علمٌ واحد مسمَّيين (بلدة سلوى/شاطئ سلوى)؛ (٦) الشارة كلمة واحدة لا تُوسَّع. **الكرفان = «дома на колёсах»** لا «караваны» (الأخيرة قافلة جِمال، وصفحات العقير تتكلم على القوافل فعلاً)؛ **«охраняемая территория» لا «заповедник»** (الأخيرة محمية مغلقة بتصريح فتناقض «вход свободный»)؛ اسم الملك «король Абдалла» بسند ru.wikipedia وبنية «…короля Абдаллы» بأربع سوابق؛ الجافورة «пустыня Эль-Джафура» بالنمط (لا مقالة). سياج المرحلة 4 حذفان («Отсюда» و«сюда приезжают» في سلوى). القارئ الأعمى **57 ملاحظة: 12 مطبَّقة · 25 مرفوضة · 20 علّة مصدر** — أثقل المطبَّق تصحيح «компонентов оазиса» إلى «объекта»، و«точки слива» بدل «слива» (تُقرأ برقوقاً). المعجم +11 = 166 (كلها verified). **12 علّة مصدر لإياد** أثقلها: منتزه الملك عبدالله جملة واحدة مطبوعة مرتين بلا موقع ولا بيان؛ الأصفر «مكوّن من الواحة» و«شمال شرق الواحة» في آن؛ الحبيل بحيرة صرف زراعي تُعرض «بلا رسوم» بلا كلمة عن سلامة الماء؛ سلوى تثير الحدود مع قطر بلا جواب عن التصريح؛ «يُحجز إلكترونياً» في العقير بلا قناة؛ تفاوت الشارات (بحيرة/معلم طبيعي) والتسميات (الرسوم/الزيارة) لواقعة واحدة. البناء أخضر. |
| ب.١٣ | **الدفعة الروسية السابعة للمعالم `ru-attr-7`**: بحيرة موسمية وأربعة منتزهات بترتيب `order` (بحيرة الديلفوون · منتزه الأحساء الوطني · منتزه جواثا · حديقة عين النجم · المشقر) — 25 حقلاً و3 بنود بطاقة موثّقة و**7 أسئلة `q_ru/a_ru`**، وشارة جديدة «بحيرة موسمية» ⇐ «Сезонное озеро» | أنا | ✅ **منجز بطلب دمج** (2026-09-07، فرع `feat/ru-attractions-7` فوق `feat/ru-attractions-6`؛ **طلب الدمج #26 موجَّه إلى `main` ويوصل الدفعات 3–7 معاً**: السلسلة #21→#25 دُمجت في 07:19–07:21 بلا إعادة توجيه فوصل #21 وحده إلى main وبقيت الدفعات 3–6 في الفروع الوسيطة (الإنتاج 404 لصفحاتها) — أُعيد توجيه رأس السلسلة بعد `merge-tree` نظيف، والفحوص خضراء ومعاينة النشر تخدم صفحات الدفعتين 3 و7): سبع مراحل بوكيل Opus مستقل لكلٍّ، الحاكم **90/100 · APPROVE من الدورة الأولى** (الصفحات 90/90/91/90/90؛ المحاور 28/26/19/7/10 — ملاءمة المسافر 7 لأن ثلاث صفحات من خمس بلا أي بيان تشغيلي). **قرارا تسمية مسنَدان**: (١) **الديلفوون ⇐ «Эд-Дильфун»** من الرسم العربي «الديلفوون» (الأداة بالإدغام الشمسي + كسرة-ياء ⇐ и) لا من `title_en` الذي أسقط الأداة — وصُحّح مدخل المعجم القديم «озеро Дельфун» المبني على الإنجليزية (لا صفحة منشورة تحمله)؛ (٢) **أول گلوس لاتيني في الخط الروسي «(Delfoon)»** بحكم P4 (اسم يواجهه الزائر على خرائط قوقل، والسابقة «(Land of Civilisations)» في صفحة المعايرة). **ستة أحكام عامة** ثُبّتت في `termbase._meta.batch_rulings["ru-attr-7"]` (B7-1…B7-6) وتسري على ما بقي: تعارض المفرد/الجمع بين العنوان والمتن يُجسَر في المتن والجواب لا في العنوان («X — это несколько…»)؛ إحالة المتن إلى بطاقة الزيارة تُنقل بتسمية الواجهة حرفياً «Практическая информация» ولا تُحذف؛ الاسم الواحد لعدّة معالم (Джавата: مسجد · منتزه · موقع أثري · غابة) يميّزه اسم الجنس داخل الصفحة بلا إحالة عبر الصفحات؛ الشرطة المائلة في `title_en` تُنقل بترتيبها («Парк Эш-Шайбани / Эль-Мушаккар») والمتن يفصل السابق من الحالي؛ اسم علم روسي مستقرّ لا يُستورد حين ترجمه المصدر وصفياً عمداً («أيام العرب» تبقى «предания о знаменитых битвах арабов» رغم مقالة «Айям аль-Араб»)؛ أداة الاستدراك تُردّ إذا فُصلت الجملة («Впрочем»). أحكام أصغر: `Джавата` несклоняемое؛ «بلدة الكلابية» ⇐ «посёлок» بلا توحيد مع «деревня» في صفحة المسجد (التفاوت علّة مصدر)؛ «Национальный парк Аль-Ахсы» بالمضاف إليه (مدخل قائم)؛ التاريخ الهجري الثاني في الصفحة بالصيغة القصيرة «в 1367 г. х. (1948 г. н. э.)»؛ «около 4 500 га» بمسافة U+0020 و«двадцать километров» بالكلمة (B6-1). سياج المرحلة 4 حذف واحد («детских»). القارئ الأعمى **46 ملاحظة: 10 مطبَّقة · 25 مرفوضة · 11 علّة مصدر** — أثقل المطبَّق **«построенный по её подобию» ⇐ «в память о ней»** في ملخّص المشقر (دعوى مماثلة لحصن مجهول الموقع تسرّبت من `summary_en` وحدها والعربية تقول «استلهاماً» في الموضعين — علّة دقّة نجت من السياج)، و«удобства» (تُقرأ دورات مياه) ⇐ «места». المعجم +14 = 180 (3 verified · 11 pending بحكم مكتوب)، ذاكرة الترجمة 364. **27 علّة مصدر لإياد** أثقلها: **ثلاث صفحات من خمس بلا حقل `practical`** (الوطني · عين النجم · المشقر — وهي التي قال القارئ إنه يذهب إليها فعلاً مع العائلة)؛ عين النجم مبنيّة على ماء استشفائي بلا قول هل يبلغه الزائر اليوم، وعبدالعزيزان في سطرين متجاورين (1799 و1948) بلا تفريق؛ المشقر `summary_en` «built in its image» يناقض `body_en` «in homage» والعربية «استلهاماً» فيهما؛ ترتيب الاسمين المعكوس في `title_en`؛ منتزه جواثا: الملخّص «بجوار» المسجد والمتن «في نطاقه»، ورسم «10–15» بلا معيار ومسجد «مجاني» داخل منتزه برسم؛ الديلفوون: مفرد/جمع بين العنوان والمتن و`title_en` أسقط الأداة ولا شهر للأمطار؛ **أربعة كيانات باسم «جواثا»** بلا سطر يفرّق؛ وصيغة `area` «north of Al-Ahsa» تجعل الأحساء نقطةً (تتكرر من الدفعة الأولى). مرشَّح اصطلاح مستقل يمسّ /ru/ كلها: «н. э.» لتاريخ من القرن العشرين. البناء أخضر: 24 فحصاً، `C20` على 38 صفحة، `C21` على 198 سؤالاً. |
| ب.١٤ | **الدفعة الروسية الثامنة للمعالم `ru-attr-8`**: أربعة منتزهات ومتحف بترتيب `order` (منتزه جبل كنزان · حديقة الكوت · حديقة الموسى · حديقة الاستاد الرياضي · متحف الأحساء) — 24 حقلاً، **بلا بند بطاقة واحد ولا سؤال** لأن الخمس بلا حقل `practical` أصلاً | أنا | ✅ **منجز بطلب دمج** (2026-09-07، فرع `feat/ru-attractions-8` **من main** بعد دمج #26 الذي أوصل الدفعات 3–7): سبع مراحل بوكيل Opus مستقل لكلٍّ، الحاكم **90/100 · APPROVE من الدورة الأولى** (الصفحات: `koot-park` 91 والأربع الباقية 90؛ المحاور 28/26/19/**7**/10 — ملاءمة المسافر عند أرضية السلسلة لأنها **أول دفعة روسية بصفر بند موثّق**). **قرار العنوان الأثقل نُقض في المرحلة 4**: `kanzan-park` صار **«Парк горы Канзан (Эш-Шууба)»** لا «Парк Джабаль-Канзан» — لأن `title_en` ترجم جنسه («Park») ولا يحمل جنساً للجبل، فالجنس الذي يردّه سياج الوقائع من العربية («منتزه **جبل** كنزان») يدخل مترجَماً؛ والحجّة القاطعة أن `gloss_regel` تجعل ترتيب الگلوس محكوماً بالعنوان والمتن يتصدّر بـ«Канзан (по-арабски — Джабаль-Канзан)». وتبعه الاسم الرسمي في المتن ⇐ «Парк горы Эш-Шууба»، فنزلت الصفحة من أربع صيغ للجبل إلى اثنتين. **وتعارضا `title_en` مع العربية سُجِّلا علّتَي مصدر ولم يُصحَّحا**: «Adventure» زائدة و«جبل/Mountain» ساقطة. **عشرة أحكام عامة** ثُبّتت في `termbase._meta.batch_rulings["ru-attr-8"]` (B8-1…B8-10) وتسري على الـ17 الباقية: اسم الجنس داخل الاسم يُنقحر حيث ينقحره المصدر ويُترجم حيث يترجمه — والقاعدة واحدة في العنوان والمتن (B8-1)؛ **`hidzhra` تحكم صيغة التقويم لا أيَّهما يتصدّر، والتصدُّر يتبع المصدر** فبقي «в начале 1980-х годов (1404 год хиджры)» على ترتيب المصدر، وقلبُه كان سيختلق دقّةً (عقدٌ مقابل سنة) (B8-2)؛ اسم الجنس الذي توجبه `sklonenie` يُؤخذ من المدوّنة المنشورة لا يُختار (B8-3)؛ **الصياغة المنشورة في صفحة شقيقة لا تُستنسخ إلا بقدر ما يحمله مصدرُ الصفحة الحالية** — وهو أخطر تسرّب لأن الجملة المنقولة مصدَّقةٌ بدرجة سابقة فتبدو آمنة (B8-4)؛ الشرطة المائلة بين اسمين تُنقل شرطةً في العنوان و«(или …)» في النثر الجاري (B8-5)؛ **حرف الجرّ الذي يبني علاقة لا يُطوى** خصوصاً مع اسم مقتبَس غير مصرَّف (B8-6)؛ في سلسلة «от … через … к X» يُعاد حرف الجرّ مع الحلقة الأخيرة «и к Y» وإلا قُرئت بدلاً (B8-7)؛ الكلمة المتعدّدة المعنى تأخذ نعتها عند أول ورود (B8-8)؛ **«стоит» لا تُستعمل لإقامة مؤسسة في صفحة قد يُبحث فيها عن سعر** («стои́т»/«сто́ит» متجانستان خطّاً) فالمؤسسة «находится» (B8-9)؛ و**فعل الإقامة يحكمه شكلُ الظرف لا نوعُ المعلم**: اتجاهيّ ⇐ «лежит» وعنوانٌ مُعيَّن ⇐ «разбит» للحديقة (B8-10 — بعدٍّ على المدوّنة: «лежит» خمسٌ من خمس بإطار اتجاهيّ). سياج المرحلة 4 حذف واقعتين، أخطرهما **«В исторических сочинениях»** المنقولة من الصفحة الشقيقة `kanzan` المنشورة (مصدر هذه الصفحة يقول «historically» وحدها) — وهي التي وُلد منها B8-4. القارئ الأعمى **61 ملاحظة: 17 مطبَّقة · 17 مرفوضة · 27 علّة مصدر**؛ ورفض انقسام `Аль-`/`Эль-` الذي رآه «كلمات من لغتين» **مردودٌ بحكم `C20` المسنَد** (‏`Аль-Ахса` صيغة اليونسكو ورأس العلامة). **ومراجعة لاحقة (`07b`)** ردّت تعديلاً للحاكم عكس تعديلاً موثَّقاً للمرحلة 2 بلا ذكره: كلمة واحدة في `musa-park` («лежит» ⇐ «разбит») بعد عدٍّ على 58 ملفاً أثبت أن الشكل المرصوف «в районе X в Y» منشورٌ ثلاث مرات وأن الشاذّ هو الفعل لا حرفا الجرّ. **ثلاثة تصحيحات توثيقية في المعجم** كشفتها المرحلتان 5 و7: مثال `sklonenie` في مدخل `Каср-Ибрахим` كان يعلّم الحشو الذي يحرّمه `note` في المدخل نفسه («во дворце» ⇐ «в крепости»، والمنشور ستة مواضع من ستة بـ«крепость»)؛ وحقل `rod` في مدخل `Эль-Ку` بميم عربية U+0645 بدل «м» السيريلية (فخّ الدفعة السابعة، لم يُغلق — وهو صنف الإخفاق الصامت نفسه الذي يُحرَس منه U+00A0)؛ وسطر تصحيح في المدخل نفسه طرفاه متطابقان فلا يقول شيئاً. **الصيغة النهائية «Эль-Ку» لم تُمسّ** (حكم العين الأخيرة). المعجم +17 = **197 مدخلاً** (‏6 verified بجلبٍ مقروء: `Эль-Хубар` · `Дильмун` · `Герра` المرفوعة من pending بشاهدَين · `район Эль-Фейсал` · `Второе Саудовское государство`)، وذاكرة الترجمة **376 زوجاً**. **وفحص كيان Wikidata `Q20400438` بالجلب المباشر جاء سلبياً** (لا تسمية `ru` ولا `ruwiki`) فبقي عنوان المتحف محكوماً بالقاعدة الداخلية «Музей Аль-Ахсы» بالمضاف إليه على سابقة «Национальный парк Аль-Ахсы». **28 علّة مصدر لإياد** أثقلها: **صفر بند موثّق في خمس صفحات من خمس** — و`ahsa-museum` متحفٌ حكوميّ رسميّ بلا ساعة ولا رسم ولا يوم إغلاق بينما متنُه يبني نفسه في خطة يوم الزائر («نقطة انطلاق مثالية قبل جولتك»)، فالصفحة تعطي نصيحةً وتحجب وسيلة تنفيذها؛ و`stadium-park` **صفحة بجملة واحدة مطبوعة مرتين وبلا حقل `area` أصلاً** (أفقر صفحة في الموقع، ونمطٌ معلوم بسابقة `king-abdullah-park`)؛ وشارة `ahsa-museum` «معلم تاريخي» تناقض متنَها (المعلم التاريخي ما يعرضه المتحف لا المتحف المفتتَح في الثمانينيات) — **تصيب اللغات الخمس**؛ وبطاقة `kanzan-park` تَعِد بشلالات بلا صفة والمتن يقول إنها **صناعية** — الخمس لغات كذلك؛ و`summary_en` في `musa-park` ينسب الحديقة **للمستشفى وحده** بينما العربية و`body_en` يقولان إنها شراكة (علّة دقّة تسرّبت إلى الملخص وحده، نظير «по её подобию» في المشقر). وثغرة جانبية **سابقة لهذه الدفعة**: `king-abdullah-park` بلا بصمة محتوى روسية لأن متنه أقصر من عتبة الستين محرفاً في `scripts/fingerprint.mjs` — أثرٌ مباشر لعلّة المصدر نفسها المسجَّلة في ب.١٢. البناء أخضر: **24 فحصاً**، `C20` على **43 صفحة**، البصمات **270** (ru 40). |
| ب.١٥ | **الدفعة الروسية التاسعة للمعالم `ru-attr-9`**: خمسة متاحف خاصة بترتيب `order` (متحف البيت الأحسائي · متحف الخليفة التراثي · متحف عبد الرزاق العرب · دار التراث · متحف صالح الظفر) — 25 حقلاً بلا بنود بطاقة ولا أسئلة، وشارة موحّدة «Частный музей» | أنا | ✅ **منجز بطلب دمج** (2026-09-07، فرع `feat/ru-attractions-9` من main بعد دمج #27، طلب الدمج #28 وكل فحوصه خضراء): سبع مراحل بوكيل Opus مستقل لكلٍّ، الحاكم **90/100 · APPROVE من الدورة الأولى** (الصفحات 90/90/90/90/91؛ المحاور 28/26/19/7/10) — على حافة العتبة لا فوقها، كالدفعات السبع قبلها. **القارئ الأعمى أعطى 76 ملاحظة — أكثر دفعةٍ حتى الآن — بُتّت كلها: 21 مطبَّقة في 22 استبدالاً · 21 مرفوضة بسند · 34 علّة مصدر.** وأثقل المطبَّق إجرائياً «документированная специализация»: **رفعتها المرحلة 2 صراحةً ولم تُبتّ في 3 و4 و5 حتى أمسكها القارئ استقلالاً** — وهو صنف الإخفاق الذي يعدّه هذا المشروع الأسوأ (فحصٌ يمرّ على ما كان عليه أن يمسكه). أربعة أحكام عامة تسري على ما بقي: **`B9-1`** الإطار الاتجاهيّ شرطٌ لازم لـ«лежит» لا موجِب (المدوّنة تكذّب الإيجاب أربع مرات) والمرخِّص امتدادُ الموضوع فالمؤسسة «находится» أياً كان الظرف — **بيانٌ لمدى `B8-10` لا نقضٌ له**؛ **`B9-2`** «Hasawi X» بثلاثة فروع؛ **`B9-8`** `B8-4` سياجُ وقائع لا قاعدةَ تنويع لفظيّ؛ **`B9-9`** «известный как» يصحّ في المتن وحده لأن الملخّص يُصدَّر إلى البطاقات. المعجم **197 ← 218** (‏6 محقَّقة و15 pending) والذاكرة **376 ← 389**، ووُوئم مثال «Эль-Курайн» مع المنشور («между посёлками» لا «деревнями») بحكم `B9-4` والعربية الفيصل. البناء أخضر: 24 فحصاً، و`C20` صار يغطّي **48 صفحة**. **وأثقل ما يُرفع لإياد أن الفجوة التشغيلية بوّابيّة لا كماليّة**: صفر بند موثّق في خمس صفحات **للمرة الثانية على التوالي**، وأربعة من الخمسة داخل بيوت أصحابها — والقارئ **لم يبلغ ولا واحدة** وختم بأنه لن يضع في مساره حتى المتحفين اللذين أرادهما. |

### طلبات مؤجَّلة لإياد (تُطلب كلٌّ في دورها، لا دفعةً واحدة)

بيانات المعالم العشرين · المزارع المفتوحة للزوار · حسابات التواصل · ~~`GOOGLE_PLACES_API_KEY` على Netlify~~ (مؤكَّد موجوداً 2026-09-04 — بنطاق يشمل المعاينات؛ **بعد دمج #10** يُطلب حصره في نطاق Production وحده، انظر ب.٢) · اعتماد نصّ صفحة «من نحن».
