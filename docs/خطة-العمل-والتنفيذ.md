# خطة العمل — التنفيذ وحلقة التحقق

مرجع الفجوات: [خطة-تنفيذ-دراسة-التطوير.md](./خطة-تنفيذ-دراسة-التطوير.md).
هذا الملف هو **سجل التنفيذ**: ما نُفِّذ، وبأي دليل تحقق، وما التالي.

## قاعدة العمل

1. **ما تنفيذه بيدي أبدأ به فوراً** — لا انتظار.
2. **طلب واحد من إياد في كل مرة**، وحين يُجاب يأتي الذي يليه.
3. **حلقة تحقق مستقلة لكل مخرج**: لا يُعدّ البند منجزاً حتى يمرّ بفحص لا يعتمد على الكود الذي كتبه.

### شكل حلقة التحقق

| الطبقة | ماذا تُثبت | الأداة |
|---|---|---|
| 1. أنواع | لا خطأ نوع | `astro check` (صفر أخطاء شرط) |
| 2. بناء | الصفحات تُبنى فعلاً | `astro build` |
| 3. **المخرج المبنيّ** | الزائر يرى الصحيح | `tools/check-consistency.mjs` على `dist/` |
| 4. متصفح | العنصر ظاهر لا مخفيّ | تأكيد DOM حيّ على خادم المعاينة |

الطبقة الثالثة هي الأهم: تفحص `dist/` لا المصدر — فحص المصدر يثبت أن الكود صحيح، وفحص `dist` يثبت أن **المنشور** صحيح.

---

## المرحلة صفر: التوحيد — ✅ منجزة (2026-08-30)

### 0.1 اشتقاق عدد المعالم بدل تثبيته

**كان:** `HomeView.astro` يكتب `'60+'` نصاً حرفياً، والخريطة تحسب 58، وllms.txt يقول 58.
**صار:** `String(allEntries.length)` مشتقاً من المجموعة.

**التحقق المستقل:**
- `C3` في فاحص التضارب ينزع الوسوم من `dist/index.html` ويطابق الرقم المرئي بعدد ملفات المجموعة → `✓ الرئيسية تعرض 58 معلماً`.
- تأكيد DOM حيّ: `.fact-n` الثالث نصّه `58`، `opacity: 1`، `visibility: visible`.

### 0.2 توليد llms.txt وقت البناء

**كان:** `public/llms.txt` ملفاً ثابتاً فيه عددان بخط اليد (58 معلماً، 20 منشأة) يتقادمان بصمت.
**صار:** `src/pages/llms.txt.ts` يشتقّ الأعداد من مجموعة المعالم و`dining.ts` و`events.ts` والمدونة.
**زيادة:** أُضيف قسم المدونة (7 مقالات) وذكر النسخة الصينية (16 معلماً) — كلاهما كان غائباً عن الملف رغم وجوده على الموقع.

**التحقق المستقل:**
- `C2` يستخرج العدد من `dist/llms.txt` ويطابقه بعدد الملفات → `✓ llms.txt يعلن 58 معلماً`.
- فحص ترميز فعلي على الإنتاج: `curl -I` أثبت أن Netlify يخدم `charset=UTF-8` (والمحارف العربية سليمة)، والتشويه ظهر في `astro preview` وحده. ثُبِّت الرأس صراحةً في `public/_headers` رفعاً للاعتماد على السلوك الافتراضي.

### 0.3 فاحص التضارب في خط البناء

**المخرج:** `tools/check-consistency.mjs`، مربوط في `postbuild` فلا يمكن تخطّيه، ومتاح مستقلاً بـ`npm run check:consistency`.
ثماني مجموعات فحص على `dist/`:

| # | الفحص | النتيجة |
|---|---|---|
| C1 | صفحات المعالم المبنية = ملفات المجموعة | ✓ 58 |
| C2 | العدد المعلن في llms.txt = الحقيقة | ✓ 58 |
| C3 | العدد المعروض في الرئيسية = الحقيقة | ✓ 58 |
| C4 | لا أرقام عربية-هندية في أي صفحة | ✓ 200 صفحة |
| C5 | المصطلحات المرفوضة توثيقياً | ✓ |
| C5b | كل اسم شائع مقرون باسمه الموثّق | ✓ |
| C6 | البنود غير الموثّقة محجوبة عن بطاقة الزيارة | ✓ 58 صفحة |
| C7 | sitemap غير فارغ | ✓ 190 رابطاً |
| C8 | lastmod تواريخ حقيقية لا ملفَّقة | ✓ 190 |

**درجتا خطورة:** `fail` يُفشل البناء، و`warn` يُبلِّغ ولا يعطّل النشر — للبنود الموقوفة على قرار.

**تصحيحان ذاتيان أثناء البناء** (الفاحص أخفق أولاً وكان محقّاً في مرة وخاطئاً في مرة):
- C3 كان يفشل بسبب `<bdi>` متداخلة داخل `<span>` — أُعيدت صياغته على النصّ المجرَّد من الوسوم.
- C6 كان يبحث عن نصّ القيمة غير الموثّقة في الصفحات، وكل القيم عبارات نائبة («بانتظار التأكيد») ترد مشروعةً في نبذات أخرى — فأعطى إنذاراً كاذباً في 54 صفحة. أُعيدت صياغته ليقارن **عدد صفوف `<dt>`** في بطاقة الزيارة بعدد البنود الموثّقة في المصدر.

### حالة البناء بعد المرحلة

```
astro check        →  0 errors (102 ملفاً)
astro build        →  200 صفحة
check-consistency  →  10 فحوص ناجحة، صفر تحذيرات
```

### 0.4 حسم «الخبز الأحمر» — ✅ منجزة (2026-08-30)

**قرار إياد:** «الخبز الأحمر» و«خبز التمر» اسمان لطبقٍ واحد — فلا طبق تاسع يُضاف.

**ما نُفِّذ:**
- `dishes.ts`: أُثبت الاسم مرادفاً على بطاقة خبز التمر باللغتين («ويُسمّى أيضاً الخبز الأحمر» / «Also known as red bread (khubz ahmar)») مع تعليق إسناد في ترويسة الملف.
- `PlanTripView.astro`: قُرن الاسمان في المواضع الأربعة — «خبز التمر (الخبز الأحمر)» و«date bread (also called red bread)» — فصار كل ذكر للاسم الشائع موصولاً بالطبق الموثّق في صفحة الأكلات.

**التحقق المستقل:**
- الفحص انقلب من قائمة حظر إلى **قاعدة اقتران** (`C5b`): ورود الاسم الشائع في أي صفحة يلزمه ورود الاسم الموثّق في الصفحة نفسها، وإلا فشل البناء. هذا يمنع عودة الانفصال مستقبلاً بدل مجرد رصده مرة.
- تحقق على `dist`: بطاقة خبز التمر تحمل المرادف باللغتين، وصفحتا «خطط لرحلتك» العربية والإنجليزية تقرنان الاسمين في كل ورود.
- **الفاحص صار 10 فحوص كلها خضراء بصفر تحذيرات.**

---

## المرحلة الأولى: الفهرسة والقياس

### 1.2 `lastmod` في sitemap — ✅ منجزة (2026-08-30)

**كان:** صفر ورود لـ`lastmod` في `sitemap.xml` — لا إشارة حداثة لأي محرك.
**صار:** 190 رابطاً من 190 يحمل تاريخاً حقيقياً.

**قاعدة المصدر — تاريخ حقيقي أو لا تاريخ:**

| نوع الصفحة | مصدر التاريخ |
|---|---|
| مقال مدونة | `updatedDate ?? pubDate` من ترويسة المقال — إعلان الكاتب أوثق من تاريخ الالتزام |
| صفحة معلم | آخر التزام git مسّ ملف المعلم |
| صفحة قائمة | أحدث تاريخ بين قالبها وملفات محتواها (الرئيسية = القالب + القاموس + كل المعالم) |
| صفحة فعالية | آخر التزام مسّ `events.ts` أو قالب التفاصيل |

المخرج الجديد `src/lib/git-dates.ts`: استدعاء واحد لـ`git log --name-only` لكل بناء يبني خريطة (ملف → تاريخ)، وتُخزَّن في الذاكرة.

**الحارس ضد التلفيق:** الاستنساخ الضحل (shallow clone) يجعل `git log` يعيد تاريخاً موحَّداً لكل الملفات — رقماً كاذباً. الحقل يُرفع كلياً في تلك الحالة، لأن غياب `lastmod` أصدق من تاريخ موحَّد لا يعني شيئاً.

**التحقق المستقل:**
- `C8` في الفاحص: صيغة `YYYY-MM-DD`، لا تاريخ مستقبلي، ولا تاريخ واحد يعمّ كل الروابط → `✓ 190 lastmod، 8 تواريخ مميزة، أحدثها 2026-08-29`.
- تحليل XML مستقل بمُحلِّل خارجي: **XML صالح**، 190 عنصر `<url>`، ترتيب العناصر مطابق لمواصفة sitemaps.org (`loc` ثم `lastmod`) في 190 من 190، صفر تاريخ مستقبلي، والتوزيع طبيعي عبر ثمانية تواريخ لا كتلة واحدة.

**فخّ مكتشف أثناء التنفيذ:** Vite يخزّن وحدة `sitemap.xml.ts` مؤقتاً، فبناءٌ بعد تعديلها أعطى 14 تاريخاً فقط من 190 — والنتيجة الصحيحة ظهرت بعد إبطال الكاش. **عند تعديل أي endpoint وقياس مخرجه: احذف `dist` وأعد البناء، ولا تثق ببناء تفاضلي.**

### 1.1 ربط Bing Webmaster Tools — 🔜 عند إياد

**قرار إياد:** الاستيراد من Google Search Console — لا وسم ولا سجل DNS، فلا عمل برمجي.

**الخطوات عندك:** `bing.com/webmasters` ← Import from Google Search Console ← تسجيل الدخول بحساب قوقل ← الموافقة. ينتقل النطاق والـsitemap تلقائياً.
بعدها أخبرني فأتحقق أن Bing قرأ `sitemap.xml` بـ190 رابطاً وأن IndexNow يصل.

### 1.3 IndexNow عبر Netlify — ✅ منجزة (2026-08-30)

**التصحيح المعماري:** الدراسة اقترحت GitHub Action، وهو **ممتنع** — رمز OAuth بلا نطاق `workflow` يُعطِّل أي دفعة تلمس `.github/workflows/` (قرار 2026-08-16). البديل المنفَّذ: إضافة Netlify محلية تعمل عند `onSuccess`.

**المخرجات:**
- `netlify/plugins/indexnow/submit.mjs` — منطق الاختيار والإرسال، قابل للتشغيل مستقلاً بـ`--dry-run`.
- `netlify/plugins/indexnow/index.mjs` + `manifest.yml` — خطّاف `onSuccess`.
- `public/bfa79848415477bf3b25134e4b45f0a3.txt` — ملف المفتاح.
- `[[plugins]]` في `netlify.toml`.

**اختيار الروابط — لا دفع أعمى:** يُقرأ `lastmod` من sitemap المبنيّ (وهو مشتقّ من تواريخ git الحقيقية)، وتُدفع الروابط التي تغيّرت منذ آخر نشر ناجح عبر `CACHED_COMMIT_REF`. وبلا مرجع سابق يُقتصر على روابط اليوم — أضيق احتمال وأسلمه. سقف 10000 رابط لكل طلب.

**عدم إسقاط النشر:** كل المسار داخل `try/catch` يعيد سطر تقرير لا استثناءً — فشل الفهرسة لا يُفشل نشراً ناجحاً.

**التحقق المستقل — ثلاثة مسارات دخول:**

| المسار | النتيجة |
|---|---|
| CLI بلا مرجع نشر | `لا رابط تغيّر تاريخ اليوم (2026-08-30) من أصل 190 — لا إشعار` ✓ |
| CLI بمرجع نشر حقيقي (`HEAD~5`) | `98 رابطاً من 190 منذ 2026-08-29` ✓ |
| محاكاة `onSuccess` بواجهة Netlify | نفس النتيجة + عرض `status` في ملخّص النشر ✓ |
| `onSuccess` بلا `utils` وبلا `PUBLISH_DIR` | نجا بلا انهيار ✓ |

**عطبٌ حقيقي أمسكته الحلقة:** حارس «التشغيل المباشر» كان يقرأ `process.argv[1].replace(...)` — و`argv[1]` غير معرَّف حين تستورد الوحدةَ إضافةُ Netlify، فينهار **الاستيراد نفسه** ويسقط الخطّاف كله. لولا محاكاة `onSuccess` لظهر العطب أول نشرٍ على الإنتاج. أُبدل بحارسٍ يحتمل الغياب.

**فحص دائم:** `C9` يقارن اسم ملف المفتاح المنشور بمحتواه وبثابت `KEY` في الإضافة — انفصال أيٍّ منها يجعل كل إشعار يُرفض بصمت، وصار الآن يُفشل البناء.

**ما سيحدث عند أول نشر:** الإضافة تُشعر Bing وYandex وSeznam وNaver بالروابط المتغيّرة. للتعطيل: احذف قسم `[[plugins]]` من `netlify.toml`، أو اضبط `INDEXNOW_DRY_RUN=1` في متغيرات بيئة Netlify.

---

## الطابور — بالترتيب

| # | البند | المنفِّذ | الحالة |
|---|---|---|---|
| 0.4 | حسم «الخبز الأحمر» | إياد ثم أنا | ✅ منجزة |
| 1.1 | ربط Bing Webmaster Tools | **إياد** | 🔜 استيراد من GSC — خطوة يدوية عنده |
| 1.2 | `lastmod` في sitemap | أنا | ✅ منجزة |
| 1.3 | IndexNow عبر Netlify (لا GitHub Action) | أنا | ✅ منجزة |
| 1.4 | تصنيف إحالات الذكاء الاصطناعي في GA4 + توسيع `bot-log` | أنا | ▶ التالي في يدي |
| 2.1 | توسيع نموذج `practical` بـ`source`/`sourceUrl`/`verifiedAt`/`nextReview` | أنا | جاهز للتنفيذ |
| 2.2 | بيانات مواعيد ورسوم 20 معلماً | **إياد** | ينتظر دوره |
| 3.1 | `sameAs` + Wikidata + اليونسكو | أنا (يُراجعها إياد) | ينتظر دوره |
| 3.2 | `faq` لكل معلم + تصدير `FAQPage` | أنا | ينتظر دوره |
| 3.3 | `openingHoursSpecification` + `offers` | أنا (بعد 2.2) | ينتظر دوره |
| 4.1 | حقل `answer` (فقرة Answer-First) لأهم 15 صفحة | أنا | ينتظر دوره |
| 4.2 | ثلاثة جداول HTML | أنا | ينتظر دوره |
| 5.1 | صفحة «من نحن والسياسة التحريرية» | أنا (يعتمدها إياد) | ينتظر دوره |
| 5.2 | صفحة اليونسكو الشاملة | أنا | ينتظر دوره |
| 5.3 | صفحة الفرق بين الأحساء والهفوف والمبرز | أنا | ينتظر دوره |
| 5.4 | صفحة العائلات والأطفال | أنا | ينتظر دوره |

### طلبات مؤجَّلة لإياد (تُطلب كلٌّ في دورها، لا دفعةً واحدة)

بيانات المعالم العشرين · المزارع المفتوحة للزوار · حسابات التواصل · `GOOGLE_PLACES_API_KEY` على Netlify · اعتماد نصّ صفحة «من نحن».
