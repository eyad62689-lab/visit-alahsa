# أمرُ بدء المرحلة 6 — انسخه إلى محادثةٍ جديدة

> أكمل في مستودع `visit-alahsa`، الفرع `claude/alahsa-site-audit-plan-u7kp5v`.
>
> اقرأ `docs/جدول-إكمال-الترجمة-2026-09-12.md`، ونفّذ **المرحلة رقم 6** منه وحدها — لا تتقدّمها ولا تتجاوزها. واقرأ قبلها مخرجاتِ المرحلة 5 في `zh-translation/output/batch-zh-21/` (‏`brief.md` و`judge-notes.md` و`source_defects.md` والوثائقَ المقيسةَ الثلاثَ **بذيول تصويبها**) — فيها ما لا يُعاد اكتشافه.
>
> **النطاق مقيسٌ سلفاً: سبعةُ ملفات، حقلُ `body_zh` وحدَه** (الدفعة `batch-zh-22`)، وكلُّها في `src/content/dining/`:
>
> | الملف | كلماتُ `body_en` |
> |---|---|
> | `sharq-alqaisariah` | 128 |
> | `ratio-alkoot` | 125 |
> | `alhawi` | 124 |
> | `baithana` | 123 |
> | `7st` | 121 |
> | `baking-up` | 120 |
> | `soulaf` | 119 |
> | **المجموع** | **860** |
>
> وتبقى بعدها ستٌّ للمرحلة 7 (‏`dot-bakery` · `lava` · `ammo` · `koud` · `greasy-fingers` · `karak-raslan`، 657 كلمة).
>
> **‏`blurb_zh` و`area_zh` و`alt_zh` مترجَمةٌ ومعتمدةٌ سلفاً — لا تُمَسّ.** و`name` لا يُترجَم بقاعدة الموقع.
>
> التزم قواعدَ القسم 3 كلَّها: خطُّ `zh-translation-pipeline` بمراحله الستِّ **ومرحلةِ 6.5 القارئ الأعمى قبل الحاكم**، وعتبةِ 90، ودرجةِ الدفعة أدنى صفحاتها؛ ولا معلومةَ بلا مصدر؛ وأرقامٌ لاتينية؛ والإنجليزيةُ مصدرُ الترجمة **والعربيةُ الفيصلُ عند أيِّ اختلافِ واقعة**.
>
> **وستّةُ أشياءَ استقرّت في المرحلة 5 فلا تُعاد من الصفر:**
>
> 1. **مطابقةُ المعجم**: لا يُعَدّ اسمٌ حاضراً إلا برأسٍ **يساويه** — لا يحويه ولا يذكره — **ومع معايرةِ التهجئة**، و**الأكثرُ تقاسُماً يُفحَص أولاً**، **وعنوانُ الصفحة نفسِه أولَ ما يُفحَص**. أربعُ دفعاتٍ أخطأت هنا بأربع صورٍ مختلفة.
> 2. **‏`ZH-W2` تحكم أسماءَ الأطباق**: لا نقحرةَ مسكوكةً بلا شاهدٍ منشور. والمرحلة 5 حسمتها: **الاسمُ اللاتينيُّ عارياً** (‏`marqooq` · `balilah` · `bajilla` · `kleija` · `zalabia`)، والوصفُ الذي يذكر مكوّناً لا يقوله المصدر **يسقط بالسياج**. وهذه الدفعةُ فيها أطباقٌ كثيرة (‏`7st` · `baithana` · `soulaf` · `baking-up`) فالحكمُ نفسُه ينطبق.
> 3. **التحقّقُ الويبيُّ محجوب** (‏`zh.wikipedia` و`unesco/zh` و`visitsaudi/zh` تردّ `000` — مقيسٌ 2026-09-13). فالمداخلُ بسندٍ داخليٍّ **بالملفّ والحقل**، والتحقّقُ الخارجيُّ **مؤجَّلٌ ومُعلَنٌ داخل كلِّ مدخل**.
> 4. **الناشرُ يعرف المنشآت**: `node tools/translation/apply-attraction-batch.mjs zh batch-zh-22 judge.json [--dry]`، و**مفاتيحُ `fields.final.json` مسبوقةٌ بمجموعتها** (`dining:7st`)، **و`body_zh` هو الحقلُ الوحيدُ المسموح** — وما عداه يُرفض. جرّبه جافّاً قبل النشر.
> 5. **‏C11 له ذراعٌ صينية** (لا نسخةَ نبذة، ولا أقصرَ منها، ولا متنَ صينيّ بلا `body_en`)، وسيعدّ **13** بعد هذه المرحلة.
> 6. **العرضُ ليس من عملك**: `/zh/restaurants-cafes/[slug]/` غيرُ مبنيّ — يُبنى في **المرحلة 16**. فما تكتبه مخزَّنٌ محروسٌ ينتظر مساره.
>
> **وواقعةٌ حسمها المالك 2026-09-13 تخصّ ملفَّين من دفعتك**: «‏`alsayed` داخل سوق القيصرية» — وصفحةُ السوق نفسُها (`src/content/attractions/qaisariyah.md`) تضعه **في حي الرفعة**. فبطاقتا `sharq-alqaisariah` و`ratio-alkoot` لا تُمَسّان، و`Ratio Al-Koot` مدخلٌ ملزمٌ **قيمتُه اللاتينيةُ نفسُها**.
>
> واختم بالبناء الكامل (`npm run build`) والاختبارات الأربعة منقولةً **لكلِّ أداةٍ على حدة** (‏`PUPPETEER_DIR=/tmp/pptr CHROME=/opt/pw-browsers/chromium-1194/chrome-linux/chrome BASE=http://localhost:4399` بعد `npx astro preview --port 4399`).
>
> ثم حدّث الجدول: علّم المرحلة 6 منجزةً بدرجتها، وأعِد قياسَ ما تغيّر، وأبلغني بما بقي — **وبما نُقض من قياساتك أنت**.
>
> **وذكّرني بـ`POL-ZH-21`** (أسماءُ الأطباق اللاتينيةُ العارية: القاعدةُ تشتري الدقّةَ بثمنٍ في القراءة، والحلُّ مصدرٌ يصفها) **وبـ`SD16`** (‏`StayView.astro` يخالف مدخلَين ملزمَين — خارج نطاق الدفعات).
