node_modules/
dist/
.astro/
.claude/settings.local.json

# بيانات خرائط قوقل الحية — تُجلب في كل بناء ولا تُخزَّن في المستودع (شروط قوقل)
src/data/places-live.json

# متغيرات البيئة — القيم الحقيقية لا تدخل المستودع أبداً.
# `.env.example` وحده متعقَّب (نموذج بقيم وهمية) بالاستثناء الصريح أدناه.
.env
.env.*
!.env.example
