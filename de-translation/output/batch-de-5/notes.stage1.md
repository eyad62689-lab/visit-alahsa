# المرحلة 1 · `batch-de-5`

52 حقلاً · 8 مفاتيح (19·5·5·5·5·5·4·4، مقيسةً آلياً) · 31 مدخل معجم. تعليلُ كلِّ مصطلحٍ في ملف المعجم.

## أ. المعتمد المُعاد حرفاً
زوجُ ذاكرةٍ يطابق آخر جملةٍ في `jawatha-park.body_en` ⇒ `Öffnungszeiten und Eintritt stehen weiter unten bei „Besuchsinformationen“` (tm، `qaisariyah`)؛ ومعه `Genehmigter Entwurf …` · `Öffnungszeiten`/`Eintritt` · `abgehalten wurde` · `den Treueid auf … leisten` · `unter der Aufsicht …`.

## ب. `source_defects_for_owner`
1. **`musa-park.summary_en` يخالف متنَه والعربية** (العهدة 3): ينفرد بإسناد البناء لمستشفى الموسى وحده، و`body_en` والعربيتان تقولان شراكةَ جهتين ضمن «نلعب معاً»، والعربيةُ وحدها تحمل حيّ الفيصل ⇒ الألمانيةُ على العربية. **العلّة في الإنجليزية المنشورة.**
2. **صيغةُ الساعة تتفاوت في الألمانية المنشورة**: `qaisariyah` يكتب `8:00–12:00 … Uhr` و`jawatha-mosque` يكتب `8–24 Uhr` لساعاتِ **المنتزه نفسِه** التي تكتبها هذه الدفعة `Täglich 8:00–24:00 Uhr` (صيغةُ المعجم + شرطُ C21). **الشاذُّ `jawatha-mosque.value_de`**، ولا حارسَ يمسك تفاوتَ صفحتين.
3. **الكلابية «بلدة» هنا و«قرية» في `jawatha-mosque`** (ومنه `Dorf Al-Kilabiyah` المنشور) ⇒ `die Ortschaft Al-Kilabiyah` تغطّيهما بلا نقضِ المنشور.
4. **تضاربٌ داخل الإنجليزية**: «Al-Faisal district»/«the Faisal district» ⇒ `Al-Faisal-` (سابقة `Al-Uqair`)؛ و`Al-Moosa Park`/`Almoosa Specialist Hospital` ⇒ كلٌّ كما تسمّي نفسَها.
5. **`najm-park`: «the most famous» في النبذة و«one of …» في المتن** (والعربيةُ تطابقهما حقلاً بحقل) ⇒ كلُّ حقلٍ على نصّه.
6. **العربيةُ الفيصل ثلاثاً**: «wellness» ⇒ محذوفة · «some 2,200» ⇒ «تزيد على 2200» = `mehr als 2.200` · «shaded seating» ⇒ «مظلات» = `Schattendächer` · «open lawns» ⇒ «مساحات خضراء» = `offene Grünflächen`.
7. **«at the city of Al-Omran»**: الإنجليزيةُ هنا **تحمل** «مدينة» خلافاً لـ`ardh-alhadarat` التي بُني عليها مدخلُ المعجم، وتعليلُ المدخل مشروطٌ بالإنجليزية ⇒ `bei der Stadt Al-Omran`. **نطاقٌ للمرحلة 4.**

## ج. قرارات ذات وجهين
1. **العنوان بـ`titel_regel` وحدها**: الإنجليزيةُ ترجمت اسمَ الجنس في الثمانية ⇒ Durchkopplung في الثمانية. و`Al-Shaibani-/Al-Mushaqar-Park` بشرطةِ التكملة قبل المائلة (قاعدةُ دودن)، وبدونها يبقى الاسمُ الأول بلا اسمِ جنس. و`Sportstadion-Park` لا علمَ فيه فالشرطةُ للقراءة — بديلُه `Park am Sportstadion`.
2. **صنفٌ نظاميٌّ ألمانيٌّ ممنوعٌ بلا مصدر** — سابقةُ `denkmalgeschützt` تمتدّ إلى أربعة: `Nationalpark` مرّةً في العنوان وحده والمتنُ خالٍ من `Schutzgebiet`/`Naturschutzgebiet`/`Naturpark`/`unter Schutz` (العهدة 4؛ والإشرافُ واقعةٌ) · `Umweltpark` لـ`king-abdullah-park` لأنها وحدَها ليست صنفاً نظامياً · `Heilquelle`/`Kurort` ممنوعةٌ ⇒ `zur Heilung aufgesucht` (المصدرُ يقول لماذا قُصدت لا أن الأثر مثبَت) · `barrierefrei` ممنوعةٌ (DIN 18040) ⇒ `Bedürfnisse … berücksichtigt`.
3. **`Markt` للسوق الجاهلي و`Souk` للسوق الحيّ** في `mushaqar` — لو اتّحدا لقُرئا سوقاً واحداً مستمراً.
4. **الگلوس**: `Souk (traditioneller Markt)` و`Ayyam al-Arab (die … Schlachttage der Araber)` (مصطلحان ⇒ گلوسان، سابقة `dibs`) · `Ain Najm (die Najm-Quelle)` بترتيبِ العنوان كما يوجب `gloss_regel`. **ولم يُگلَس `Qaisariyah-Souk`** اتّباعاً لـ`baiah`.
5. **التقويم** (العهدة 5): `najm-park` وحدَها يتكرّر فيها الهجريّ ⇒ `1115 nach der Hidschra (n. H.; 1703 n. Chr.)` ثم `1367 n. H. (1948 n. Chr.)` — **بفاصلةٍ منقوطة داخل القوس كنمطِ الخمسِ المنشورة**، لا بقوسين متتاليين كما اقترح الموجز. و`national-park` مرّةً ⇒ `1382 nach der Hidschra (1962 n. Chr.)` (سابقة `khuzam`)؛ و`1799` ميلاديٌّ وحده.
6. **الأرقام والعملة** (6 و1): `2.200` · `4.500` بنقطة الآلاف، و`zwanzig Kilometer` · `mehr als zwölf` · `acht Gärten` · `zwei Millionen` بالحروف. وأولُ ذكرٍ للعملة هو البطاقة ⇒ `10–15 Saudi-Riyal (SAR)` ثم السؤال `10–15 SAR` — **محاكاةُ C21: PASS**.
7. **`kommunal` لا `städtisch`**: المعجمُ يرفض `Stadtverwaltung` كمسألةِ واقعة، و`städtisch` تعيد معادلةَ «الأحساء = مدينة». و`architektonische Motive aus Al-Ahsa` لا `Hasawi-`.
8. **أسماءُ المؤسسات**: الوصفُ يُترجَم حيث ترجمته الإنجليزية (`Al-Muhaidib für den Dienst an der Gemeinschaft`)، والاسمُ اللاتينيُّ الذاتيُّ يبقى (`Almoosa Specialist Hospital`)؛ وجمعُهما ممنوع (`no_pleonasm`).
9. **الحياد في `mushaqar`** (العهدة 7): `… ist in der Forschung bis heute umstritten: Al-Battaliyah, Jawatha oder Al-Qarah.` — لا ترجيحَ ولا إسقاط؛ و«الجاهلي» ⇒ `vorislamisch`. و`„Mushaqar-Palast“` بأقواسٍ كالمصدرين، ولصقه `ein moderner Bau, errichtet in Anlehnung an jene alte Festung` وإلا نشأت دعوى قِدَم.
10. **الرقيقتان** (العهدة 2): `summary_de == body_de` بايتاً ببايت، ولا كلمةَ زائدة فوق عتبة C23؛ و`kicker_de` = `Park` في الثمانية. و**لا `Sie` مبتدئةً جملة** (تتأرجح بصرياً مع المخاطبة): أُعيدت صياغةُ موضعين، والدفعةُ خاليةٌ من المخاطبة أصلاً فلا خلط.

## د. `policy_for_owner`
1. **العلّة ب-2 تحتاج بتّاً**: توحيدُ `jawatha-mosque.value_de` على `8:00–24:00` أو إعفاء.
2. **لا `DISTRICT_NAMES.de`** في `src/data/districts.ts` (ar/en/zh فقط). الدفعةُ تضيف `Al-Faisal-Viertel` إلى سابقيه — تُؤخذ عند بناء `de` وإلا افترقت صفحةُ المنشأة عن صفحة المعلم.
3. **عشرةُ أسماءٍ بلا تحقق ويب** (موسومةٌ في ملف المعجم): أنسنة المدن · نلعب معاً · المهيدب · مستشفى الموسى · الخبر · وزارة الزراعة · المركز الوطني · حصن المشقر/هجر · أيام العرب · الملك عبدالله.

## هـ. فحوصٌ آليّة بعد آخر تحرير
52 حقلاً · kicker واحد · الرقيقتان متطابقتان · لا `answer_de`/`bestTime_de` · **C21 محاكاةً: PASS** · لا U+2014/U+00A0/شرطةٍ لينة/`"` مستقيمة (`„…“` ×7 متوازنة) · لا رقمَ عربيَّ-هنديّ ولا `Souq` ولا `du` · مسافتان حول `–` · محظوراتٌ مُسحت مصرَّفةً.

**مسحُ المعجم في الاتجاهين**: لا اسمَ علمٍ خارجه، وكلُّ مدخلٍ يذكره المصدر مستعمَلٌ بصيغته. والمُهمَلةُ ثلاثةٌ مُعلَّلة: `die Al-Ahsa-Oase` و`Saudi-Arabien` لا يسمّيهما المصدر («the oasis»/«the Kingdom»)، و`Imam Saud bin Abdulaziz` كُتب `Saud bin Abdulaziz, den späteren Imam` لأن المصدر يقول «later Imam».
