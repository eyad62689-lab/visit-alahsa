# المرحلة 5 — قائمة فحص المدقق اللغوي (دفعة المعالم الألمانية 1)

المدخل: `fields.stage4.json`. المخرج: `fields.stage5.json`. الدور: `de-translation/prompts/05-proofreader.md`.

**كيف قيس هذا الملف:** بسكربت `python3` يقرأ ملف الحقول نفسه ويطبع عدداً لكل بند — لا بالنظر. والتعابير النمطية لـ**C18 · C14 · C15 · C16** منسوخة حرفياً من `tools/check-consistency.mjs` كي لا يفترق المقياس عن الحارس.

**الدرسان المقيسان من دفعة المدونة الصينية 19، وقد التُزما:**
1. **المسح بحدود الكلمات لا بالكلمة مفردةً.** والفرق **قيس ولم يُفترض**: البحث الساذج عن `du` يعطي **9 إصابات** في هذه الدفعة (داخل `durch` · `Gebäude` … )، وبحدود الكلمات `\bdu\b` يعطي **0**. فلو كان المسح ساذجاً لأبلغ عن تسعة خروق وهمية — وهو الوجه المقابل للعلّة التي وقعت هناك.
2. **إعادة المسح كلَّه بعد آخر تحرير.** أُعيدت البنود العشرة على `fields.stage5.json` **بعد** كتابته وبعد كتابة `termbase-additions.stage4.json` (آخر تحرير لي)، وخرج السكربت بالرمز 0.

**الحصيلة: 9 بنود PASS من أول مسح · 1 بند FIXED (البند 10) · صفر تعديل في النصّ الألماني.**
`fields.stage5.json` **مطابق لـ`fields.stage4.json` بايتاً ببايت** (‏sha256 مقارن: `95233da6766e264b…` للاثنين) — والخرق الوحيد الذي وجدته المرحلة 5 كان في المعجم لا في النصّ، فعولج بمدخلٍ جديد.

---

## 1. C18 — صفر `Souq` في النصّ الألماني · **PASS**

| المقياس | العدد |
|---|---|
| `\bSouqs?\b` (تعبير الحارس نفسه) | **0** |
| بحث ساذج عن `Souq` بلا حدود | **0** |
| `\bSouks?\b` | **24**، في **3 صفحات من 5** |

الثلاث هي `qaisariyah` (عنواناً ولصيقةً ومتناً وأسئلة) و`craftsmen-souq` و`baiah` (في الإحالة `dem Qaisariyah-Souk`). و`Souk` داخل مركّبات الدوركوبلونگ (`Qaisariyah-Souk` · `Handwerker-Souk`) وفي المفرد المعرّف والجمع `Souks`.

## 2. C17 — شرطة U+2013 لا U+2014 · **PASS**

| المقياس | العدد |
|---|---|
| U+2014 (‏`—`) | **0** |
| U+2013 (‏`–`) | **16** |
| منها شرطة اعتراض بمسافتين ` – ` | **12** |
| منها نطاق لاصق بلا مسافة | **4** (‏`1853–1921` · `8:00–12:00` · `15:30–22:00` · `7:30–24:00`) |
| لاصقة ليست نطاقاً (خطأ) | **0** |

وجرد المحارف فوق ASCII في الدفعة كلها لا يحمل إلا: `ä ö ü Ä Ö ß` · U+2013 · U+201E/U+201C · U+00B7 (الفاصل `·` في بند المواعيد، على النمط المعتمد في `jabal-al-qarah`). **لا U+2010 ولا U+2212 ولا U+2014.**

## 3. C14 — `bestTime_de` تطابق تسميتها · **PASS**

| المقياس | العدد |
|---|---|
| حقول `bestTime_de` في الدفعة | **1** (‏`qaisariyah` وحدها — الأربع الباقية بلا `bestTime` في المصدر) |
| كلمات من `SEASONAL_DE` داخلها | **0** |
| كلمات موسمية في بقية الدفعة | **5** (‏`März`×2 · `Mai` · `Saison`×2) |

الخمس كلها **خارج** `bestTime_de` فلا يمسّها الحارس: `März 2020` تاريخ افتتاح، و`4. Mai 1913` تاريخ، و`in der Saison` في `lemon-farm` وهي بلا `bestTime` أصلاً. والقيمة المصيَّرة تحت `Beste Tageszeit` وقت نهارٍ خالص («Am Abend …») فالتسمية صادقة.

## 4. C4 · C15 · C16 · **PASS**

| المقياس | العدد |
|---|---|
| أرقام عربية-هندية `[٠-٩۰-۹]` | **0** |
| `größten?\s+Palmenoase` / `Dattelpalmenoase` | **0** |
| `Hochsommer[…]{0,100}Nacht` | **0** |

الدفعة لا تحمل دعوى «أكبر واحة» ولا وصف كهوف القارة أصلاً (لا `jabal-al-qarah` فيها)، فالبندان يمرّان بصفر إصابة **وبصفر حامل** — أي أن هذه الصفحات لا تضيف ولا تنقص من حارسَي C15/C16 القائمين على الصفحات الأخرى.

## 5. U+00A0 · الاقتباس · `du` · الصيغة السويسرية · **PASS**

| المقياس | العدد |
|---|---|
| U+00A0 (مسافة غير فاصلة) | **0** |
| اقتباس مستقيم `"` | **0** |
| فاصلة عليا U+0027 | **2** — وكلتاهما داخل `Bayt Al-Bay'ah` (‏`title_de` و`body_de`)، كما يوجب مدخل المعجم (لا `’` طباعية) |
| `„…“` | **2 / 2** متوازنة (‏`„Hasawi-Weberei“` · `„Besuchsinformationen“`) |
| `‚…‘` داخلي | **0** (لا اقتباس داخل اقتباس في الدفعة) |
| `\bdu\b` · `\bdein…\b` · `\bdich\b` · `\bdir\b` | **0** (بحثٌ ساذج عن `du`: **9** — الفرق هو الدرس المقيس) |
| صيغة سويسرية `ss` محلَّ `ß` | **0** |
| `ß` | **6**: `große`×2 · `großer` · `weiße` · `Gefäße` · `Fuß` |
| صيغ المخاطبة `Sie` الكبيرة | **5** · `Ihr…` الكبيرة: **0** |

وفُحصت المخاطبة يدوياً فوق العدد، لأن الآلة لا تميّز `ihr` الغائبة من `Ihr` المخاطَبة: الخمس كلها مخاطبةٌ صحيحة (`führen Sie` · `finden Sie`×2 · `legen Sie` · `verfolgen Sie`)، والتسع الصغيرة (`ihre Türen` · `ihrer Handstickerei` · `ihre Geschäfte` · `ihr Können` · `ihr Handwerk` · `ihre Arbeiten` · `ihren Wänden` …) كلها ضمائر غائب لمرجعٍ ظاهر — **فلا خلط بين `Sie` و`sie`/`ihr`**. وفحص `ß`/`ss` جرد الطرفين: الستّ أعلاه كلها بعد حرف علّة طويل أو مزدوج فتلزمها `ß` (‏`große` · `weiße` · `Fuß` · `Gefäße`)، والثلاث عشرة التي تحمل `ss` (‏`Adresse` · `Anlässe` · `Ausstellungen` · `Erdgeschoss` · `Erzeugnisse` · `Gassen` · `Geschosse` · `Muss` · `Obergeschoss` · `Presseberichten` · `Sammlungsstücke` · `dessen` · `schlossen`) كلها بعد حرف علّة قصير فتلزمها `ss` — **فلا واحدة منها صيغةٌ سويسرية محلَّ `ß`**.

## 6. Denglisch · **PASS**

| المقياس | العدد |
|---|---|
| `Spot · Location · Must-see · Hotspot · Guide · Feeling · Vibe · Shopping · Event` (بحدود الكلمات، مع اللواحق) | **0** |
| البحث الساذج نفسه بلا حدود | **0** |
| `\bHighlights?\b` | **0** (الحدّ: 1) |

والسلسلة الإنجليزية الوحيدة في الدفعة هي **`UNESCO Creative Cities`** — اسم برنامجٍ رسميّ أثبتته المرحلة 4 من اللجنة الألمانية لليونسكو، لا كلمة جنسٍ إنجليزية. وحظر الـDenglisch يقع على أسماء الأجناس لا على أسماء الأعلام، وللموقع سابقتان معتمدتان (`Heritage Commission` · `Land of Civilisations`). التفصيل في `termbase-additions.stage4.json`.

## 7. الأرقام والتواريخ والأوقات والعملة · **PASS**

| المقياس | العدد |
|---|---|
| نقطة آلاف | **1** (‏`12.000`) · فاصلة آلاف إنجليزية `12,000`: **0** |
| عشري بفاصلة | **0** مطلوب · عشري بنقطة (خطأ): **0** |
| نطاقات وقت `H:MM–H:MM` | **3** · `Uhr`: **9** · `am`/`pm` إنجليزية: **0** |
| تاريخ بالنمط `d. Monat JJJJ` | **1** (‏`4. Mai 1913`) — ولا تاريخ بنمطٍ آخر |
| `SAR` / `Saudi-Riyal` | **0 / 0** |
| صيغ الهجري | **3**: `nach der Hidschra` ×1 ثم `n. H.` ×2 |

**العملة:** الدفعة لا تحمل مبلغاً واحداً (رسوم `qaisariyah` مجانية و`craftsmen-souq` بلا بند رسوم)، فقاعدة «‏50 SAR وأول ذكر 50 Saudi-Riyal (SAR)» **لا موضع لها هنا** — والعدد صفر لا إغفال.
**الهجري:** الترتيب صحيح — الصيغة الكاملة في `summary_de` (وهي أول ما يُصيَّر على الصفحة، `DetailView.astro:230`) ثم المختصرة مرتين في المتن، على سابقة `ameeriah.md` المعتمدة. و`n. H.` بمسافة عادية U+0020 لا U+00A0 (انظر البند 5) — وهو ما تحذّر منه `_meta.style_rules.einheiten` بالذات لأنه يُفشل الحرّاس **بصمت**.

## 8. الگلوس — مرة واحدة لكل مصطلح، في المتن لا في العنوان · **PASS**

ستة گلوسات في الدفعة، ولكلٍّ ثلاثة أعداد: في المتن · في الصفحة كلها · في العنوان.

| الصفحة / المصطلح | المتن | الصفحة | العنوان | الترتيب محكوم بالعنوان |
|---|---|---|---|---|
| `craftsmen-souq` / `Tanur (Lehmofen)` | 1 | 1 | 0 | — (اسم جنس) |
| `baiah` / `Bayt Al-Bay'ah (das Haus des Treueids)` | 1 | 1 | 0 | ✔ العنوان نقحرة ⇒ النقحرة تتصدّر |
| `baiah` / `Qasr Ibrahim (dem Ibrahim-Palast)` | 1 | 1 | 0 | ✔ محكوم بعنوان صفحة القصر (`Qasr Ibrahim`) |
| `duqat` / `Jabal al-Qarah (des Qarah-Bergs)` | 1 | 1 | 0 | ✔ محكوم بعنوان صفحة الجبل (`Jabal al-Qarah`) |
| `duqat` / `Dougha Al-Gharash, ein traditionelles Töpferhaus` | 1 | 1 | 0 | ✔ |
| `lemon-farm` / `Hasawi-Limette, die Limette aus Al-Ahsa` | 1 | 1 | 0 | ✔ العنوان `Hasawi-Limettenfarm` ⇒ `Hasawi-Limette` تتصدّر |

**صفر گلوس في عنوان، وصفر تكرار لگلوسٍ داخل الصفحة الواحدة.** و`(Lomi)` ترد في النبذة والمتن معاً لأنها **نصّ المصدر** (‏`the famous Hasawi lime (lomi)` في الحقلين) لا گلوساً من صنع الترجمة.

## 9. التغطية والبنية · **PASS**

| المعلم | الحقول اللازمة | ناقص | `answer_de` | `practical` (ألماني/مصدر) | `faq` (ألماني/مصدر) | الترتيب |
|---|---|---|---|---|---|---|
| `qaisariyah` | 6/6 | 0 | 0 | 2 / 2 | 5 / 5 | ✔ |
| `craftsmen-souq` | 5/5 | 0 | 0 | 1 / 1 | 4 / 4 | ✔ |
| `baiah` | 5/5 | 0 | 0 | 0 / 0 | 0 / 0 | ✔ |
| `duqat-algharash` | 5/5 | 0 | 0 | 0 / 0 | 0 / 0 | ✔ |
| `lemon-farm` | 4/4 | 0 | 0 | 0 / 0 | 0 / 0 | ✔ |

اللوازم محسوبة من المصدر لا من قائمةٍ ثابتة: `area_de` تُطلب متى وُجد `area`/`area_en`، و`bestTime_de` متى وُجد `bestTime` — ولذلك `lemon-farm` أربعةٌ لا خمسة (لا `area` لها في المصدر) و`qaisariyah` ستة. **صفر `answer_de` في الدفعة كلها** (فقرة الإجابة عربية وإنجليزية فقط). وبنود `practical` كلها بمفاتيحها الثلاثة `label_de`/`value_de`/`source_de`، والأسئلة كلها بـ`q_de`/`a_de`. والترتيب فُحص بمراسي كلماتٍ مفتاحية لا بالعدد وحده: `[Öffnungszeiten, Eintritt]` و`[Öffnungszeiten, Eintritt, kaufen, besuche, alt]` لـ`qaisariyah`، و`[Öffnungszeiten]` و`[Öffnungszeiten, Was gibt es, Wo liegt, eröffnet]` لـ`craftsmen-souq` — كلها في مواضعها من ترتيب المصدر.

## 10. الالتزام المعجمي 100% · **FIXED**

| المقياس | العدد |
|---|---|
| مرشّحات اسم علم مستخرجة من النصّ | **44** |
| بلا مدخل في المعجم أو إضافات الدفعة (قبل الإصلاح) | **1** |
| بلا مدخل (بعد الإصلاح) | **0** |
| المفحوص عليه | `termbase.json` (‏62) + `termbase-additions.stage1.json` (‏17) + `termbase-additions.stage4.json` (‏6) |

**الخرق: `Lomi`.** المدخل القائم `lime (lomi / Hasawi lime) → die Hasawi-Limette` يحمل «lomi» في **جانبه الإنجليزي وحده**، فالكلمة ترد في النصّ الألماني اسماً مكتوباً بحرفٍ كبير بلا مدخلٍ ألماني يحمل جنسها. **الإصلاح في المعجم لا في النصّ:** أُضيف `die Lomi` (الجنس من `die Limette`، بمصدرٍ من المدخل القائم ومن `summary_en`/`body_en` ومن تحقيق المالك الميداني 2026-08-29 المسجَّل في رأس `lemon-farm.md`). **النصّ لم يتغيّر**: الكلمة تقع في القوس بعد الاسم القائد كما في المصدر تماماً.

والأربع والأربعون الباقية كلها مغطّاة، ومنها مداخل المرحلة 4 الخمسة المحسومة (‏`Dschumada` · `Creative`/`Cities` · `Kommunalverwaltung` · `Agentur` · `Zari-Fäden`) وصيغٌ مصرّفة رُدّت إلى جذورها قبل الحكم (`Qaisariyah-Souks` · `Handwerker-Souks` · `Al-Ahsas` · `Al-Ahsa-Reiseführers` · `Qarah-Bergs`).
