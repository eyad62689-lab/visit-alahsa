# زوروا الأحساء — قواعد العمل في هذا المستودع

موقع Astro 7 متعدد اللغات: عربي RTL في الجذر، إنجليزي تحت `/en/`، صيني جزئي تحت `/zh/`، ألماني جزئي تحت `/de/`، روسي جزئي تحت `/ru/`. يُنشر آلياً من `main` عبر Netlify إلى visit-alahsa.com. هذا الملف يجمع القواعد التي كانت موزّعة في تعليقات الكود ووثائق `docs/` — عند التعارض تَغلِب وثائق `docs/` الأحدث.

## القواعد التحريرية (غير قابلة للتفاوض)

- **لا تُختلق أي معلومة.** موعد أو رسم أو مسافة أو تاريخ لا يُنشر إلا بمصدر. ما لم يتأكد يوسم «بانتظار التأكيد» في بند `practical` بـ`verified: false`، وهو محجوب من الصفحة تلقائياً.
- **أرقام لاتينية (0-9) في كل النصوص** بكل اللغات. فاحص البناء (C4) يُفشل النشر عند أي رقم عربي-هندي.
- **أسماء المنشآت من خرائط قوقل الرسمية**، والمنشأة تُسمّى كما تسمّي نفسها. لا أسعار ولا أحكام جودة في بطاقات المطاعم والإقامة.
- **الاسم الشائع مقرون بالموثّق** في الصفحة نفسها («الخبز الأحمر» مع «خبز التمر»). المصطلحات المرفوضة في `tools/check-consistency.mjs`.
- **رقم واحد لكل حقيقة** عبر الموقع (عدد المعالم مشتق من المجموعة؛ النخيل «أكثر من 2.5 مليون»).

## قواعد الكود

- **الروابط:** كل رابط صفحة تفصيلية يُبنى من `src/lib/routes.ts` وينتهي بشرطة ختامية (تطابق canonical). لا تركيب مسارات يدوياً.
- **الترجمة:** نصوص الواجهة في `src/i18n/ui.ts`؛ مفاتيح `ar` و`en` متطابقة إلزامياً، و`zh` و`de` و`ru` جزئية تتراجع للإنجليزية. الكتل متعددة اللغة داخل الواجهات عبر `localize(lang, { ar, en, zh?, de?, ru? })` — **صيغة السجلّ منذ 2026-09-05**، والصيغة الموضعية أُزيلت فلا تكتبها.
- **الصينية** لا تُكتب إلا عبر `.claude/skills/zh-translation-pipeline` (المعجم `zh-translation/glossary/termbase.json` ملزم)، و**الألمانية** عبر `.claude/skills/de-translation-pipeline` (‏`de-translation/glossary/termbase.json` ملزم بأجناسه النحوية)، و**الروسية** عبر `.claude/skills/ru-translation-pipeline` (‏`ru-translation/glossary/termbase.json` ملزم بأجناسه وأحكام تصريفه). الخط الروسي **مُدمج منذ 2026-09-05**: نواة `/ru/` (الرئيسية وفهرس المعالم) وصفحة جبل القارة (91/100)، وحارس `C20` لانقسام الأداة، وru في أداة قياس الترويسة. **ودفعة `ui.ru` مرّت بالخط 2026-09-05 (91/100)**: 144 مفتاحاً + `CATEGORY_LABELS.ru` + السلاسل المضمّنة العشر؛ مؤجَّل `map.*`/`tmap.*`/`plan.*`/`blog.*` حتى تُبنى صفحاتها. الشريط الروسي داخل كتلة `[lang='ru']` المضغوطة مع الألمانية في `Header.astro` (قرار مقيس: 1391px للتسميات الطبيعية مقابل ميزانية 1188). و`C14`/`C15` وُسِّعا بالروسية.
- **السكربتات الداخلية** يجب أن تنجو من تنقّلات ClientRouter: ما يُسجَّل على `document`/`window` يُحرَس بحارس مرة واحدة، وما يخص الصفحة يُعاد عبر `astro:page-load`، والمؤقّتات تُلغى عند `astro:before-swap`.
- **التخزين المحلي** دائماً داخل `try/catch`.
- **القياس:** كل تفاعل يُقاس عبر `window.VATrack(name, params)` أو سمة `data-track` (ومعاملاتها `data-track-*`) من `src/scripts/analytics.ts` — لا نداء `gtag` مباشراً خارجها (الاستثناء: `page_view` و`ai_referral` في `Base.astro`). فحص القبول: `tools/test-analytics-events.mjs` على `dist` (تعليمات التشغيل في رأسه)، ويعمل في `build.yml` بعد البناء.
- **استقبال الزائر بلغته:** شريط اقتراح اللغة (`LangSuggest.astro`، يُصيَّر من `Base.astro` متى وُجد `alt`) يقترح أول لغة متصفح تسبق لغة الصفحة ولها نظير في `alt` — لا تحويل بالخادم أبداً. نصوصه للعربية والإنجليزية في `lang.suggest.*`؛ ولا تُكتب لـzh/de/ru إلا عبر خطوط الترجمة، وبدونها يظهر اسم اللغة الأصلي وحده. صفحة 404 تكشف كتلة لغة بادئة المسار، وبيان التطبيق لكل لغة من `src/pages/[lang]/site.webmanifest.ts`. فحص القبول: `tools/test-lang-suggest.mjs` (يشمل axe-core).
- **الأداء (الخطوة 3 من خطة التفاعل العالمي):** كل CSS مضمَّن في الصفحة (`inlineStylesheets: 'always'`) فلا ملف CSS حاجباً؛ الوجوه السيريلية تُعلَن في `fonts.css` بـ`unicode-range` (ملفات fontsource الفرعية بلا نطاق فتُنزَّل في كل صفحة)؛ خطوط Plex العربية مُشذَّبة إلى U+0600–06FF ونطاقها المعلَن يطابقها؛ فيديو الهيرو لا يبدأ إلا بعد `load` ثم خمول (وعلى الجوال بعد 3 ثوانٍ أخرى) ويُتخطّى على `saveData` و2g/3g؛ والصور الكسولة `fetchpriority="low"`؛ وأقسام الرئيسية تحت الطيّة تحمل `cv-auto` (`content-visibility: auto` مع ارتفاع تقديري) لأن كل وصول خط يعيد تخطيط الصفحة كلها وكان ذلك ما يرفع LCP المحاكى فوق حارس البوابة على عدّاء GitHub (قياس 2026-09-10: زمن التخطيط 490 ← 289 مللي ثانية بمحاكاة 4x) — لا يُطبَّق على قسم الهيرو. البوابة: `tools/lighthouse-gate.mjs` (ميزانيات حتمية + حارسا محاكاة) تعمل في `build.yml`؛ الهدف الميداني LCP ≤ 2.5 ث يُقرأ من حدث `web_vitals`.
- **الصور:** مسار أساسي بلا امتداد (`/img/name`)، ويُشغَّل `node tools/gen-image-widths.mjs` بعد إضافة أي صورة لتوليد النسخ والبيان.
- **بيانات قوقل** (التقييمات وأوقات العمل) تُعرض في الواجهة فقط ولا تدخل JSON-LD أبداً (C10).
- **الأمان:** لا مفاتيح في الكود؛ كلها متغيرات بيئة Netlify. لا IP خام في أي سجل.
- **الترويسة تُقاس ولا تُقدَّر:** عتبات انهيار الشريط في `Header.astro` مشتقّة من قياس فعلي بمتصفح لا مختارة بالنظر. بعد **أي** تغيير في تسمية تنقّل أو في عتبات الترويسة شغّل `tools/measure-header-fit.mjs` (تعليمات التشغيل في رأسه)، ويخرج بـ1 عند أي إخفاق. يفحص **خمس لغات** منذ 2026-09-05 (اللازم مقيساً: ar ‏1123px · en ‏1188 · zh ‏1023 · de ‏1298 · ru ‏1291 — الروسية والألمانية تحت الكتلة المضغوطة وتنهاران عند 1338px). علّة الإلزام: العتبة كانت 1199px مقيسةً على ثمانية روابط، فلمّا صاروا تسعة صارت تكذب على ثلاث لغات من أربع بلا أن يفشل بناءٌ أو فحص. **ولا يكفي غياب التراكب حكماً على الاتّساع**: عند تجاوز سقف الحاوية ينضغط `hd-actions` فيلتفّ «رحلتي» سطرين ويبدو الشريط سليماً — لذلك يشترط الحارس الارتفاع 68px والسطر الواحد معاً.

## عتبات النشر

- صفحة منشأة أو إقامة مفردة تُولَّد فقط إذا تجاوز متنها 80 كلمة عربية / 100 إنجليزية ولم يكن نسخة من النبذة.
- صفحة معلم بالصينية تُولَّد فقط بوجود `title_zh` معتمد من خط الترجمة، وبالألمانية بوجود `title_de`، وبالروسية بوجود `title_ru`.
- **«بالقرب مني» والمسافة:** الموقع الجغرافي مسموح للأصل نفسه (`geolocation=(self)` في `netlify.toml` و`public/_headers`)، ويُطلب بضغطة صريحة فقط (زر «موقعي» في `MapView` وزر «كم يبعد عني» في `DetailView`)، ويُحسب محلياً بـ`src/lib/distance.ts` ولا يُخزَّن ولا يُرسَل. صياغة المسافة لكل لغة في `DIST_LABELS` (نصوص zh/de/ru معتمدة سلفاً)؛ التسميات ar/en في `map.locate`/`det.distance`، وzh/de/ru أيقونة وحدها. فحص القبول: `tools/test-near-me.mjs`.
- **المشاركة والروابط العميقة:** `ShareButton.astro` في قوالب المعلم والمقال والمنشأة والإقامة والفعالية والخريطة (Web Share ثم نسخ الرابط، حدث `share`)؛ نصوصه ar/en في `share.btn`، وzh من `plan.copy` المعتمد، وde/ru أيقونة وحدها حتى تمرّ `share.*` بالخطوط. الخريطة تقرأ حالتها من الرابط وتكتبها (`?cat=` و`?q=` و`#slug` بـ`slug_en`) بـ`replaceState`. فحص القبول: `tools/test-share-map.mjs`.
- **سلامة اللغة (zh/de/ru):** صفحة معلَنة (تحمل hreflang) يجب أن يبقى نصّها غير المترجم دون 20% (كتل إنجليزية مطابقة لنظيرتها الإنجليزية) — يحرسها C22 على dist. ما يتجاوزها يُدرج في `src/i18n/unlisted.ts` فيخرج من hreflang وsitemap ومبدّل اللغة وشريط الاقتراح (وnoindex متى وُسم) حتى تكتمل ترجمته عبر الخطوط؛ وكتلة بلا نسخة بلغة الصفحة تُخفى (كـ`SeasonNow` على de/ru) لا تُعرض إنجليزية.
- **صفحة المعلم الرقيقة** (المتن + الأسئلة الشائعة دون 40 كلمة بالعربية أو الإنجليزية — `src/lib/publish.ts`) تُبنى للزائر لكنها `noindex` بلا hreflang وخارج sitemap في كل لغاتها حتى يصل متن مصدري؛ يحرسها C23 على dist. لا تُكتب كلمة لرفع صفحة فوق العتبة بلا مصدر.
- **بطاقات المشاركة (OG):** الصفحة بلا صورة خاصة تأخذ الافتراضية بلغتها (`og-default.png` للعربية و`og-default-{lang}.png` لغيرها، تولّدها `tools/gen-og-images.mjs` محلياً من نصوص القواميس المعتمدة)؛ صفحات المنشآت والإقامة تمرّر صورتها. يحرسها C23.
- **الجداول والمواعيد المبنيَنة:** كل جدول في الموقع يُبنى بـ`DataTable.astro` من `src/lib/tables.ts` ومن مصادر الحقيقة القائمة (بنود `practical` الموثّقة، `events.ts`، `fruits.ts`) — لا جدول يدوي ولا رقم فيه من خارج مصدره؛ ورؤوس أعمدته لـzh/de/ru من نصوص معتمدة سلفاً وإلا لا يُبنى بتلك اللغة. `hoursSpec`/`fee` في ملف المعلم نسخة آلية القراءة من بند `practical` موثّق نفسه (المخطط يرفضهما بدونه) وتغذّي `openingHoursSpecification` و`isAccessibleForFree`/`offers` — وبيانات قوقل تبقى خارج JSON-LD (C10). يحرسها C24 على dist.
- **فقرة الإجابة (`answer`/`answer_en`):** 35–45 كلمة تتصدّر متن صفحة المعلم وتصير وصفها، تُركَّب حصراً من متن الصفحة نفسها وبطاقة زيارتها (كل رقم فيها يجب أن يرد في بقية المتن أو البطاقة — يحرسه C24، والمخطط يرفض الحدود والنقص بلغة). لا تُكتب لـzh/de/ru إلا عبر خطوط الترجمة. صفحة «واحة الأحساء في اليونسكو» (`UnescoView.astro`) تُركَّب من المعالم الحاملة لـ`unesco` في ترويستها ولا تُكتب فيها حقيقة عن مكوّن غير موجود في الموقع — القائمة الكاملة إحالة إلى صفحة اليونسكو الرسمية.
- **الربط الداخلي:** المقالات تربط المعالم بروابط صريحة، و`src/lib/related.ts` يقلبها وقت البناء إلى «مقالات تذكر هذا المعلم» بلغة الصفحة — لا قائمة يدوية. حيّ المعلم (`district`) بمفاتيح `src/data/districts.ts` (المصدر الواحد للمفاتيح والتسميات) ولا يُسند إلا حين يسمّيه نصّ `area` أو العنوان (يرفضه المخطط وإلا)؛ صفحة المنشأة تعرض معالم حيّها منه. كل رابط ذي صلة يحمل `data-track="related_click"` مع `kind` و`item_id`. يحرسه C25 على dist، و`tools/check-links.mjs` في `postbuild` يُفشل البناء عند أي هدف داخلي مفقود أو رابط صفحة بلا شرطة ختامية.
- مقال المدونة `draft: true` لا يُبنى ولا يدخل الخريطة.
- **المدونة متعددة اللغات (الخطوة 10):** كل لغة ملف Markdown مستقل يقترن بنظائره بحقل `key` (‏`lang ∈ ar|en|zh|de|ru`)؛ الـslug اللاتيني مشترك بين en/zh/de/ru تحت بادئة لغته، لذلك **معرّف المدخل من اسم الملف** (`generateId` في محمّل المدونة) لا من `slug` — وإلا تصادمت المعرّفات وطُمس مقالٌ بصمت. المقال لا يُكتب بـzh/de/ru إلا عبر خط ترجمته بعتبة 90 وقارئ أعمى، وملفه يُنشر في `src/content/blog/` مع دمج `tm.json` و`scores.csv` ومعجمه. الروابط داخل المقال المترجم تقصد صفحة لغته إن وُجدت وإلا الإنجليزية (لا رابط إلى صفحة غير مبنية — يحرسه `check-links`)، و`related.ts` يلتقطها تلقائياً. نظائر المقال من `postAlt` (‏`src/lib/blog.ts`) مصدراً لـhreflang ومبدّل اللغة؛ de/ru بلا فهرس مدونة فيقصدان الإنجليزي؛ و`/zh/blog/` يبقى في `unlisted.ts` ما دام فوق عتبة C22 (83% بمقالين من ثمانية). يحرس C26 تبادل hreflang على كل الصفحات.

## التحقق قبل الدفع

```bash
npm run build      # astro check (0 أخطاء، 0 تحذيرات) + build + pagefind + فاحص التضارب
```

الفاحص يعمل على `dist/` لا على المصدر، ويُفشل البناء عند أي تضارب. التقارير والقرارات المعمارية في `docs/`.
